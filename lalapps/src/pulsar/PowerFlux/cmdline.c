/*
  File autogenerated by gengetopt version 2.18
  generated with the following command:
  gengetopt --string-parser --conf-parser 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

const char *gengetopt_args_info_purpose = "Powerflux analysis program";

const char *gengetopt_args_info_usage = "Usage: " CMDLINE_PARSER_PACKAGE " [OPTIONS]...";

const char *gengetopt_args_info_help[] = {
  "  -h, --help                    Print help and exit",
  "  -V, --version                 Print version and exit",
  "  -c, --config=STRING           configuration file (in gengetopt format) to \n                                  pass parameters",
  "      --label=STRING            arbitrary string to be printed in the beginning \n                                  of PowerFlux log file",
  "      --sky-grid=STRING         sky grid type (arcsin, plain_rectangular, \n                                  sin_theta)  (default=`sin_theta')",
  "      --skymap-orientation=STRING\n                                orientation of produced skymaps: equatorial, \n                                  ecliptic, band_axis  (default=`equatorial')",
  "      --skyband-method=STRING   method of assigning band numbers: angle, S  \n                                  (default=`S')",
  "      --nskybands=INT           split sky in this many bands for logging \n                                  maximum upper limits  (default=`11')",
  "      --large-S=DOUBLE          value of S to consider good enough",
  "      --band-axis=STRING        which band axis to use for splitting sky into \n                                  bands (perpendicular to band axis) (possible \n                                  values: equatorial, auto, \n                                  explicit(float,float,float)  (default=`auto')",
  "      --band-axis-norm=DOUBLE   norm of band axis vector to use in S value \n                                  calculation",
  "      --sky-marks-file=STRING   file describing how to mark up a sky",
  "      --fine-factor=INT         make fine grid this times finer  (default=`5')",
  "      --skymap-resolution=DOUBLE\n                                specify skymap resolution explicitly",
  "      --skymap-resolution-ratio=DOUBLE\n                                adjust default coarseness of the grid by this \n                                  factor  (default=`1.0')",
  "      --small-weight-ratio=DOUBLE\n                                ratio that determines which weight is too small \n                                  to include in max statistics  (default=`0.2')",
  "      --strain-norm-factor=DOUBLE\n                                strain normalization factor to prevent \n                                  overflowing of the exponent  \n                                  (default=`1e-20')",
  "      --lock-file=STRING        file to lock when reading SFTs in order to \n                                  globally serialize disk access",
  "      --enable-dataset-locking=INT\n                                set to 1 to enable dataset level locking  \n                                  (default=`0')",
  "  -s, --dataset=STRING          dataset file",
  "      --input-format=STRING     format of input files (GEO, SFT, Power)  \n                                  (default=`GEO')",
  "      --dump-data=STRING        file to output loaded SFT data into, for \n                                  testing",
  "      --dump-sftv2=STRING       directory to output loaded data, together with \n                                  dataset description",
  "  -o, --output=STRING           output directory",
  "      --ephemeris-path=STRING   path to detresponse program from lalapps",
  "      --earth-ephemeris=STRING  Earth ephemeris file, overrides ephemeris-path \n                                  argument",
  "      --sun-ephemeris=STRING    Sun ephemeris file, overrides ephemeris-path \n                                  argument",
  "  -f, --first-bin=INT           first frequency bin in the band to be analyzed",
  "  -n, --nbins=INT               number of frequency bins to analyze  \n                                  (default=`501')",
  "      --side-cut=INT            number of bins to cut from each side due to \n                                  corruption from doppler shifts",
  "      --expected-timebase=INT   expected timebase in months  (default=`6')",
  "      --hist-bins=INT           number of bins to use when producing histograms \n                                   (default=`200')",
  "  -d, --detector=STRING         detector location (i.e. LHO or LLO), passed to \n                                  detresponse",
  "      --doppler-multiplier=DOUBLE\n                                a constant to multiply Doppler shifts by (1.0 \n                                  corresponds to standard physics)  \n                                  (default=`1.0')",
  "      --spindown-start-time=DOUBLE\n                                specify spindown start time in GPS sec. Assumed \n                                  to be the first SFT segment by default",
  "      --frequency-offset=DOUBLE (small) frequency offset - used to achieve \n                                  fractional bin shifts  (default=`0.0')",
  "      --spindown-start=DOUBLE   first spindown value to process  \n                                  (default=`0.0')",
  "      --spindown-step=DOUBLE    step for processing multiple spindown values  \n                                  (default=`5e-10')",
  "      --spindown-count=INT      how many separate spindown values to process  \n                                  (default=`1')",
  "      --fdotdot=DOUBLE          second frequency derivative  (default=`0.0')",
  "      --orientation=DOUBLE      additional orientation phase, specifying 0.7853 \n                                  will turn plus into cross  (default=`0')",
  "      --nlinear-polarizations=INT\n                                even number of linear polarizations to profile, \n                                  distributed uniformly between 0 and PI/2  \n                                  (default=`4')",
  "      --no-demodulation=INT     do not perform demodulation stage, analyze \n                                  background only  (default=`0')",
  "      --no-decomposition=INT    do not perform noise decomposition stage, \n                                  output simple statistics only  (default=`0')",
  "      --no-candidates=INT       do not perform analysis to identify candidates  \n                                  (default=`0')",
  "      --no-am-response=INT      force AM_response() function to return 1.0 \n                                  irrespective of the arguments  (default=`0')",
  "      --no-secondary-skymaps=INT\n                                do not store values not essential for upper \n                                  limits and followup  (default=`0')",
  "      --averaging-mode=STRING   1 - use one bin, 3 - average 3, matched - use 7 \n                                  bin matched filter  (default=`1')",
  "      --subtract-background=INT subtract rank 1 matrix in order to flatten \n                                  noise spectrum  (default=`0')",
  "      --do-cutoff=INT           neglect contribution from SFT with high \n                                  effective noise level  (default=`1')",
  "      --filter-lines=INT        perform detection of lines in background noise \n                                  and veto corresponding frequency bins  \n                                  (default=`1')",
  "      --ks-test=INT             perform Kolmogorov-Smirnov test for normality \n                                  of averaged powers  (default=`1')",
  "      --compute-betas=INT       compute beta coefficients as described in \n                                  PowerFlux polarizations document  \n                                  (default=`0')",
  "      --upper-limit-comp=STRING upper limit compensation factor - used to \n                                  account for windowing in SFTs (possible \n                                  values: Hann, flat, arbitrary number)  \n                                  (default=`Hann')",
  "      --lower-limit-comp=STRING lower limit compensation factor - used to \n                                  account for windowing in SFTs (possible \n                                  values: Hann, flat, arbitrary number)  \n                                  (default=`Hann')",
  "      --write-dat=STRING        regular expression describing which *.dat files \n                                  to write  (default=`.*')",
  "      --write-png=STRING        regular expression describing which *.png files \n                                  to write  (default=`.*')",
  "      --dump-points=INT         output averaged power bins for each point in \n                                  the sky  (default=`0')",
  "      --dump-candidates=INT     output SFT data for first N candidates  \n                                  (default=`0')",
  "      --focus-ra=DOUBLE         focus computation on a circular area with \n                                  center at this RA",
  "      --focus-dec=DOUBLE        focus computation on a circular area with \n                                  center at this DEC",
  "      --focus-radius=DOUBLE     focus computation on a circular area with this \n                                  radius",
  "      --only-large-cos=DOUBLE   restrict computation to points on the sky with \n                                  cos of angle to band axis larger than a given \n                                  number",
  "\n Group: injection",
  "      --fake-linear             Inject linearly polarized fake signal",
  "      --fake-circular           Inject circularly polarized fake signal",
  "      --fake-ref-time=DOUBLE    time of signal start  (default=`0')",
  "      --fake-ra=DOUBLE          RA of fake signal to inject  (default=`3.14')",
  "      --fake-dec=DOUBLE         DEC of fake signal to inject  (default=`0.0')",
  "      --fake-iota=DOUBLE        iota of fake signal to inject  (default=`0.0')",
  "      --fake-psi=DOUBLE         orientation of fake signal to inject  \n                                  (default=`0.0')",
  "      --fake-phi=DOUBLE         phase of fake signal to inject  (default=`0.0')",
  "      --fake-spindown=DOUBLE    spindown of fake signal to inject  \n                                  (default=`0.0')",
  "      --fake-strain=DOUBLE      amplitude of fake signal to inject  \n                                  (default=`1e-23')",
  "      --fake-freq=DOUBLE        frequency of fake signal to inject",
  "      --snr-precision=DOUBLE    Assumed level of error in detection strength - \n                                  used for listing candidates  (default=`0.2')",
  "      --max-candidates=INT      Do not optimize more than this number of \n                                  candidates  (default=`-1')",
  "      --min-candidate-snr=DOUBLE\n                                Do not optimize candidates with SNR below this \n                                  level  (default=`5.0')",
  "      --output-initial=INT      write initial candidates into log file  \n                                  (default=`0')",
  "      --output-optimized=INT    write optimized (second pass) candidates into \n                                  log file  (default=`0')",
  "      --output-cache=INT        write out all candidates in cache to log file  \n                                  (default=`0')",
  "      --extended-test=INT       Perform extended self test  (default=`0')",
  "      --max-sft-report=INT      Maximum count of SFTs to report with veto \n                                  information  (default=`100')",
  "      --num-threads=INT         Use that many threads for computation  \n                                  (default=`-1')",
    0
};

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);
struct line_list
{
  char * string_arg;
  struct line_list * next;
};

static struct line_list *cmd_line_list = 0;
static struct line_list *cmd_line_list_tmp = 0;

static void
free_cmd_list(void)
{
  /* free the list of a previous call */
  if (cmd_line_list)
    {
      while (cmd_line_list) {
        cmd_line_list_tmp = cmd_line_list;
        cmd_line_list = cmd_line_list->next;
        free (cmd_line_list_tmp->string_arg);
        free (cmd_line_list_tmp);
      }
    }
}


struct string_list
{
  char * arg;
  char *orig;
  struct string_list * next;
};

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->config_given = 0 ;
  args_info->label_given = 0 ;
  args_info->sky_grid_given = 0 ;
  args_info->skymap_orientation_given = 0 ;
  args_info->skyband_method_given = 0 ;
  args_info->nskybands_given = 0 ;
  args_info->large_S_given = 0 ;
  args_info->band_axis_given = 0 ;
  args_info->band_axis_norm_given = 0 ;
  args_info->sky_marks_file_given = 0 ;
  args_info->fine_factor_given = 0 ;
  args_info->skymap_resolution_given = 0 ;
  args_info->skymap_resolution_ratio_given = 0 ;
  args_info->small_weight_ratio_given = 0 ;
  args_info->strain_norm_factor_given = 0 ;
  args_info->lock_file_given = 0 ;
  args_info->enable_dataset_locking_given = 0 ;
  args_info->dataset_given = 0 ;
  args_info->input_format_given = 0 ;
  args_info->dump_data_given = 0 ;
  args_info->dump_sftv2_given = 0 ;
  args_info->output_given = 0 ;
  args_info->ephemeris_path_given = 0 ;
  args_info->earth_ephemeris_given = 0 ;
  args_info->sun_ephemeris_given = 0 ;
  args_info->first_bin_given = 0 ;
  args_info->nbins_given = 0 ;
  args_info->side_cut_given = 0 ;
  args_info->expected_timebase_given = 0 ;
  args_info->hist_bins_given = 0 ;
  args_info->detector_given = 0 ;
  args_info->doppler_multiplier_given = 0 ;
  args_info->spindown_start_time_given = 0 ;
  args_info->frequency_offset_given = 0 ;
  args_info->spindown_start_given = 0 ;
  args_info->spindown_step_given = 0 ;
  args_info->spindown_count_given = 0 ;
  args_info->fdotdot_given = 0 ;
  args_info->orientation_given = 0 ;
  args_info->nlinear_polarizations_given = 0 ;
  args_info->no_demodulation_given = 0 ;
  args_info->no_decomposition_given = 0 ;
  args_info->no_candidates_given = 0 ;
  args_info->no_am_response_given = 0 ;
  args_info->no_secondary_skymaps_given = 0 ;
  args_info->averaging_mode_given = 0 ;
  args_info->subtract_background_given = 0 ;
  args_info->do_cutoff_given = 0 ;
  args_info->filter_lines_given = 0 ;
  args_info->ks_test_given = 0 ;
  args_info->compute_betas_given = 0 ;
  args_info->upper_limit_comp_given = 0 ;
  args_info->lower_limit_comp_given = 0 ;
  args_info->write_dat_given = 0 ;
  args_info->write_png_given = 0 ;
  args_info->dump_points_given = 0 ;
  args_info->dump_candidates_given = 0 ;
  args_info->focus_ra_given = 0 ;
  args_info->focus_dec_given = 0 ;
  args_info->focus_radius_given = 0 ;
  args_info->only_large_cos_given = 0 ;
  args_info->fake_linear_given = 0 ;
  args_info->fake_circular_given = 0 ;
  args_info->fake_ref_time_given = 0 ;
  args_info->fake_ra_given = 0 ;
  args_info->fake_dec_given = 0 ;
  args_info->fake_iota_given = 0 ;
  args_info->fake_psi_given = 0 ;
  args_info->fake_phi_given = 0 ;
  args_info->fake_spindown_given = 0 ;
  args_info->fake_strain_given = 0 ;
  args_info->fake_freq_given = 0 ;
  args_info->snr_precision_given = 0 ;
  args_info->max_candidates_given = 0 ;
  args_info->min_candidate_snr_given = 0 ;
  args_info->output_initial_given = 0 ;
  args_info->output_optimized_given = 0 ;
  args_info->output_cache_given = 0 ;
  args_info->extended_test_given = 0 ;
  args_info->max_sft_report_given = 0 ;
  args_info->num_threads_given = 0 ;
  args_info->injection_group_counter = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->config_arg = NULL;
  args_info->config_orig = NULL;
  args_info->label_arg = NULL;
  args_info->label_orig = NULL;
  args_info->sky_grid_arg = gengetopt_strdup ("sin_theta");
  args_info->sky_grid_orig = NULL;
  args_info->skymap_orientation_arg = gengetopt_strdup ("equatorial");
  args_info->skymap_orientation_orig = NULL;
  args_info->skyband_method_arg = gengetopt_strdup ("S");
  args_info->skyband_method_orig = NULL;
  args_info->nskybands_arg = 11;
  args_info->nskybands_orig = NULL;
  args_info->large_S_orig = NULL;
  args_info->band_axis_arg = gengetopt_strdup ("auto");
  args_info->band_axis_orig = NULL;
  args_info->band_axis_norm_orig = NULL;
  args_info->sky_marks_file_arg = NULL;
  args_info->sky_marks_file_orig = NULL;
  args_info->fine_factor_arg = 5;
  args_info->fine_factor_orig = NULL;
  args_info->skymap_resolution_orig = NULL;
  args_info->skymap_resolution_ratio_arg = 1.0;
  args_info->skymap_resolution_ratio_orig = NULL;
  args_info->small_weight_ratio_arg = 0.2;
  args_info->small_weight_ratio_orig = NULL;
  args_info->strain_norm_factor_arg = 1e-20;
  args_info->strain_norm_factor_orig = NULL;
  args_info->lock_file_arg = NULL;
  args_info->lock_file_orig = NULL;
  args_info->enable_dataset_locking_arg = 0;
  args_info->enable_dataset_locking_orig = NULL;
  args_info->dataset_arg = NULL;
  args_info->dataset_orig = NULL;
  args_info->input_format_arg = gengetopt_strdup ("GEO");
  args_info->input_format_orig = NULL;
  args_info->dump_data_arg = NULL;
  args_info->dump_data_orig = NULL;
  args_info->dump_sftv2_arg = NULL;
  args_info->dump_sftv2_orig = NULL;
  args_info->output_arg = NULL;
  args_info->output_orig = NULL;
  args_info->ephemeris_path_arg = NULL;
  args_info->ephemeris_path_orig = NULL;
  args_info->earth_ephemeris_arg = NULL;
  args_info->earth_ephemeris_orig = NULL;
  args_info->sun_ephemeris_arg = NULL;
  args_info->sun_ephemeris_orig = NULL;
  args_info->first_bin_orig = NULL;
  args_info->nbins_arg = 501;
  args_info->nbins_orig = NULL;
  args_info->side_cut_orig = NULL;
  args_info->expected_timebase_arg = 6;
  args_info->expected_timebase_orig = NULL;
  args_info->hist_bins_arg = 200;
  args_info->hist_bins_orig = NULL;
  args_info->detector_arg = NULL;
  args_info->detector_orig = NULL;
  args_info->doppler_multiplier_arg = 1.0;
  args_info->doppler_multiplier_orig = NULL;
  args_info->spindown_start_time_orig = NULL;
  args_info->frequency_offset_arg = 0.0;
  args_info->frequency_offset_orig = NULL;
  args_info->spindown_start_arg = 0.0;
  args_info->spindown_start_orig = NULL;
  args_info->spindown_step_arg = 5e-10;
  args_info->spindown_step_orig = NULL;
  args_info->spindown_count_arg = 1;
  args_info->spindown_count_orig = NULL;
  args_info->fdotdot_arg = 0.0;
  args_info->fdotdot_orig = NULL;
  args_info->orientation_arg = 0;
  args_info->orientation_orig = NULL;
  args_info->nlinear_polarizations_arg = 4;
  args_info->nlinear_polarizations_orig = NULL;
  args_info->no_demodulation_arg = 0;
  args_info->no_demodulation_orig = NULL;
  args_info->no_decomposition_arg = 0;
  args_info->no_decomposition_orig = NULL;
  args_info->no_candidates_arg = 0;
  args_info->no_candidates_orig = NULL;
  args_info->no_am_response_arg = 0;
  args_info->no_am_response_orig = NULL;
  args_info->no_secondary_skymaps_arg = 0;
  args_info->no_secondary_skymaps_orig = NULL;
  args_info->averaging_mode_arg = gengetopt_strdup ("1");
  args_info->averaging_mode_orig = NULL;
  args_info->subtract_background_arg = 0;
  args_info->subtract_background_orig = NULL;
  args_info->do_cutoff_arg = 1;
  args_info->do_cutoff_orig = NULL;
  args_info->filter_lines_arg = 1;
  args_info->filter_lines_orig = NULL;
  args_info->ks_test_arg = 1;
  args_info->ks_test_orig = NULL;
  args_info->compute_betas_arg = 0;
  args_info->compute_betas_orig = NULL;
  args_info->upper_limit_comp_arg = gengetopt_strdup ("Hann");
  args_info->upper_limit_comp_orig = NULL;
  args_info->lower_limit_comp_arg = gengetopt_strdup ("Hann");
  args_info->lower_limit_comp_orig = NULL;
  args_info->write_dat_arg = gengetopt_strdup (".*");
  args_info->write_dat_orig = NULL;
  args_info->write_png_arg = gengetopt_strdup (".*");
  args_info->write_png_orig = NULL;
  args_info->dump_points_arg = 0;
  args_info->dump_points_orig = NULL;
  args_info->dump_candidates_arg = 0;
  args_info->dump_candidates_orig = NULL;
  args_info->focus_ra_orig = NULL;
  args_info->focus_dec_orig = NULL;
  args_info->focus_radius_orig = NULL;
  args_info->only_large_cos_orig = NULL;
  args_info->fake_ref_time_arg = 0;
  args_info->fake_ref_time_orig = NULL;
  args_info->fake_ra_arg = 3.14;
  args_info->fake_ra_orig = NULL;
  args_info->fake_dec_arg = 0.0;
  args_info->fake_dec_orig = NULL;
  args_info->fake_iota_arg = 0.0;
  args_info->fake_iota_orig = NULL;
  args_info->fake_psi_arg = 0.0;
  args_info->fake_psi_orig = NULL;
  args_info->fake_phi_arg = 0.0;
  args_info->fake_phi_orig = NULL;
  args_info->fake_spindown_arg = 0.0;
  args_info->fake_spindown_orig = NULL;
  args_info->fake_strain_arg = 1e-23;
  args_info->fake_strain_orig = NULL;
  args_info->fake_freq_orig = NULL;
  args_info->snr_precision_arg = 0.2;
  args_info->snr_precision_orig = NULL;
  args_info->max_candidates_arg = -1;
  args_info->max_candidates_orig = NULL;
  args_info->min_candidate_snr_arg = 5.0;
  args_info->min_candidate_snr_orig = NULL;
  args_info->output_initial_arg = 0;
  args_info->output_initial_orig = NULL;
  args_info->output_optimized_arg = 0;
  args_info->output_optimized_orig = NULL;
  args_info->output_cache_arg = 0;
  args_info->output_cache_orig = NULL;
  args_info->extended_test_arg = 0;
  args_info->extended_test_orig = NULL;
  args_info->max_sft_report_arg = 100;
  args_info->max_sft_report_orig = NULL;
  args_info->num_threads_arg = -1;
  args_info->num_threads_orig = NULL;
  
}

static
void init_args_info(struct gengetopt_args_info *args_info)
{
  args_info->help_help = gengetopt_args_info_help[0] ;
  args_info->version_help = gengetopt_args_info_help[1] ;
  args_info->config_help = gengetopt_args_info_help[2] ;
  args_info->config_min = -1;
  args_info->config_max = -1;
  args_info->label_help = gengetopt_args_info_help[3] ;
  args_info->sky_grid_help = gengetopt_args_info_help[4] ;
  args_info->skymap_orientation_help = gengetopt_args_info_help[5] ;
  args_info->skyband_method_help = gengetopt_args_info_help[6] ;
  args_info->nskybands_help = gengetopt_args_info_help[7] ;
  args_info->large_S_help = gengetopt_args_info_help[8] ;
  args_info->band_axis_help = gengetopt_args_info_help[9] ;
  args_info->band_axis_norm_help = gengetopt_args_info_help[10] ;
  args_info->sky_marks_file_help = gengetopt_args_info_help[11] ;
  args_info->fine_factor_help = gengetopt_args_info_help[12] ;
  args_info->skymap_resolution_help = gengetopt_args_info_help[13] ;
  args_info->skymap_resolution_ratio_help = gengetopt_args_info_help[14] ;
  args_info->small_weight_ratio_help = gengetopt_args_info_help[15] ;
  args_info->strain_norm_factor_help = gengetopt_args_info_help[16] ;
  args_info->lock_file_help = gengetopt_args_info_help[17] ;
  args_info->enable_dataset_locking_help = gengetopt_args_info_help[18] ;
  args_info->dataset_help = gengetopt_args_info_help[19] ;
  args_info->input_format_help = gengetopt_args_info_help[20] ;
  args_info->dump_data_help = gengetopt_args_info_help[21] ;
  args_info->dump_sftv2_help = gengetopt_args_info_help[22] ;
  args_info->output_help = gengetopt_args_info_help[23] ;
  args_info->ephemeris_path_help = gengetopt_args_info_help[24] ;
  args_info->earth_ephemeris_help = gengetopt_args_info_help[25] ;
  args_info->sun_ephemeris_help = gengetopt_args_info_help[26] ;
  args_info->first_bin_help = gengetopt_args_info_help[27] ;
  args_info->nbins_help = gengetopt_args_info_help[28] ;
  args_info->side_cut_help = gengetopt_args_info_help[29] ;
  args_info->expected_timebase_help = gengetopt_args_info_help[30] ;
  args_info->hist_bins_help = gengetopt_args_info_help[31] ;
  args_info->detector_help = gengetopt_args_info_help[32] ;
  args_info->doppler_multiplier_help = gengetopt_args_info_help[33] ;
  args_info->spindown_start_time_help = gengetopt_args_info_help[34] ;
  args_info->frequency_offset_help = gengetopt_args_info_help[35] ;
  args_info->spindown_start_help = gengetopt_args_info_help[36] ;
  args_info->spindown_step_help = gengetopt_args_info_help[37] ;
  args_info->spindown_count_help = gengetopt_args_info_help[38] ;
  args_info->fdotdot_help = gengetopt_args_info_help[39] ;
  args_info->orientation_help = gengetopt_args_info_help[40] ;
  args_info->nlinear_polarizations_help = gengetopt_args_info_help[41] ;
  args_info->no_demodulation_help = gengetopt_args_info_help[42] ;
  args_info->no_decomposition_help = gengetopt_args_info_help[43] ;
  args_info->no_candidates_help = gengetopt_args_info_help[44] ;
  args_info->no_am_response_help = gengetopt_args_info_help[45] ;
  args_info->no_secondary_skymaps_help = gengetopt_args_info_help[46] ;
  args_info->averaging_mode_help = gengetopt_args_info_help[47] ;
  args_info->subtract_background_help = gengetopt_args_info_help[48] ;
  args_info->do_cutoff_help = gengetopt_args_info_help[49] ;
  args_info->filter_lines_help = gengetopt_args_info_help[50] ;
  args_info->ks_test_help = gengetopt_args_info_help[51] ;
  args_info->compute_betas_help = gengetopt_args_info_help[52] ;
  args_info->upper_limit_comp_help = gengetopt_args_info_help[53] ;
  args_info->lower_limit_comp_help = gengetopt_args_info_help[54] ;
  args_info->write_dat_help = gengetopt_args_info_help[55] ;
  args_info->write_png_help = gengetopt_args_info_help[56] ;
  args_info->dump_points_help = gengetopt_args_info_help[57] ;
  args_info->dump_candidates_help = gengetopt_args_info_help[58] ;
  args_info->focus_ra_help = gengetopt_args_info_help[59] ;
  args_info->focus_dec_help = gengetopt_args_info_help[60] ;
  args_info->focus_radius_help = gengetopt_args_info_help[61] ;
  args_info->only_large_cos_help = gengetopt_args_info_help[62] ;
  args_info->fake_linear_help = gengetopt_args_info_help[63] ;
  args_info->fake_circular_help = gengetopt_args_info_help[64] ;
  args_info->fake_ref_time_help = gengetopt_args_info_help[65] ;
  args_info->fake_ra_help = gengetopt_args_info_help[66] ;
  args_info->fake_dec_help = gengetopt_args_info_help[67] ;
  args_info->fake_iota_help = gengetopt_args_info_help[68] ;
  args_info->fake_psi_help = gengetopt_args_info_help[69] ;
  args_info->fake_phi_help = gengetopt_args_info_help[70] ;
  args_info->fake_spindown_help = gengetopt_args_info_help[71] ;
  args_info->fake_strain_help = gengetopt_args_info_help[72] ;
  args_info->fake_freq_help = gengetopt_args_info_help[73] ;
  args_info->snr_precision_help = gengetopt_args_info_help[74] ;
  args_info->max_candidates_help = gengetopt_args_info_help[75] ;
  args_info->min_candidate_snr_help = gengetopt_args_info_help[76] ;
  args_info->output_initial_help = gengetopt_args_info_help[77] ;
  args_info->output_optimized_help = gengetopt_args_info_help[78] ;
  args_info->output_cache_help = gengetopt_args_info_help[79] ;
  args_info->extended_test_help = gengetopt_args_info_help[80] ;
  args_info->max_sft_report_help = gengetopt_args_info_help[81] ;
  args_info->num_threads_help = gengetopt_args_info_help[82] ;
  
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

void
cmdline_parser_print_help (void)
{
  int i = 0;
  cmdline_parser_print_version ();

  if (strlen(gengetopt_args_info_purpose) > 0)
    printf("\n%s\n", gengetopt_args_info_purpose);

  printf("\n%s\n\n", gengetopt_args_info_usage);
  while (gengetopt_args_info_help[i])
    printf("%s\n", gengetopt_args_info_help[i++]);
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
  init_args_info (args_info);
}

static void
cmdline_parser_release (struct gengetopt_args_info *args_info)
{
  
  unsigned int i;
  if (args_info->config_arg)
    {
      for (i = 0; i < args_info->config_given; ++i)
        {
          if (args_info->config_arg [i])
            {
              free (args_info->config_arg [i]); /* free previous argument */
              args_info->config_arg [i] = 0;
            }
          if (args_info->config_orig [i])
            {
              free (args_info->config_orig [i]); /* free previous argument */
              args_info->config_orig [i] = 0;
            }
        }
      if (args_info->config_arg [0])
        free (args_info->config_arg [0]); /* free default string */
      free (args_info->config_arg); /* free previous argument */
      args_info->config_arg = 0;
      free (args_info->config_orig); /* free previous argument */
      args_info->config_orig = 0;
    }
  if (args_info->label_arg)
    {
      free (args_info->label_arg); /* free previous argument */
      args_info->label_arg = 0;
    }
  if (args_info->label_orig)
    {
      free (args_info->label_orig); /* free previous argument */
      args_info->label_orig = 0;
    }
  if (args_info->sky_grid_arg)
    {
      free (args_info->sky_grid_arg); /* free previous argument */
      args_info->sky_grid_arg = 0;
    }
  if (args_info->sky_grid_orig)
    {
      free (args_info->sky_grid_orig); /* free previous argument */
      args_info->sky_grid_orig = 0;
    }
  if (args_info->skymap_orientation_arg)
    {
      free (args_info->skymap_orientation_arg); /* free previous argument */
      args_info->skymap_orientation_arg = 0;
    }
  if (args_info->skymap_orientation_orig)
    {
      free (args_info->skymap_orientation_orig); /* free previous argument */
      args_info->skymap_orientation_orig = 0;
    }
  if (args_info->skyband_method_arg)
    {
      free (args_info->skyband_method_arg); /* free previous argument */
      args_info->skyband_method_arg = 0;
    }
  if (args_info->skyband_method_orig)
    {
      free (args_info->skyband_method_orig); /* free previous argument */
      args_info->skyband_method_orig = 0;
    }
  if (args_info->nskybands_orig)
    {
      free (args_info->nskybands_orig); /* free previous argument */
      args_info->nskybands_orig = 0;
    }
  if (args_info->large_S_orig)
    {
      free (args_info->large_S_orig); /* free previous argument */
      args_info->large_S_orig = 0;
    }
  if (args_info->band_axis_arg)
    {
      free (args_info->band_axis_arg); /* free previous argument */
      args_info->band_axis_arg = 0;
    }
  if (args_info->band_axis_orig)
    {
      free (args_info->band_axis_orig); /* free previous argument */
      args_info->band_axis_orig = 0;
    }
  if (args_info->band_axis_norm_orig)
    {
      free (args_info->band_axis_norm_orig); /* free previous argument */
      args_info->band_axis_norm_orig = 0;
    }
  if (args_info->sky_marks_file_arg)
    {
      free (args_info->sky_marks_file_arg); /* free previous argument */
      args_info->sky_marks_file_arg = 0;
    }
  if (args_info->sky_marks_file_orig)
    {
      free (args_info->sky_marks_file_orig); /* free previous argument */
      args_info->sky_marks_file_orig = 0;
    }
  if (args_info->fine_factor_orig)
    {
      free (args_info->fine_factor_orig); /* free previous argument */
      args_info->fine_factor_orig = 0;
    }
  if (args_info->skymap_resolution_orig)
    {
      free (args_info->skymap_resolution_orig); /* free previous argument */
      args_info->skymap_resolution_orig = 0;
    }
  if (args_info->skymap_resolution_ratio_orig)
    {
      free (args_info->skymap_resolution_ratio_orig); /* free previous argument */
      args_info->skymap_resolution_ratio_orig = 0;
    }
  if (args_info->small_weight_ratio_orig)
    {
      free (args_info->small_weight_ratio_orig); /* free previous argument */
      args_info->small_weight_ratio_orig = 0;
    }
  if (args_info->strain_norm_factor_orig)
    {
      free (args_info->strain_norm_factor_orig); /* free previous argument */
      args_info->strain_norm_factor_orig = 0;
    }
  if (args_info->lock_file_arg)
    {
      free (args_info->lock_file_arg); /* free previous argument */
      args_info->lock_file_arg = 0;
    }
  if (args_info->lock_file_orig)
    {
      free (args_info->lock_file_orig); /* free previous argument */
      args_info->lock_file_orig = 0;
    }
  if (args_info->enable_dataset_locking_orig)
    {
      free (args_info->enable_dataset_locking_orig); /* free previous argument */
      args_info->enable_dataset_locking_orig = 0;
    }
  if (args_info->dataset_arg)
    {
      free (args_info->dataset_arg); /* free previous argument */
      args_info->dataset_arg = 0;
    }
  if (args_info->dataset_orig)
    {
      free (args_info->dataset_orig); /* free previous argument */
      args_info->dataset_orig = 0;
    }
  if (args_info->input_format_arg)
    {
      free (args_info->input_format_arg); /* free previous argument */
      args_info->input_format_arg = 0;
    }
  if (args_info->input_format_orig)
    {
      free (args_info->input_format_orig); /* free previous argument */
      args_info->input_format_orig = 0;
    }
  if (args_info->dump_data_arg)
    {
      free (args_info->dump_data_arg); /* free previous argument */
      args_info->dump_data_arg = 0;
    }
  if (args_info->dump_data_orig)
    {
      free (args_info->dump_data_orig); /* free previous argument */
      args_info->dump_data_orig = 0;
    }
  if (args_info->dump_sftv2_arg)
    {
      free (args_info->dump_sftv2_arg); /* free previous argument */
      args_info->dump_sftv2_arg = 0;
    }
  if (args_info->dump_sftv2_orig)
    {
      free (args_info->dump_sftv2_orig); /* free previous argument */
      args_info->dump_sftv2_orig = 0;
    }
  if (args_info->output_arg)
    {
      free (args_info->output_arg); /* free previous argument */
      args_info->output_arg = 0;
    }
  if (args_info->output_orig)
    {
      free (args_info->output_orig); /* free previous argument */
      args_info->output_orig = 0;
    }
  if (args_info->ephemeris_path_arg)
    {
      free (args_info->ephemeris_path_arg); /* free previous argument */
      args_info->ephemeris_path_arg = 0;
    }
  if (args_info->ephemeris_path_orig)
    {
      free (args_info->ephemeris_path_orig); /* free previous argument */
      args_info->ephemeris_path_orig = 0;
    }
  if (args_info->earth_ephemeris_arg)
    {
      free (args_info->earth_ephemeris_arg); /* free previous argument */
      args_info->earth_ephemeris_arg = 0;
    }
  if (args_info->earth_ephemeris_orig)
    {
      free (args_info->earth_ephemeris_orig); /* free previous argument */
      args_info->earth_ephemeris_orig = 0;
    }
  if (args_info->sun_ephemeris_arg)
    {
      free (args_info->sun_ephemeris_arg); /* free previous argument */
      args_info->sun_ephemeris_arg = 0;
    }
  if (args_info->sun_ephemeris_orig)
    {
      free (args_info->sun_ephemeris_orig); /* free previous argument */
      args_info->sun_ephemeris_orig = 0;
    }
  if (args_info->first_bin_orig)
    {
      free (args_info->first_bin_orig); /* free previous argument */
      args_info->first_bin_orig = 0;
    }
  if (args_info->nbins_orig)
    {
      free (args_info->nbins_orig); /* free previous argument */
      args_info->nbins_orig = 0;
    }
  if (args_info->side_cut_orig)
    {
      free (args_info->side_cut_orig); /* free previous argument */
      args_info->side_cut_orig = 0;
    }
  if (args_info->expected_timebase_orig)
    {
      free (args_info->expected_timebase_orig); /* free previous argument */
      args_info->expected_timebase_orig = 0;
    }
  if (args_info->hist_bins_orig)
    {
      free (args_info->hist_bins_orig); /* free previous argument */
      args_info->hist_bins_orig = 0;
    }
  if (args_info->detector_arg)
    {
      free (args_info->detector_arg); /* free previous argument */
      args_info->detector_arg = 0;
    }
  if (args_info->detector_orig)
    {
      free (args_info->detector_orig); /* free previous argument */
      args_info->detector_orig = 0;
    }
  if (args_info->doppler_multiplier_orig)
    {
      free (args_info->doppler_multiplier_orig); /* free previous argument */
      args_info->doppler_multiplier_orig = 0;
    }
  if (args_info->spindown_start_time_orig)
    {
      free (args_info->spindown_start_time_orig); /* free previous argument */
      args_info->spindown_start_time_orig = 0;
    }
  if (args_info->frequency_offset_orig)
    {
      free (args_info->frequency_offset_orig); /* free previous argument */
      args_info->frequency_offset_orig = 0;
    }
  if (args_info->spindown_start_orig)
    {
      free (args_info->spindown_start_orig); /* free previous argument */
      args_info->spindown_start_orig = 0;
    }
  if (args_info->spindown_step_orig)
    {
      free (args_info->spindown_step_orig); /* free previous argument */
      args_info->spindown_step_orig = 0;
    }
  if (args_info->spindown_count_orig)
    {
      free (args_info->spindown_count_orig); /* free previous argument */
      args_info->spindown_count_orig = 0;
    }
  if (args_info->fdotdot_orig)
    {
      free (args_info->fdotdot_orig); /* free previous argument */
      args_info->fdotdot_orig = 0;
    }
  if (args_info->orientation_orig)
    {
      free (args_info->orientation_orig); /* free previous argument */
      args_info->orientation_orig = 0;
    }
  if (args_info->nlinear_polarizations_orig)
    {
      free (args_info->nlinear_polarizations_orig); /* free previous argument */
      args_info->nlinear_polarizations_orig = 0;
    }
  if (args_info->no_demodulation_orig)
    {
      free (args_info->no_demodulation_orig); /* free previous argument */
      args_info->no_demodulation_orig = 0;
    }
  if (args_info->no_decomposition_orig)
    {
      free (args_info->no_decomposition_orig); /* free previous argument */
      args_info->no_decomposition_orig = 0;
    }
  if (args_info->no_candidates_orig)
    {
      free (args_info->no_candidates_orig); /* free previous argument */
      args_info->no_candidates_orig = 0;
    }
  if (args_info->no_am_response_orig)
    {
      free (args_info->no_am_response_orig); /* free previous argument */
      args_info->no_am_response_orig = 0;
    }
  if (args_info->no_secondary_skymaps_orig)
    {
      free (args_info->no_secondary_skymaps_orig); /* free previous argument */
      args_info->no_secondary_skymaps_orig = 0;
    }
  if (args_info->averaging_mode_arg)
    {
      free (args_info->averaging_mode_arg); /* free previous argument */
      args_info->averaging_mode_arg = 0;
    }
  if (args_info->averaging_mode_orig)
    {
      free (args_info->averaging_mode_orig); /* free previous argument */
      args_info->averaging_mode_orig = 0;
    }
  if (args_info->subtract_background_orig)
    {
      free (args_info->subtract_background_orig); /* free previous argument */
      args_info->subtract_background_orig = 0;
    }
  if (args_info->do_cutoff_orig)
    {
      free (args_info->do_cutoff_orig); /* free previous argument */
      args_info->do_cutoff_orig = 0;
    }
  if (args_info->filter_lines_orig)
    {
      free (args_info->filter_lines_orig); /* free previous argument */
      args_info->filter_lines_orig = 0;
    }
  if (args_info->ks_test_orig)
    {
      free (args_info->ks_test_orig); /* free previous argument */
      args_info->ks_test_orig = 0;
    }
  if (args_info->compute_betas_orig)
    {
      free (args_info->compute_betas_orig); /* free previous argument */
      args_info->compute_betas_orig = 0;
    }
  if (args_info->upper_limit_comp_arg)
    {
      free (args_info->upper_limit_comp_arg); /* free previous argument */
      args_info->upper_limit_comp_arg = 0;
    }
  if (args_info->upper_limit_comp_orig)
    {
      free (args_info->upper_limit_comp_orig); /* free previous argument */
      args_info->upper_limit_comp_orig = 0;
    }
  if (args_info->lower_limit_comp_arg)
    {
      free (args_info->lower_limit_comp_arg); /* free previous argument */
      args_info->lower_limit_comp_arg = 0;
    }
  if (args_info->lower_limit_comp_orig)
    {
      free (args_info->lower_limit_comp_orig); /* free previous argument */
      args_info->lower_limit_comp_orig = 0;
    }
  if (args_info->write_dat_arg)
    {
      free (args_info->write_dat_arg); /* free previous argument */
      args_info->write_dat_arg = 0;
    }
  if (args_info->write_dat_orig)
    {
      free (args_info->write_dat_orig); /* free previous argument */
      args_info->write_dat_orig = 0;
    }
  if (args_info->write_png_arg)
    {
      free (args_info->write_png_arg); /* free previous argument */
      args_info->write_png_arg = 0;
    }
  if (args_info->write_png_orig)
    {
      free (args_info->write_png_orig); /* free previous argument */
      args_info->write_png_orig = 0;
    }
  if (args_info->dump_points_orig)
    {
      free (args_info->dump_points_orig); /* free previous argument */
      args_info->dump_points_orig = 0;
    }
  if (args_info->dump_candidates_orig)
    {
      free (args_info->dump_candidates_orig); /* free previous argument */
      args_info->dump_candidates_orig = 0;
    }
  if (args_info->focus_ra_orig)
    {
      free (args_info->focus_ra_orig); /* free previous argument */
      args_info->focus_ra_orig = 0;
    }
  if (args_info->focus_dec_orig)
    {
      free (args_info->focus_dec_orig); /* free previous argument */
      args_info->focus_dec_orig = 0;
    }
  if (args_info->focus_radius_orig)
    {
      free (args_info->focus_radius_orig); /* free previous argument */
      args_info->focus_radius_orig = 0;
    }
  if (args_info->only_large_cos_orig)
    {
      free (args_info->only_large_cos_orig); /* free previous argument */
      args_info->only_large_cos_orig = 0;
    }
  if (args_info->fake_ref_time_orig)
    {
      free (args_info->fake_ref_time_orig); /* free previous argument */
      args_info->fake_ref_time_orig = 0;
    }
  if (args_info->fake_ra_orig)
    {
      free (args_info->fake_ra_orig); /* free previous argument */
      args_info->fake_ra_orig = 0;
    }
  if (args_info->fake_dec_orig)
    {
      free (args_info->fake_dec_orig); /* free previous argument */
      args_info->fake_dec_orig = 0;
    }
  if (args_info->fake_iota_orig)
    {
      free (args_info->fake_iota_orig); /* free previous argument */
      args_info->fake_iota_orig = 0;
    }
  if (args_info->fake_psi_orig)
    {
      free (args_info->fake_psi_orig); /* free previous argument */
      args_info->fake_psi_orig = 0;
    }
  if (args_info->fake_phi_orig)
    {
      free (args_info->fake_phi_orig); /* free previous argument */
      args_info->fake_phi_orig = 0;
    }
  if (args_info->fake_spindown_orig)
    {
      free (args_info->fake_spindown_orig); /* free previous argument */
      args_info->fake_spindown_orig = 0;
    }
  if (args_info->fake_strain_orig)
    {
      free (args_info->fake_strain_orig); /* free previous argument */
      args_info->fake_strain_orig = 0;
    }
  if (args_info->fake_freq_orig)
    {
      free (args_info->fake_freq_orig); /* free previous argument */
      args_info->fake_freq_orig = 0;
    }
  if (args_info->snr_precision_orig)
    {
      free (args_info->snr_precision_orig); /* free previous argument */
      args_info->snr_precision_orig = 0;
    }
  if (args_info->max_candidates_orig)
    {
      free (args_info->max_candidates_orig); /* free previous argument */
      args_info->max_candidates_orig = 0;
    }
  if (args_info->min_candidate_snr_orig)
    {
      free (args_info->min_candidate_snr_orig); /* free previous argument */
      args_info->min_candidate_snr_orig = 0;
    }
  if (args_info->output_initial_orig)
    {
      free (args_info->output_initial_orig); /* free previous argument */
      args_info->output_initial_orig = 0;
    }
  if (args_info->output_optimized_orig)
    {
      free (args_info->output_optimized_orig); /* free previous argument */
      args_info->output_optimized_orig = 0;
    }
  if (args_info->output_cache_orig)
    {
      free (args_info->output_cache_orig); /* free previous argument */
      args_info->output_cache_orig = 0;
    }
  if (args_info->extended_test_orig)
    {
      free (args_info->extended_test_orig); /* free previous argument */
      args_info->extended_test_orig = 0;
    }
  if (args_info->max_sft_report_orig)
    {
      free (args_info->max_sft_report_orig); /* free previous argument */
      args_info->max_sft_report_orig = 0;
    }
  if (args_info->num_threads_orig)
    {
      free (args_info->num_threads_orig); /* free previous argument */
      args_info->num_threads_orig = 0;
    }
  
  clear_given (args_info);
}

int
cmdline_parser_file_save(const char *filename, struct gengetopt_args_info *args_info)
{
  FILE *outfile;
  int i = 0;

  outfile = fopen(filename, "w");

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot open file for writing: %s\n", CMDLINE_PARSER_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  if (args_info->help_given) {
    fprintf(outfile, "%s\n", "help");
  }
  if (args_info->version_given) {
    fprintf(outfile, "%s\n", "version");
  }
  if (args_info->config_orig)
    {
      for (i = 0; i < args_info->config_given; ++i)
        {
          if (args_info->config_orig [i])
            {
              fprintf(outfile, "%s=\"%s\"\n", "config", args_info->config_orig [i]);
            }
        }
    }
  if (args_info->label_given) {
    if (args_info->label_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "label", args_info->label_orig);
    } else {
      fprintf(outfile, "%s\n", "label");
    }
  }
  if (args_info->sky_grid_given) {
    if (args_info->sky_grid_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "sky-grid", args_info->sky_grid_orig);
    } else {
      fprintf(outfile, "%s\n", "sky-grid");
    }
  }
  if (args_info->skymap_orientation_given) {
    if (args_info->skymap_orientation_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "skymap-orientation", args_info->skymap_orientation_orig);
    } else {
      fprintf(outfile, "%s\n", "skymap-orientation");
    }
  }
  if (args_info->skyband_method_given) {
    if (args_info->skyband_method_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "skyband-method", args_info->skyband_method_orig);
    } else {
      fprintf(outfile, "%s\n", "skyband-method");
    }
  }
  if (args_info->nskybands_given) {
    if (args_info->nskybands_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "nskybands", args_info->nskybands_orig);
    } else {
      fprintf(outfile, "%s\n", "nskybands");
    }
  }
  if (args_info->large_S_given) {
    if (args_info->large_S_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "large-S", args_info->large_S_orig);
    } else {
      fprintf(outfile, "%s\n", "large-S");
    }
  }
  if (args_info->band_axis_given) {
    if (args_info->band_axis_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "band-axis", args_info->band_axis_orig);
    } else {
      fprintf(outfile, "%s\n", "band-axis");
    }
  }
  if (args_info->band_axis_norm_given) {
    if (args_info->band_axis_norm_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "band-axis-norm", args_info->band_axis_norm_orig);
    } else {
      fprintf(outfile, "%s\n", "band-axis-norm");
    }
  }
  if (args_info->sky_marks_file_given) {
    if (args_info->sky_marks_file_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "sky-marks-file", args_info->sky_marks_file_orig);
    } else {
      fprintf(outfile, "%s\n", "sky-marks-file");
    }
  }
  if (args_info->fine_factor_given) {
    if (args_info->fine_factor_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fine-factor", args_info->fine_factor_orig);
    } else {
      fprintf(outfile, "%s\n", "fine-factor");
    }
  }
  if (args_info->skymap_resolution_given) {
    if (args_info->skymap_resolution_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "skymap-resolution", args_info->skymap_resolution_orig);
    } else {
      fprintf(outfile, "%s\n", "skymap-resolution");
    }
  }
  if (args_info->skymap_resolution_ratio_given) {
    if (args_info->skymap_resolution_ratio_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "skymap-resolution-ratio", args_info->skymap_resolution_ratio_orig);
    } else {
      fprintf(outfile, "%s\n", "skymap-resolution-ratio");
    }
  }
  if (args_info->small_weight_ratio_given) {
    if (args_info->small_weight_ratio_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "small-weight-ratio", args_info->small_weight_ratio_orig);
    } else {
      fprintf(outfile, "%s\n", "small-weight-ratio");
    }
  }
  if (args_info->strain_norm_factor_given) {
    if (args_info->strain_norm_factor_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "strain-norm-factor", args_info->strain_norm_factor_orig);
    } else {
      fprintf(outfile, "%s\n", "strain-norm-factor");
    }
  }
  if (args_info->lock_file_given) {
    if (args_info->lock_file_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "lock-file", args_info->lock_file_orig);
    } else {
      fprintf(outfile, "%s\n", "lock-file");
    }
  }
  if (args_info->enable_dataset_locking_given) {
    if (args_info->enable_dataset_locking_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "enable-dataset-locking", args_info->enable_dataset_locking_orig);
    } else {
      fprintf(outfile, "%s\n", "enable-dataset-locking");
    }
  }
  if (args_info->dataset_given) {
    if (args_info->dataset_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "dataset", args_info->dataset_orig);
    } else {
      fprintf(outfile, "%s\n", "dataset");
    }
  }
  if (args_info->input_format_given) {
    if (args_info->input_format_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "input-format", args_info->input_format_orig);
    } else {
      fprintf(outfile, "%s\n", "input-format");
    }
  }
  if (args_info->dump_data_given) {
    if (args_info->dump_data_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "dump-data", args_info->dump_data_orig);
    } else {
      fprintf(outfile, "%s\n", "dump-data");
    }
  }
  if (args_info->dump_sftv2_given) {
    if (args_info->dump_sftv2_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "dump-sftv2", args_info->dump_sftv2_orig);
    } else {
      fprintf(outfile, "%s\n", "dump-sftv2");
    }
  }
  if (args_info->output_given) {
    if (args_info->output_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "output", args_info->output_orig);
    } else {
      fprintf(outfile, "%s\n", "output");
    }
  }
  if (args_info->ephemeris_path_given) {
    if (args_info->ephemeris_path_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "ephemeris-path", args_info->ephemeris_path_orig);
    } else {
      fprintf(outfile, "%s\n", "ephemeris-path");
    }
  }
  if (args_info->earth_ephemeris_given) {
    if (args_info->earth_ephemeris_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "earth-ephemeris", args_info->earth_ephemeris_orig);
    } else {
      fprintf(outfile, "%s\n", "earth-ephemeris");
    }
  }
  if (args_info->sun_ephemeris_given) {
    if (args_info->sun_ephemeris_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "sun-ephemeris", args_info->sun_ephemeris_orig);
    } else {
      fprintf(outfile, "%s\n", "sun-ephemeris");
    }
  }
  if (args_info->first_bin_given) {
    if (args_info->first_bin_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "first-bin", args_info->first_bin_orig);
    } else {
      fprintf(outfile, "%s\n", "first-bin");
    }
  }
  if (args_info->nbins_given) {
    if (args_info->nbins_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "nbins", args_info->nbins_orig);
    } else {
      fprintf(outfile, "%s\n", "nbins");
    }
  }
  if (args_info->side_cut_given) {
    if (args_info->side_cut_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "side-cut", args_info->side_cut_orig);
    } else {
      fprintf(outfile, "%s\n", "side-cut");
    }
  }
  if (args_info->expected_timebase_given) {
    if (args_info->expected_timebase_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "expected-timebase", args_info->expected_timebase_orig);
    } else {
      fprintf(outfile, "%s\n", "expected-timebase");
    }
  }
  if (args_info->hist_bins_given) {
    if (args_info->hist_bins_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "hist-bins", args_info->hist_bins_orig);
    } else {
      fprintf(outfile, "%s\n", "hist-bins");
    }
  }
  if (args_info->detector_given) {
    if (args_info->detector_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "detector", args_info->detector_orig);
    } else {
      fprintf(outfile, "%s\n", "detector");
    }
  }
  if (args_info->doppler_multiplier_given) {
    if (args_info->doppler_multiplier_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "doppler-multiplier", args_info->doppler_multiplier_orig);
    } else {
      fprintf(outfile, "%s\n", "doppler-multiplier");
    }
  }
  if (args_info->spindown_start_time_given) {
    if (args_info->spindown_start_time_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "spindown-start-time", args_info->spindown_start_time_orig);
    } else {
      fprintf(outfile, "%s\n", "spindown-start-time");
    }
  }
  if (args_info->frequency_offset_given) {
    if (args_info->frequency_offset_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "frequency-offset", args_info->frequency_offset_orig);
    } else {
      fprintf(outfile, "%s\n", "frequency-offset");
    }
  }
  if (args_info->spindown_start_given) {
    if (args_info->spindown_start_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "spindown-start", args_info->spindown_start_orig);
    } else {
      fprintf(outfile, "%s\n", "spindown-start");
    }
  }
  if (args_info->spindown_step_given) {
    if (args_info->spindown_step_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "spindown-step", args_info->spindown_step_orig);
    } else {
      fprintf(outfile, "%s\n", "spindown-step");
    }
  }
  if (args_info->spindown_count_given) {
    if (args_info->spindown_count_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "spindown-count", args_info->spindown_count_orig);
    } else {
      fprintf(outfile, "%s\n", "spindown-count");
    }
  }
  if (args_info->fdotdot_given) {
    if (args_info->fdotdot_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fdotdot", args_info->fdotdot_orig);
    } else {
      fprintf(outfile, "%s\n", "fdotdot");
    }
  }
  if (args_info->orientation_given) {
    if (args_info->orientation_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "orientation", args_info->orientation_orig);
    } else {
      fprintf(outfile, "%s\n", "orientation");
    }
  }
  if (args_info->nlinear_polarizations_given) {
    if (args_info->nlinear_polarizations_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "nlinear-polarizations", args_info->nlinear_polarizations_orig);
    } else {
      fprintf(outfile, "%s\n", "nlinear-polarizations");
    }
  }
  if (args_info->no_demodulation_given) {
    if (args_info->no_demodulation_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "no-demodulation", args_info->no_demodulation_orig);
    } else {
      fprintf(outfile, "%s\n", "no-demodulation");
    }
  }
  if (args_info->no_decomposition_given) {
    if (args_info->no_decomposition_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "no-decomposition", args_info->no_decomposition_orig);
    } else {
      fprintf(outfile, "%s\n", "no-decomposition");
    }
  }
  if (args_info->no_candidates_given) {
    if (args_info->no_candidates_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "no-candidates", args_info->no_candidates_orig);
    } else {
      fprintf(outfile, "%s\n", "no-candidates");
    }
  }
  if (args_info->no_am_response_given) {
    if (args_info->no_am_response_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "no-am-response", args_info->no_am_response_orig);
    } else {
      fprintf(outfile, "%s\n", "no-am-response");
    }
  }
  if (args_info->no_secondary_skymaps_given) {
    if (args_info->no_secondary_skymaps_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "no-secondary-skymaps", args_info->no_secondary_skymaps_orig);
    } else {
      fprintf(outfile, "%s\n", "no-secondary-skymaps");
    }
  }
  if (args_info->averaging_mode_given) {
    if (args_info->averaging_mode_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "averaging-mode", args_info->averaging_mode_orig);
    } else {
      fprintf(outfile, "%s\n", "averaging-mode");
    }
  }
  if (args_info->subtract_background_given) {
    if (args_info->subtract_background_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "subtract-background", args_info->subtract_background_orig);
    } else {
      fprintf(outfile, "%s\n", "subtract-background");
    }
  }
  if (args_info->do_cutoff_given) {
    if (args_info->do_cutoff_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "do-cutoff", args_info->do_cutoff_orig);
    } else {
      fprintf(outfile, "%s\n", "do-cutoff");
    }
  }
  if (args_info->filter_lines_given) {
    if (args_info->filter_lines_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "filter-lines", args_info->filter_lines_orig);
    } else {
      fprintf(outfile, "%s\n", "filter-lines");
    }
  }
  if (args_info->ks_test_given) {
    if (args_info->ks_test_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "ks-test", args_info->ks_test_orig);
    } else {
      fprintf(outfile, "%s\n", "ks-test");
    }
  }
  if (args_info->compute_betas_given) {
    if (args_info->compute_betas_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "compute-betas", args_info->compute_betas_orig);
    } else {
      fprintf(outfile, "%s\n", "compute-betas");
    }
  }
  if (args_info->upper_limit_comp_given) {
    if (args_info->upper_limit_comp_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "upper-limit-comp", args_info->upper_limit_comp_orig);
    } else {
      fprintf(outfile, "%s\n", "upper-limit-comp");
    }
  }
  if (args_info->lower_limit_comp_given) {
    if (args_info->lower_limit_comp_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "lower-limit-comp", args_info->lower_limit_comp_orig);
    } else {
      fprintf(outfile, "%s\n", "lower-limit-comp");
    }
  }
  if (args_info->write_dat_given) {
    if (args_info->write_dat_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "write-dat", args_info->write_dat_orig);
    } else {
      fprintf(outfile, "%s\n", "write-dat");
    }
  }
  if (args_info->write_png_given) {
    if (args_info->write_png_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "write-png", args_info->write_png_orig);
    } else {
      fprintf(outfile, "%s\n", "write-png");
    }
  }
  if (args_info->dump_points_given) {
    if (args_info->dump_points_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "dump-points", args_info->dump_points_orig);
    } else {
      fprintf(outfile, "%s\n", "dump-points");
    }
  }
  if (args_info->dump_candidates_given) {
    if (args_info->dump_candidates_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "dump-candidates", args_info->dump_candidates_orig);
    } else {
      fprintf(outfile, "%s\n", "dump-candidates");
    }
  }
  if (args_info->focus_ra_given) {
    if (args_info->focus_ra_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "focus-ra", args_info->focus_ra_orig);
    } else {
      fprintf(outfile, "%s\n", "focus-ra");
    }
  }
  if (args_info->focus_dec_given) {
    if (args_info->focus_dec_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "focus-dec", args_info->focus_dec_orig);
    } else {
      fprintf(outfile, "%s\n", "focus-dec");
    }
  }
  if (args_info->focus_radius_given) {
    if (args_info->focus_radius_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "focus-radius", args_info->focus_radius_orig);
    } else {
      fprintf(outfile, "%s\n", "focus-radius");
    }
  }
  if (args_info->only_large_cos_given) {
    if (args_info->only_large_cos_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "only-large-cos", args_info->only_large_cos_orig);
    } else {
      fprintf(outfile, "%s\n", "only-large-cos");
    }
  }
  if (args_info->fake_linear_given) {
    fprintf(outfile, "%s\n", "fake-linear");
  }
  if (args_info->fake_circular_given) {
    fprintf(outfile, "%s\n", "fake-circular");
  }
  if (args_info->fake_ref_time_given) {
    if (args_info->fake_ref_time_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fake-ref-time", args_info->fake_ref_time_orig);
    } else {
      fprintf(outfile, "%s\n", "fake-ref-time");
    }
  }
  if (args_info->fake_ra_given) {
    if (args_info->fake_ra_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fake-ra", args_info->fake_ra_orig);
    } else {
      fprintf(outfile, "%s\n", "fake-ra");
    }
  }
  if (args_info->fake_dec_given) {
    if (args_info->fake_dec_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fake-dec", args_info->fake_dec_orig);
    } else {
      fprintf(outfile, "%s\n", "fake-dec");
    }
  }
  if (args_info->fake_iota_given) {
    if (args_info->fake_iota_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fake-iota", args_info->fake_iota_orig);
    } else {
      fprintf(outfile, "%s\n", "fake-iota");
    }
  }
  if (args_info->fake_psi_given) {
    if (args_info->fake_psi_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fake-psi", args_info->fake_psi_orig);
    } else {
      fprintf(outfile, "%s\n", "fake-psi");
    }
  }
  if (args_info->fake_phi_given) {
    if (args_info->fake_phi_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fake-phi", args_info->fake_phi_orig);
    } else {
      fprintf(outfile, "%s\n", "fake-phi");
    }
  }
  if (args_info->fake_spindown_given) {
    if (args_info->fake_spindown_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fake-spindown", args_info->fake_spindown_orig);
    } else {
      fprintf(outfile, "%s\n", "fake-spindown");
    }
  }
  if (args_info->fake_strain_given) {
    if (args_info->fake_strain_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fake-strain", args_info->fake_strain_orig);
    } else {
      fprintf(outfile, "%s\n", "fake-strain");
    }
  }
  if (args_info->fake_freq_given) {
    if (args_info->fake_freq_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "fake-freq", args_info->fake_freq_orig);
    } else {
      fprintf(outfile, "%s\n", "fake-freq");
    }
  }
  if (args_info->snr_precision_given) {
    if (args_info->snr_precision_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "snr-precision", args_info->snr_precision_orig);
    } else {
      fprintf(outfile, "%s\n", "snr-precision");
    }
  }
  if (args_info->max_candidates_given) {
    if (args_info->max_candidates_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "max-candidates", args_info->max_candidates_orig);
    } else {
      fprintf(outfile, "%s\n", "max-candidates");
    }
  }
  if (args_info->min_candidate_snr_given) {
    if (args_info->min_candidate_snr_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "min-candidate-snr", args_info->min_candidate_snr_orig);
    } else {
      fprintf(outfile, "%s\n", "min-candidate-snr");
    }
  }
  if (args_info->output_initial_given) {
    if (args_info->output_initial_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "output-initial", args_info->output_initial_orig);
    } else {
      fprintf(outfile, "%s\n", "output-initial");
    }
  }
  if (args_info->output_optimized_given) {
    if (args_info->output_optimized_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "output-optimized", args_info->output_optimized_orig);
    } else {
      fprintf(outfile, "%s\n", "output-optimized");
    }
  }
  if (args_info->output_cache_given) {
    if (args_info->output_cache_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "output-cache", args_info->output_cache_orig);
    } else {
      fprintf(outfile, "%s\n", "output-cache");
    }
  }
  if (args_info->extended_test_given) {
    if (args_info->extended_test_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "extended-test", args_info->extended_test_orig);
    } else {
      fprintf(outfile, "%s\n", "extended-test");
    }
  }
  if (args_info->max_sft_report_given) {
    if (args_info->max_sft_report_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "max-sft-report", args_info->max_sft_report_orig);
    } else {
      fprintf(outfile, "%s\n", "max-sft-report");
    }
  }
  if (args_info->num_threads_given) {
    if (args_info->num_threads_orig) {
      fprintf(outfile, "%s=\"%s\"\n", "num-threads", args_info->num_threads_orig);
    } else {
      fprintf(outfile, "%s\n", "num-threads");
    }
  }
  
  fclose (outfile);

  i = EXIT_SUCCESS;
  return i;
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  cmdline_parser_release (args_info);
}


/* gengetopt_strdup() */
/* strdup.c replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

static char *
get_multiple_arg_token(const char *arg)
{
  char *tok, *ret;
  size_t len, num_of_escape;
  int i, j;

  if (!arg)
    return NULL;

  tok = strchr (arg, ',');
  num_of_escape = 0;

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
          ++num_of_escape;
        }
      else
        break;
    }

  if (tok)
    len = (size_t)(tok - arg + 1);
  else
    len = strlen (arg) + 1;

  len -= num_of_escape;

  ret = (char *) malloc (len);

  i = 0;
  j = 0;
  while (arg[i] && (j < len-1))
    {
      if (arg[i] == '\\')
        ++i;

      ret[j++] = arg[i++];
    }

  ret[len-1] = '\0';

  return ret;
}

static char *
get_multiple_arg_token_next(const char *arg)
{
  char *tok;

  if (!arg)
    return NULL;

  tok = strchr (arg, ',');

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
        }
      else
        break;
    }

  if (! tok || strlen(tok) == 1)
    return 0;

  return tok+1;
}

static int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, int min, int max, const char *option_desc);

int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, int min, int max, const char *option_desc)
{
  int error = 0;

  if (option_given && ! (min < 0 && max < 0))
    {
      if (min >= 0 && max >= 0)
        {
          if (min == max)
            {
              /* specific occurrences */
              if (option_given != min)
                {
                  fprintf (stderr, "%s: %s option occurrences must be %d\n",
                    prog_name, option_desc, min);
                  error = 1;
                }
            }
          else if (option_given < min
              || option_given > max)
            {
              /* range occurrences */
              fprintf (stderr, "%s: %s option occurrences must be between %d and %d\n",
                prog_name, option_desc, min, max);
              error = 1;
            }
        }
      else if (min >= 0)
        {
          /* at least check */
          if (option_given < min)
            {
              fprintf (stderr, "%s: %s option occurrences must be at least %d\n",
                prog_name, option_desc, min);
              error = 1;
            }
        }
      else if (max >= 0)
        {
          /* at most check */
          if (option_given > max)
            {
              fprintf (stderr, "%s: %s option occurrences must be at most %d\n",
                prog_name, option_desc, max);
              error = 1;
            }
        }
    }
    
  return error;
}
static void
reset_group_injection(struct gengetopt_args_info *args_info);

static void
reset_group_injection(struct gengetopt_args_info *args_info)
{
  if (! args_info->injection_group_counter)
    return;
  
  args_info->fake_linear_given = 0 ;
  args_info->fake_circular_given = 0 ;

  args_info->injection_group_counter = 0;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;

  result = cmdline_parser_internal (argc, argv, args_info, override, initialize, check_required, NULL);

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, NULL) > 0)
    result = EXIT_FAILURE;

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;

  /* checks for required options */
  if (check_multiple_option_occurrences(prog_name, args_info->config_given, args_info->config_min, args_info->config_max, "'--config' ('-c')"))
     error = 1;
  
  
  /* checks for dependences among options */

  return error;
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */
  char *multi_token, *multi_next; /* for multiple options */

  int i;        /* Counter */

  struct string_list * config_list = NULL,* config_new = NULL;
  int error = 0;
  struct gengetopt_args_info local_args_info;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 0;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int option_index = 0;
      char *stop_char;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "config",	1, NULL, 'c' },
        { "label",	1, NULL, 0 },
        { "sky-grid",	1, NULL, 0 },
        { "skymap-orientation",	1, NULL, 0 },
        { "skyband-method",	1, NULL, 0 },
        { "nskybands",	1, NULL, 0 },
        { "large-S",	1, NULL, 0 },
        { "band-axis",	1, NULL, 0 },
        { "band-axis-norm",	1, NULL, 0 },
        { "sky-marks-file",	1, NULL, 0 },
        { "fine-factor",	1, NULL, 0 },
        { "skymap-resolution",	1, NULL, 0 },
        { "skymap-resolution-ratio",	1, NULL, 0 },
        { "small-weight-ratio",	1, NULL, 0 },
        { "strain-norm-factor",	1, NULL, 0 },
        { "lock-file",	1, NULL, 0 },
        { "enable-dataset-locking",	1, NULL, 0 },
        { "dataset",	1, NULL, 's' },
        { "input-format",	1, NULL, 0 },
        { "dump-data",	1, NULL, 0 },
        { "dump-sftv2",	1, NULL, 0 },
        { "output",	1, NULL, 'o' },
        { "ephemeris-path",	1, NULL, 0 },
        { "earth-ephemeris",	1, NULL, 0 },
        { "sun-ephemeris",	1, NULL, 0 },
        { "first-bin",	1, NULL, 'f' },
        { "nbins",	1, NULL, 'n' },
        { "side-cut",	1, NULL, 0 },
        { "expected-timebase",	1, NULL, 0 },
        { "hist-bins",	1, NULL, 0 },
        { "detector",	1, NULL, 'd' },
        { "doppler-multiplier",	1, NULL, 0 },
        { "spindown-start-time",	1, NULL, 0 },
        { "frequency-offset",	1, NULL, 0 },
        { "spindown-start",	1, NULL, 0 },
        { "spindown-step",	1, NULL, 0 },
        { "spindown-count",	1, NULL, 0 },
        { "fdotdot",	1, NULL, 0 },
        { "orientation",	1, NULL, 0 },
        { "nlinear-polarizations",	1, NULL, 0 },
        { "no-demodulation",	1, NULL, 0 },
        { "no-decomposition",	1, NULL, 0 },
        { "no-candidates",	1, NULL, 0 },
        { "no-am-response",	1, NULL, 0 },
        { "no-secondary-skymaps",	1, NULL, 0 },
        { "averaging-mode",	1, NULL, 0 },
        { "subtract-background",	1, NULL, 0 },
        { "do-cutoff",	1, NULL, 0 },
        { "filter-lines",	1, NULL, 0 },
        { "ks-test",	1, NULL, 0 },
        { "compute-betas",	1, NULL, 0 },
        { "upper-limit-comp",	1, NULL, 0 },
        { "lower-limit-comp",	1, NULL, 0 },
        { "write-dat",	1, NULL, 0 },
        { "write-png",	1, NULL, 0 },
        { "dump-points",	1, NULL, 0 },
        { "dump-candidates",	1, NULL, 0 },
        { "focus-ra",	1, NULL, 0 },
        { "focus-dec",	1, NULL, 0 },
        { "focus-radius",	1, NULL, 0 },
        { "only-large-cos",	1, NULL, 0 },
        { "fake-linear",	0, NULL, 0 },
        { "fake-circular",	0, NULL, 0 },
        { "fake-ref-time",	1, NULL, 0 },
        { "fake-ra",	1, NULL, 0 },
        { "fake-dec",	1, NULL, 0 },
        { "fake-iota",	1, NULL, 0 },
        { "fake-psi",	1, NULL, 0 },
        { "fake-phi",	1, NULL, 0 },
        { "fake-spindown",	1, NULL, 0 },
        { "fake-strain",	1, NULL, 0 },
        { "fake-freq",	1, NULL, 0 },
        { "snr-precision",	1, NULL, 0 },
        { "max-candidates",	1, NULL, 0 },
        { "min-candidate-snr",	1, NULL, 0 },
        { "output-initial",	1, NULL, 0 },
        { "output-optimized",	1, NULL, 0 },
        { "output-cache",	1, NULL, 0 },
        { "extended-test",	1, NULL, 0 },
        { "max-sft-report",	1, NULL, 0 },
        { "num-threads",	1, NULL, 0 },
        { NULL,	0, NULL, 0 }
      };

      stop_char = 0;
      c = getopt_long (argc, argv, "hVc:s:o:f:n:d:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          cmdline_parser_print_version ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'c':	/* configuration file (in gengetopt format) to pass parameters.  */
          local_args_info.config_given++;
        
          multi_token = get_multiple_arg_token(optarg);
          multi_next = get_multiple_arg_token_next (optarg);
        
          while (1)
            {
              config_new = (struct string_list *) malloc (sizeof (struct string_list));
              config_new->next = config_list;
              config_list = config_new;
              config_new->arg = gengetopt_strdup (multi_token);
              config_new->orig = multi_token;
        
              if (multi_next)
                {
                  multi_token = get_multiple_arg_token(multi_next);
                  multi_next = get_multiple_arg_token_next (multi_next);
                  local_args_info.config_given++;
                }
              else
                break;
            }
          break;

        case 's':	/* dataset file.  */
          if (local_args_info.dataset_given)
            {
              fprintf (stderr, "%s: `--dataset' (`-s') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->dataset_given && ! override)
            continue;
          local_args_info.dataset_given = 1;
          args_info->dataset_given = 1;
          if (args_info->dataset_arg)
            free (args_info->dataset_arg); /* free previous string */
          args_info->dataset_arg = gengetopt_strdup (optarg);
          if (args_info->dataset_orig)
            free (args_info->dataset_orig); /* free previous string */
          args_info->dataset_orig = gengetopt_strdup (optarg);
          break;

        case 'o':	/* output directory.  */
          if (local_args_info.output_given)
            {
              fprintf (stderr, "%s: `--output' (`-o') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->output_given && ! override)
            continue;
          local_args_info.output_given = 1;
          args_info->output_given = 1;
          if (args_info->output_arg)
            free (args_info->output_arg); /* free previous string */
          args_info->output_arg = gengetopt_strdup (optarg);
          if (args_info->output_orig)
            free (args_info->output_orig); /* free previous string */
          args_info->output_orig = gengetopt_strdup (optarg);
          break;

        case 'f':	/* first frequency bin in the band to be analyzed.  */
          if (local_args_info.first_bin_given)
            {
              fprintf (stderr, "%s: `--first-bin' (`-f') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->first_bin_given && ! override)
            continue;
          local_args_info.first_bin_given = 1;
          args_info->first_bin_given = 1;
          args_info->first_bin_arg = strtol (optarg, &stop_char, 0);
          if (!(stop_char && *stop_char == '\0')) {
            fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
            goto failure;
          }
          if (args_info->first_bin_orig)
            free (args_info->first_bin_orig); /* free previous string */
          args_info->first_bin_orig = gengetopt_strdup (optarg);
          break;

        case 'n':	/* number of frequency bins to analyze.  */
          if (local_args_info.nbins_given)
            {
              fprintf (stderr, "%s: `--nbins' (`-n') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->nbins_given && ! override)
            continue;
          local_args_info.nbins_given = 1;
          args_info->nbins_given = 1;
          args_info->nbins_arg = strtol (optarg, &stop_char, 0);
          if (!(stop_char && *stop_char == '\0')) {
            fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
            goto failure;
          }
          if (args_info->nbins_orig)
            free (args_info->nbins_orig); /* free previous string */
          args_info->nbins_orig = gengetopt_strdup (optarg);
          break;

        case 'd':	/* detector location (i.e. LHO or LLO), passed to detresponse.  */
          if (local_args_info.detector_given)
            {
              fprintf (stderr, "%s: `--detector' (`-d') option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
              goto failure;
            }
          if (args_info->detector_given && ! override)
            continue;
          local_args_info.detector_given = 1;
          args_info->detector_given = 1;
          if (args_info->detector_arg)
            free (args_info->detector_arg); /* free previous string */
          args_info->detector_arg = gengetopt_strdup (optarg);
          if (args_info->detector_orig)
            free (args_info->detector_orig); /* free previous string */
          args_info->detector_orig = gengetopt_strdup (optarg);
          break;


        case 0:	/* Long option with no short option */
          /* arbitrary string to be printed in the beginning of PowerFlux log file.  */
          if (strcmp (long_options[option_index].name, "label") == 0)
          {
            if (local_args_info.label_given)
              {
                fprintf (stderr, "%s: `--label' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->label_given && ! override)
              continue;
            local_args_info.label_given = 1;
            args_info->label_given = 1;
            if (args_info->label_arg)
              free (args_info->label_arg); /* free previous string */
            args_info->label_arg = gengetopt_strdup (optarg);
            if (args_info->label_orig)
              free (args_info->label_orig); /* free previous string */
            args_info->label_orig = gengetopt_strdup (optarg);
          }
          /* sky grid type (arcsin, plain_rectangular, sin_theta).  */
          else if (strcmp (long_options[option_index].name, "sky-grid") == 0)
          {
            if (local_args_info.sky_grid_given)
              {
                fprintf (stderr, "%s: `--sky-grid' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->sky_grid_given && ! override)
              continue;
            local_args_info.sky_grid_given = 1;
            args_info->sky_grid_given = 1;
            if (args_info->sky_grid_arg)
              free (args_info->sky_grid_arg); /* free previous string */
            args_info->sky_grid_arg = gengetopt_strdup (optarg);
            if (args_info->sky_grid_orig)
              free (args_info->sky_grid_orig); /* free previous string */
            args_info->sky_grid_orig = gengetopt_strdup (optarg);
          }
          /* orientation of produced skymaps: equatorial, ecliptic, band_axis.  */
          else if (strcmp (long_options[option_index].name, "skymap-orientation") == 0)
          {
            if (local_args_info.skymap_orientation_given)
              {
                fprintf (stderr, "%s: `--skymap-orientation' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->skymap_orientation_given && ! override)
              continue;
            local_args_info.skymap_orientation_given = 1;
            args_info->skymap_orientation_given = 1;
            if (args_info->skymap_orientation_arg)
              free (args_info->skymap_orientation_arg); /* free previous string */
            args_info->skymap_orientation_arg = gengetopt_strdup (optarg);
            if (args_info->skymap_orientation_orig)
              free (args_info->skymap_orientation_orig); /* free previous string */
            args_info->skymap_orientation_orig = gengetopt_strdup (optarg);
          }
          /* method of assigning band numbers: angle, S.  */
          else if (strcmp (long_options[option_index].name, "skyband-method") == 0)
          {
            if (local_args_info.skyband_method_given)
              {
                fprintf (stderr, "%s: `--skyband-method' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->skyband_method_given && ! override)
              continue;
            local_args_info.skyband_method_given = 1;
            args_info->skyband_method_given = 1;
            if (args_info->skyband_method_arg)
              free (args_info->skyband_method_arg); /* free previous string */
            args_info->skyband_method_arg = gengetopt_strdup (optarg);
            if (args_info->skyband_method_orig)
              free (args_info->skyband_method_orig); /* free previous string */
            args_info->skyband_method_orig = gengetopt_strdup (optarg);
          }
          /* split sky in this many bands for logging maximum upper limits.  */
          else if (strcmp (long_options[option_index].name, "nskybands") == 0)
          {
            if (local_args_info.nskybands_given)
              {
                fprintf (stderr, "%s: `--nskybands' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->nskybands_given && ! override)
              continue;
            local_args_info.nskybands_given = 1;
            args_info->nskybands_given = 1;
            args_info->nskybands_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->nskybands_orig)
              free (args_info->nskybands_orig); /* free previous string */
            args_info->nskybands_orig = gengetopt_strdup (optarg);
          }
          /* value of S to consider good enough.  */
          else if (strcmp (long_options[option_index].name, "large-S") == 0)
          {
            if (local_args_info.large_S_given)
              {
                fprintf (stderr, "%s: `--large-S' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->large_S_given && ! override)
              continue;
            local_args_info.large_S_given = 1;
            args_info->large_S_given = 1;
            args_info->large_S_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->large_S_orig)
              free (args_info->large_S_orig); /* free previous string */
            args_info->large_S_orig = gengetopt_strdup (optarg);
          }
          /* which band axis to use for splitting sky into bands (perpendicular to band axis) (possible values: equatorial, auto, explicit(float,float,float).  */
          else if (strcmp (long_options[option_index].name, "band-axis") == 0)
          {
            if (local_args_info.band_axis_given)
              {
                fprintf (stderr, "%s: `--band-axis' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->band_axis_given && ! override)
              continue;
            local_args_info.band_axis_given = 1;
            args_info->band_axis_given = 1;
            if (args_info->band_axis_arg)
              free (args_info->band_axis_arg); /* free previous string */
            args_info->band_axis_arg = gengetopt_strdup (optarg);
            if (args_info->band_axis_orig)
              free (args_info->band_axis_orig); /* free previous string */
            args_info->band_axis_orig = gengetopt_strdup (optarg);
          }
          /* norm of band axis vector to use in S value calculation.  */
          else if (strcmp (long_options[option_index].name, "band-axis-norm") == 0)
          {
            if (local_args_info.band_axis_norm_given)
              {
                fprintf (stderr, "%s: `--band-axis-norm' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->band_axis_norm_given && ! override)
              continue;
            local_args_info.band_axis_norm_given = 1;
            args_info->band_axis_norm_given = 1;
            args_info->band_axis_norm_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->band_axis_norm_orig)
              free (args_info->band_axis_norm_orig); /* free previous string */
            args_info->band_axis_norm_orig = gengetopt_strdup (optarg);
          }
          /* file describing how to mark up a sky.  */
          else if (strcmp (long_options[option_index].name, "sky-marks-file") == 0)
          {
            if (local_args_info.sky_marks_file_given)
              {
                fprintf (stderr, "%s: `--sky-marks-file' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->sky_marks_file_given && ! override)
              continue;
            local_args_info.sky_marks_file_given = 1;
            args_info->sky_marks_file_given = 1;
            if (args_info->sky_marks_file_arg)
              free (args_info->sky_marks_file_arg); /* free previous string */
            args_info->sky_marks_file_arg = gengetopt_strdup (optarg);
            if (args_info->sky_marks_file_orig)
              free (args_info->sky_marks_file_orig); /* free previous string */
            args_info->sky_marks_file_orig = gengetopt_strdup (optarg);
          }
          /* make fine grid this times finer.  */
          else if (strcmp (long_options[option_index].name, "fine-factor") == 0)
          {
            if (local_args_info.fine_factor_given)
              {
                fprintf (stderr, "%s: `--fine-factor' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fine_factor_given && ! override)
              continue;
            local_args_info.fine_factor_given = 1;
            args_info->fine_factor_given = 1;
            args_info->fine_factor_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fine_factor_orig)
              free (args_info->fine_factor_orig); /* free previous string */
            args_info->fine_factor_orig = gengetopt_strdup (optarg);
          }
          /* specify skymap resolution explicitly.  */
          else if (strcmp (long_options[option_index].name, "skymap-resolution") == 0)
          {
            if (local_args_info.skymap_resolution_given)
              {
                fprintf (stderr, "%s: `--skymap-resolution' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->skymap_resolution_given && ! override)
              continue;
            local_args_info.skymap_resolution_given = 1;
            args_info->skymap_resolution_given = 1;
            args_info->skymap_resolution_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->skymap_resolution_orig)
              free (args_info->skymap_resolution_orig); /* free previous string */
            args_info->skymap_resolution_orig = gengetopt_strdup (optarg);
          }
          /* adjust default coarseness of the grid by this factor.  */
          else if (strcmp (long_options[option_index].name, "skymap-resolution-ratio") == 0)
          {
            if (local_args_info.skymap_resolution_ratio_given)
              {
                fprintf (stderr, "%s: `--skymap-resolution-ratio' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->skymap_resolution_ratio_given && ! override)
              continue;
            local_args_info.skymap_resolution_ratio_given = 1;
            args_info->skymap_resolution_ratio_given = 1;
            args_info->skymap_resolution_ratio_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->skymap_resolution_ratio_orig)
              free (args_info->skymap_resolution_ratio_orig); /* free previous string */
            args_info->skymap_resolution_ratio_orig = gengetopt_strdup (optarg);
          }
          /* ratio that determines which weight is too small to include in max statistics.  */
          else if (strcmp (long_options[option_index].name, "small-weight-ratio") == 0)
          {
            if (local_args_info.small_weight_ratio_given)
              {
                fprintf (stderr, "%s: `--small-weight-ratio' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->small_weight_ratio_given && ! override)
              continue;
            local_args_info.small_weight_ratio_given = 1;
            args_info->small_weight_ratio_given = 1;
            args_info->small_weight_ratio_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->small_weight_ratio_orig)
              free (args_info->small_weight_ratio_orig); /* free previous string */
            args_info->small_weight_ratio_orig = gengetopt_strdup (optarg);
          }
          /* strain normalization factor to prevent overflowing of the exponent.  */
          else if (strcmp (long_options[option_index].name, "strain-norm-factor") == 0)
          {
            if (local_args_info.strain_norm_factor_given)
              {
                fprintf (stderr, "%s: `--strain-norm-factor' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->strain_norm_factor_given && ! override)
              continue;
            local_args_info.strain_norm_factor_given = 1;
            args_info->strain_norm_factor_given = 1;
            args_info->strain_norm_factor_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->strain_norm_factor_orig)
              free (args_info->strain_norm_factor_orig); /* free previous string */
            args_info->strain_norm_factor_orig = gengetopt_strdup (optarg);
          }
          /* file to lock when reading SFTs in order to globally serialize disk access.  */
          else if (strcmp (long_options[option_index].name, "lock-file") == 0)
          {
            if (local_args_info.lock_file_given)
              {
                fprintf (stderr, "%s: `--lock-file' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->lock_file_given && ! override)
              continue;
            local_args_info.lock_file_given = 1;
            args_info->lock_file_given = 1;
            if (args_info->lock_file_arg)
              free (args_info->lock_file_arg); /* free previous string */
            args_info->lock_file_arg = gengetopt_strdup (optarg);
            if (args_info->lock_file_orig)
              free (args_info->lock_file_orig); /* free previous string */
            args_info->lock_file_orig = gengetopt_strdup (optarg);
          }
          /* set to 1 to enable dataset level locking.  */
          else if (strcmp (long_options[option_index].name, "enable-dataset-locking") == 0)
          {
            if (local_args_info.enable_dataset_locking_given)
              {
                fprintf (stderr, "%s: `--enable-dataset-locking' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->enable_dataset_locking_given && ! override)
              continue;
            local_args_info.enable_dataset_locking_given = 1;
            args_info->enable_dataset_locking_given = 1;
            args_info->enable_dataset_locking_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->enable_dataset_locking_orig)
              free (args_info->enable_dataset_locking_orig); /* free previous string */
            args_info->enable_dataset_locking_orig = gengetopt_strdup (optarg);
          }
          /* format of input files (GEO, SFT, Power).  */
          else if (strcmp (long_options[option_index].name, "input-format") == 0)
          {
            if (local_args_info.input_format_given)
              {
                fprintf (stderr, "%s: `--input-format' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->input_format_given && ! override)
              continue;
            local_args_info.input_format_given = 1;
            args_info->input_format_given = 1;
            if (args_info->input_format_arg)
              free (args_info->input_format_arg); /* free previous string */
            args_info->input_format_arg = gengetopt_strdup (optarg);
            if (args_info->input_format_orig)
              free (args_info->input_format_orig); /* free previous string */
            args_info->input_format_orig = gengetopt_strdup (optarg);
          }
          /* file to output loaded SFT data into, for testing.  */
          else if (strcmp (long_options[option_index].name, "dump-data") == 0)
          {
            if (local_args_info.dump_data_given)
              {
                fprintf (stderr, "%s: `--dump-data' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->dump_data_given && ! override)
              continue;
            local_args_info.dump_data_given = 1;
            args_info->dump_data_given = 1;
            if (args_info->dump_data_arg)
              free (args_info->dump_data_arg); /* free previous string */
            args_info->dump_data_arg = gengetopt_strdup (optarg);
            if (args_info->dump_data_orig)
              free (args_info->dump_data_orig); /* free previous string */
            args_info->dump_data_orig = gengetopt_strdup (optarg);
          }
          /* directory to output loaded data, together with dataset description.  */
          else if (strcmp (long_options[option_index].name, "dump-sftv2") == 0)
          {
            if (local_args_info.dump_sftv2_given)
              {
                fprintf (stderr, "%s: `--dump-sftv2' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->dump_sftv2_given && ! override)
              continue;
            local_args_info.dump_sftv2_given = 1;
            args_info->dump_sftv2_given = 1;
            if (args_info->dump_sftv2_arg)
              free (args_info->dump_sftv2_arg); /* free previous string */
            args_info->dump_sftv2_arg = gengetopt_strdup (optarg);
            if (args_info->dump_sftv2_orig)
              free (args_info->dump_sftv2_orig); /* free previous string */
            args_info->dump_sftv2_orig = gengetopt_strdup (optarg);
          }
          /* path to detresponse program from lalapps.  */
          else if (strcmp (long_options[option_index].name, "ephemeris-path") == 0)
          {
            if (local_args_info.ephemeris_path_given)
              {
                fprintf (stderr, "%s: `--ephemeris-path' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->ephemeris_path_given && ! override)
              continue;
            local_args_info.ephemeris_path_given = 1;
            args_info->ephemeris_path_given = 1;
            if (args_info->ephemeris_path_arg)
              free (args_info->ephemeris_path_arg); /* free previous string */
            args_info->ephemeris_path_arg = gengetopt_strdup (optarg);
            if (args_info->ephemeris_path_orig)
              free (args_info->ephemeris_path_orig); /* free previous string */
            args_info->ephemeris_path_orig = gengetopt_strdup (optarg);
          }
          /* Earth ephemeris file, overrides ephemeris-path argument.  */
          else if (strcmp (long_options[option_index].name, "earth-ephemeris") == 0)
          {
            if (local_args_info.earth_ephemeris_given)
              {
                fprintf (stderr, "%s: `--earth-ephemeris' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->earth_ephemeris_given && ! override)
              continue;
            local_args_info.earth_ephemeris_given = 1;
            args_info->earth_ephemeris_given = 1;
            if (args_info->earth_ephemeris_arg)
              free (args_info->earth_ephemeris_arg); /* free previous string */
            args_info->earth_ephemeris_arg = gengetopt_strdup (optarg);
            if (args_info->earth_ephemeris_orig)
              free (args_info->earth_ephemeris_orig); /* free previous string */
            args_info->earth_ephemeris_orig = gengetopt_strdup (optarg);
          }
          /* Sun ephemeris file, overrides ephemeris-path argument.  */
          else if (strcmp (long_options[option_index].name, "sun-ephemeris") == 0)
          {
            if (local_args_info.sun_ephemeris_given)
              {
                fprintf (stderr, "%s: `--sun-ephemeris' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->sun_ephemeris_given && ! override)
              continue;
            local_args_info.sun_ephemeris_given = 1;
            args_info->sun_ephemeris_given = 1;
            if (args_info->sun_ephemeris_arg)
              free (args_info->sun_ephemeris_arg); /* free previous string */
            args_info->sun_ephemeris_arg = gengetopt_strdup (optarg);
            if (args_info->sun_ephemeris_orig)
              free (args_info->sun_ephemeris_orig); /* free previous string */
            args_info->sun_ephemeris_orig = gengetopt_strdup (optarg);
          }
          /* number of bins to cut from each side due to corruption from doppler shifts.  */
          else if (strcmp (long_options[option_index].name, "side-cut") == 0)
          {
            if (local_args_info.side_cut_given)
              {
                fprintf (stderr, "%s: `--side-cut' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->side_cut_given && ! override)
              continue;
            local_args_info.side_cut_given = 1;
            args_info->side_cut_given = 1;
            args_info->side_cut_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->side_cut_orig)
              free (args_info->side_cut_orig); /* free previous string */
            args_info->side_cut_orig = gengetopt_strdup (optarg);
          }
          /* expected timebase in months.  */
          else if (strcmp (long_options[option_index].name, "expected-timebase") == 0)
          {
            if (local_args_info.expected_timebase_given)
              {
                fprintf (stderr, "%s: `--expected-timebase' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->expected_timebase_given && ! override)
              continue;
            local_args_info.expected_timebase_given = 1;
            args_info->expected_timebase_given = 1;
            args_info->expected_timebase_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->expected_timebase_orig)
              free (args_info->expected_timebase_orig); /* free previous string */
            args_info->expected_timebase_orig = gengetopt_strdup (optarg);
          }
          /* number of bins to use when producing histograms.  */
          else if (strcmp (long_options[option_index].name, "hist-bins") == 0)
          {
            if (local_args_info.hist_bins_given)
              {
                fprintf (stderr, "%s: `--hist-bins' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->hist_bins_given && ! override)
              continue;
            local_args_info.hist_bins_given = 1;
            args_info->hist_bins_given = 1;
            args_info->hist_bins_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->hist_bins_orig)
              free (args_info->hist_bins_orig); /* free previous string */
            args_info->hist_bins_orig = gengetopt_strdup (optarg);
          }
          /* a constant to multiply Doppler shifts by (1.0 corresponds to standard physics).  */
          else if (strcmp (long_options[option_index].name, "doppler-multiplier") == 0)
          {
            if (local_args_info.doppler_multiplier_given)
              {
                fprintf (stderr, "%s: `--doppler-multiplier' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->doppler_multiplier_given && ! override)
              continue;
            local_args_info.doppler_multiplier_given = 1;
            args_info->doppler_multiplier_given = 1;
            args_info->doppler_multiplier_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->doppler_multiplier_orig)
              free (args_info->doppler_multiplier_orig); /* free previous string */
            args_info->doppler_multiplier_orig = gengetopt_strdup (optarg);
          }
          /* specify spindown start time in GPS sec. Assumed to be the first SFT segment by default.  */
          else if (strcmp (long_options[option_index].name, "spindown-start-time") == 0)
          {
            if (local_args_info.spindown_start_time_given)
              {
                fprintf (stderr, "%s: `--spindown-start-time' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->spindown_start_time_given && ! override)
              continue;
            local_args_info.spindown_start_time_given = 1;
            args_info->spindown_start_time_given = 1;
            args_info->spindown_start_time_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->spindown_start_time_orig)
              free (args_info->spindown_start_time_orig); /* free previous string */
            args_info->spindown_start_time_orig = gengetopt_strdup (optarg);
          }
          /* (small) frequency offset - used to achieve fractional bin shifts.  */
          else if (strcmp (long_options[option_index].name, "frequency-offset") == 0)
          {
            if (local_args_info.frequency_offset_given)
              {
                fprintf (stderr, "%s: `--frequency-offset' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->frequency_offset_given && ! override)
              continue;
            local_args_info.frequency_offset_given = 1;
            args_info->frequency_offset_given = 1;
            args_info->frequency_offset_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->frequency_offset_orig)
              free (args_info->frequency_offset_orig); /* free previous string */
            args_info->frequency_offset_orig = gengetopt_strdup (optarg);
          }
          /* first spindown value to process.  */
          else if (strcmp (long_options[option_index].name, "spindown-start") == 0)
          {
            if (local_args_info.spindown_start_given)
              {
                fprintf (stderr, "%s: `--spindown-start' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->spindown_start_given && ! override)
              continue;
            local_args_info.spindown_start_given = 1;
            args_info->spindown_start_given = 1;
            args_info->spindown_start_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->spindown_start_orig)
              free (args_info->spindown_start_orig); /* free previous string */
            args_info->spindown_start_orig = gengetopt_strdup (optarg);
          }
          /* step for processing multiple spindown values.  */
          else if (strcmp (long_options[option_index].name, "spindown-step") == 0)
          {
            if (local_args_info.spindown_step_given)
              {
                fprintf (stderr, "%s: `--spindown-step' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->spindown_step_given && ! override)
              continue;
            local_args_info.spindown_step_given = 1;
            args_info->spindown_step_given = 1;
            args_info->spindown_step_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->spindown_step_orig)
              free (args_info->spindown_step_orig); /* free previous string */
            args_info->spindown_step_orig = gengetopt_strdup (optarg);
          }
          /* how many separate spindown values to process.  */
          else if (strcmp (long_options[option_index].name, "spindown-count") == 0)
          {
            if (local_args_info.spindown_count_given)
              {
                fprintf (stderr, "%s: `--spindown-count' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->spindown_count_given && ! override)
              continue;
            local_args_info.spindown_count_given = 1;
            args_info->spindown_count_given = 1;
            args_info->spindown_count_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->spindown_count_orig)
              free (args_info->spindown_count_orig); /* free previous string */
            args_info->spindown_count_orig = gengetopt_strdup (optarg);
          }
          /* second frequency derivative.  */
          else if (strcmp (long_options[option_index].name, "fdotdot") == 0)
          {
            if (local_args_info.fdotdot_given)
              {
                fprintf (stderr, "%s: `--fdotdot' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fdotdot_given && ! override)
              continue;
            local_args_info.fdotdot_given = 1;
            args_info->fdotdot_given = 1;
            args_info->fdotdot_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fdotdot_orig)
              free (args_info->fdotdot_orig); /* free previous string */
            args_info->fdotdot_orig = gengetopt_strdup (optarg);
          }
          /* additional orientation phase, specifying 0.7853 will turn plus into cross.  */
          else if (strcmp (long_options[option_index].name, "orientation") == 0)
          {
            if (local_args_info.orientation_given)
              {
                fprintf (stderr, "%s: `--orientation' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->orientation_given && ! override)
              continue;
            local_args_info.orientation_given = 1;
            args_info->orientation_given = 1;
            args_info->orientation_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->orientation_orig)
              free (args_info->orientation_orig); /* free previous string */
            args_info->orientation_orig = gengetopt_strdup (optarg);
          }
          /* even number of linear polarizations to profile, distributed uniformly between 0 and PI/2.  */
          else if (strcmp (long_options[option_index].name, "nlinear-polarizations") == 0)
          {
            if (local_args_info.nlinear_polarizations_given)
              {
                fprintf (stderr, "%s: `--nlinear-polarizations' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->nlinear_polarizations_given && ! override)
              continue;
            local_args_info.nlinear_polarizations_given = 1;
            args_info->nlinear_polarizations_given = 1;
            args_info->nlinear_polarizations_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->nlinear_polarizations_orig)
              free (args_info->nlinear_polarizations_orig); /* free previous string */
            args_info->nlinear_polarizations_orig = gengetopt_strdup (optarg);
          }
          /* do not perform demodulation stage, analyze background only.  */
          else if (strcmp (long_options[option_index].name, "no-demodulation") == 0)
          {
            if (local_args_info.no_demodulation_given)
              {
                fprintf (stderr, "%s: `--no-demodulation' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->no_demodulation_given && ! override)
              continue;
            local_args_info.no_demodulation_given = 1;
            args_info->no_demodulation_given = 1;
            args_info->no_demodulation_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->no_demodulation_orig)
              free (args_info->no_demodulation_orig); /* free previous string */
            args_info->no_demodulation_orig = gengetopt_strdup (optarg);
          }
          /* do not perform noise decomposition stage, output simple statistics only.  */
          else if (strcmp (long_options[option_index].name, "no-decomposition") == 0)
          {
            if (local_args_info.no_decomposition_given)
              {
                fprintf (stderr, "%s: `--no-decomposition' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->no_decomposition_given && ! override)
              continue;
            local_args_info.no_decomposition_given = 1;
            args_info->no_decomposition_given = 1;
            args_info->no_decomposition_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->no_decomposition_orig)
              free (args_info->no_decomposition_orig); /* free previous string */
            args_info->no_decomposition_orig = gengetopt_strdup (optarg);
          }
          /* do not perform analysis to identify candidates.  */
          else if (strcmp (long_options[option_index].name, "no-candidates") == 0)
          {
            if (local_args_info.no_candidates_given)
              {
                fprintf (stderr, "%s: `--no-candidates' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->no_candidates_given && ! override)
              continue;
            local_args_info.no_candidates_given = 1;
            args_info->no_candidates_given = 1;
            args_info->no_candidates_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->no_candidates_orig)
              free (args_info->no_candidates_orig); /* free previous string */
            args_info->no_candidates_orig = gengetopt_strdup (optarg);
          }
          /* force AM_response() function to return 1.0 irrespective of the arguments.  */
          else if (strcmp (long_options[option_index].name, "no-am-response") == 0)
          {
            if (local_args_info.no_am_response_given)
              {
                fprintf (stderr, "%s: `--no-am-response' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->no_am_response_given && ! override)
              continue;
            local_args_info.no_am_response_given = 1;
            args_info->no_am_response_given = 1;
            args_info->no_am_response_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->no_am_response_orig)
              free (args_info->no_am_response_orig); /* free previous string */
            args_info->no_am_response_orig = gengetopt_strdup (optarg);
          }
          /* do not store values not essential for upper limits and followup.  */
          else if (strcmp (long_options[option_index].name, "no-secondary-skymaps") == 0)
          {
            if (local_args_info.no_secondary_skymaps_given)
              {
                fprintf (stderr, "%s: `--no-secondary-skymaps' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->no_secondary_skymaps_given && ! override)
              continue;
            local_args_info.no_secondary_skymaps_given = 1;
            args_info->no_secondary_skymaps_given = 1;
            args_info->no_secondary_skymaps_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->no_secondary_skymaps_orig)
              free (args_info->no_secondary_skymaps_orig); /* free previous string */
            args_info->no_secondary_skymaps_orig = gengetopt_strdup (optarg);
          }
          /* 1 - use one bin, 3 - average 3, matched - use 7 bin matched filter.  */
          else if (strcmp (long_options[option_index].name, "averaging-mode") == 0)
          {
            if (local_args_info.averaging_mode_given)
              {
                fprintf (stderr, "%s: `--averaging-mode' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->averaging_mode_given && ! override)
              continue;
            local_args_info.averaging_mode_given = 1;
            args_info->averaging_mode_given = 1;
            if (args_info->averaging_mode_arg)
              free (args_info->averaging_mode_arg); /* free previous string */
            args_info->averaging_mode_arg = gengetopt_strdup (optarg);
            if (args_info->averaging_mode_orig)
              free (args_info->averaging_mode_orig); /* free previous string */
            args_info->averaging_mode_orig = gengetopt_strdup (optarg);
          }
          /* subtract rank 1 matrix in order to flatten noise spectrum.  */
          else if (strcmp (long_options[option_index].name, "subtract-background") == 0)
          {
            if (local_args_info.subtract_background_given)
              {
                fprintf (stderr, "%s: `--subtract-background' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->subtract_background_given && ! override)
              continue;
            local_args_info.subtract_background_given = 1;
            args_info->subtract_background_given = 1;
            args_info->subtract_background_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->subtract_background_orig)
              free (args_info->subtract_background_orig); /* free previous string */
            args_info->subtract_background_orig = gengetopt_strdup (optarg);
          }
          /* neglect contribution from SFT with high effective noise level.  */
          else if (strcmp (long_options[option_index].name, "do-cutoff") == 0)
          {
            if (local_args_info.do_cutoff_given)
              {
                fprintf (stderr, "%s: `--do-cutoff' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->do_cutoff_given && ! override)
              continue;
            local_args_info.do_cutoff_given = 1;
            args_info->do_cutoff_given = 1;
            args_info->do_cutoff_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->do_cutoff_orig)
              free (args_info->do_cutoff_orig); /* free previous string */
            args_info->do_cutoff_orig = gengetopt_strdup (optarg);
          }
          /* perform detection of lines in background noise and veto corresponding frequency bins.  */
          else if (strcmp (long_options[option_index].name, "filter-lines") == 0)
          {
            if (local_args_info.filter_lines_given)
              {
                fprintf (stderr, "%s: `--filter-lines' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->filter_lines_given && ! override)
              continue;
            local_args_info.filter_lines_given = 1;
            args_info->filter_lines_given = 1;
            args_info->filter_lines_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->filter_lines_orig)
              free (args_info->filter_lines_orig); /* free previous string */
            args_info->filter_lines_orig = gengetopt_strdup (optarg);
          }
          /* perform Kolmogorov-Smirnov test for normality of averaged powers.  */
          else if (strcmp (long_options[option_index].name, "ks-test") == 0)
          {
            if (local_args_info.ks_test_given)
              {
                fprintf (stderr, "%s: `--ks-test' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->ks_test_given && ! override)
              continue;
            local_args_info.ks_test_given = 1;
            args_info->ks_test_given = 1;
            args_info->ks_test_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->ks_test_orig)
              free (args_info->ks_test_orig); /* free previous string */
            args_info->ks_test_orig = gengetopt_strdup (optarg);
          }
          /* compute beta coefficients as described in PowerFlux polarizations document.  */
          else if (strcmp (long_options[option_index].name, "compute-betas") == 0)
          {
            if (local_args_info.compute_betas_given)
              {
                fprintf (stderr, "%s: `--compute-betas' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->compute_betas_given && ! override)
              continue;
            local_args_info.compute_betas_given = 1;
            args_info->compute_betas_given = 1;
            args_info->compute_betas_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->compute_betas_orig)
              free (args_info->compute_betas_orig); /* free previous string */
            args_info->compute_betas_orig = gengetopt_strdup (optarg);
          }
          /* upper limit compensation factor - used to account for windowing in SFTs (possible values: Hann, flat, arbitrary number).  */
          else if (strcmp (long_options[option_index].name, "upper-limit-comp") == 0)
          {
            if (local_args_info.upper_limit_comp_given)
              {
                fprintf (stderr, "%s: `--upper-limit-comp' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->upper_limit_comp_given && ! override)
              continue;
            local_args_info.upper_limit_comp_given = 1;
            args_info->upper_limit_comp_given = 1;
            if (args_info->upper_limit_comp_arg)
              free (args_info->upper_limit_comp_arg); /* free previous string */
            args_info->upper_limit_comp_arg = gengetopt_strdup (optarg);
            if (args_info->upper_limit_comp_orig)
              free (args_info->upper_limit_comp_orig); /* free previous string */
            args_info->upper_limit_comp_orig = gengetopt_strdup (optarg);
          }
          /* lower limit compensation factor - used to account for windowing in SFTs (possible values: Hann, flat, arbitrary number).  */
          else if (strcmp (long_options[option_index].name, "lower-limit-comp") == 0)
          {
            if (local_args_info.lower_limit_comp_given)
              {
                fprintf (stderr, "%s: `--lower-limit-comp' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->lower_limit_comp_given && ! override)
              continue;
            local_args_info.lower_limit_comp_given = 1;
            args_info->lower_limit_comp_given = 1;
            if (args_info->lower_limit_comp_arg)
              free (args_info->lower_limit_comp_arg); /* free previous string */
            args_info->lower_limit_comp_arg = gengetopt_strdup (optarg);
            if (args_info->lower_limit_comp_orig)
              free (args_info->lower_limit_comp_orig); /* free previous string */
            args_info->lower_limit_comp_orig = gengetopt_strdup (optarg);
          }
          /* regular expression describing which *.dat files to write.  */
          else if (strcmp (long_options[option_index].name, "write-dat") == 0)
          {
            if (local_args_info.write_dat_given)
              {
                fprintf (stderr, "%s: `--write-dat' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->write_dat_given && ! override)
              continue;
            local_args_info.write_dat_given = 1;
            args_info->write_dat_given = 1;
            if (args_info->write_dat_arg)
              free (args_info->write_dat_arg); /* free previous string */
            args_info->write_dat_arg = gengetopt_strdup (optarg);
            if (args_info->write_dat_orig)
              free (args_info->write_dat_orig); /* free previous string */
            args_info->write_dat_orig = gengetopt_strdup (optarg);
          }
          /* regular expression describing which *.png files to write.  */
          else if (strcmp (long_options[option_index].name, "write-png") == 0)
          {
            if (local_args_info.write_png_given)
              {
                fprintf (stderr, "%s: `--write-png' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->write_png_given && ! override)
              continue;
            local_args_info.write_png_given = 1;
            args_info->write_png_given = 1;
            if (args_info->write_png_arg)
              free (args_info->write_png_arg); /* free previous string */
            args_info->write_png_arg = gengetopt_strdup (optarg);
            if (args_info->write_png_orig)
              free (args_info->write_png_orig); /* free previous string */
            args_info->write_png_orig = gengetopt_strdup (optarg);
          }
          /* output averaged power bins for each point in the sky.  */
          else if (strcmp (long_options[option_index].name, "dump-points") == 0)
          {
            if (local_args_info.dump_points_given)
              {
                fprintf (stderr, "%s: `--dump-points' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->dump_points_given && ! override)
              continue;
            local_args_info.dump_points_given = 1;
            args_info->dump_points_given = 1;
            args_info->dump_points_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->dump_points_orig)
              free (args_info->dump_points_orig); /* free previous string */
            args_info->dump_points_orig = gengetopt_strdup (optarg);
          }
          /* output SFT data for first N candidates.  */
          else if (strcmp (long_options[option_index].name, "dump-candidates") == 0)
          {
            if (local_args_info.dump_candidates_given)
              {
                fprintf (stderr, "%s: `--dump-candidates' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->dump_candidates_given && ! override)
              continue;
            local_args_info.dump_candidates_given = 1;
            args_info->dump_candidates_given = 1;
            args_info->dump_candidates_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->dump_candidates_orig)
              free (args_info->dump_candidates_orig); /* free previous string */
            args_info->dump_candidates_orig = gengetopt_strdup (optarg);
          }
          /* focus computation on a circular area with center at this RA.  */
          else if (strcmp (long_options[option_index].name, "focus-ra") == 0)
          {
            if (local_args_info.focus_ra_given)
              {
                fprintf (stderr, "%s: `--focus-ra' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->focus_ra_given && ! override)
              continue;
            local_args_info.focus_ra_given = 1;
            args_info->focus_ra_given = 1;
            args_info->focus_ra_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->focus_ra_orig)
              free (args_info->focus_ra_orig); /* free previous string */
            args_info->focus_ra_orig = gengetopt_strdup (optarg);
          }
          /* focus computation on a circular area with center at this DEC.  */
          else if (strcmp (long_options[option_index].name, "focus-dec") == 0)
          {
            if (local_args_info.focus_dec_given)
              {
                fprintf (stderr, "%s: `--focus-dec' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->focus_dec_given && ! override)
              continue;
            local_args_info.focus_dec_given = 1;
            args_info->focus_dec_given = 1;
            args_info->focus_dec_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->focus_dec_orig)
              free (args_info->focus_dec_orig); /* free previous string */
            args_info->focus_dec_orig = gengetopt_strdup (optarg);
          }
          /* focus computation on a circular area with this radius.  */
          else if (strcmp (long_options[option_index].name, "focus-radius") == 0)
          {
            if (local_args_info.focus_radius_given)
              {
                fprintf (stderr, "%s: `--focus-radius' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->focus_radius_given && ! override)
              continue;
            local_args_info.focus_radius_given = 1;
            args_info->focus_radius_given = 1;
            args_info->focus_radius_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->focus_radius_orig)
              free (args_info->focus_radius_orig); /* free previous string */
            args_info->focus_radius_orig = gengetopt_strdup (optarg);
          }
          /* restrict computation to points on the sky with cos of angle to band axis larger than a given number.  */
          else if (strcmp (long_options[option_index].name, "only-large-cos") == 0)
          {
            if (local_args_info.only_large_cos_given)
              {
                fprintf (stderr, "%s: `--only-large-cos' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->only_large_cos_given && ! override)
              continue;
            local_args_info.only_large_cos_given = 1;
            args_info->only_large_cos_given = 1;
            args_info->only_large_cos_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->only_large_cos_orig)
              free (args_info->only_large_cos_orig); /* free previous string */
            args_info->only_large_cos_orig = gengetopt_strdup (optarg);
          }
          /* Inject linearly polarized fake signal.  */
          else if (strcmp (long_options[option_index].name, "fake-linear") == 0)
          {
            if (local_args_info.fake_linear_given)
              {
                fprintf (stderr, "%s: `--fake-linear' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_linear_given && ! override)
              continue;
            local_args_info.fake_linear_given = 1;
            args_info->fake_linear_given = 1;
            if (args_info->injection_group_counter && override)
              reset_group_injection (args_info);
            args_info->injection_group_counter += 1;
            break;
          }
          /* Inject circularly polarized fake signal.  */
          else if (strcmp (long_options[option_index].name, "fake-circular") == 0)
          {
            if (local_args_info.fake_circular_given)
              {
                fprintf (stderr, "%s: `--fake-circular' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_circular_given && ! override)
              continue;
            local_args_info.fake_circular_given = 1;
            args_info->fake_circular_given = 1;
            if (args_info->injection_group_counter && override)
              reset_group_injection (args_info);
            args_info->injection_group_counter += 1;
            break;
          }
          /* time of signal start.  */
          else if (strcmp (long_options[option_index].name, "fake-ref-time") == 0)
          {
            if (local_args_info.fake_ref_time_given)
              {
                fprintf (stderr, "%s: `--fake-ref-time' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_ref_time_given && ! override)
              continue;
            local_args_info.fake_ref_time_given = 1;
            args_info->fake_ref_time_given = 1;
            args_info->fake_ref_time_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fake_ref_time_orig)
              free (args_info->fake_ref_time_orig); /* free previous string */
            args_info->fake_ref_time_orig = gengetopt_strdup (optarg);
          }
          /* RA of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-ra") == 0)
          {
            if (local_args_info.fake_ra_given)
              {
                fprintf (stderr, "%s: `--fake-ra' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_ra_given && ! override)
              continue;
            local_args_info.fake_ra_given = 1;
            args_info->fake_ra_given = 1;
            args_info->fake_ra_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fake_ra_orig)
              free (args_info->fake_ra_orig); /* free previous string */
            args_info->fake_ra_orig = gengetopt_strdup (optarg);
          }
          /* DEC of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-dec") == 0)
          {
            if (local_args_info.fake_dec_given)
              {
                fprintf (stderr, "%s: `--fake-dec' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_dec_given && ! override)
              continue;
            local_args_info.fake_dec_given = 1;
            args_info->fake_dec_given = 1;
            args_info->fake_dec_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fake_dec_orig)
              free (args_info->fake_dec_orig); /* free previous string */
            args_info->fake_dec_orig = gengetopt_strdup (optarg);
          }
          /* iota of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-iota") == 0)
          {
            if (local_args_info.fake_iota_given)
              {
                fprintf (stderr, "%s: `--fake-iota' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_iota_given && ! override)
              continue;
            local_args_info.fake_iota_given = 1;
            args_info->fake_iota_given = 1;
            args_info->fake_iota_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fake_iota_orig)
              free (args_info->fake_iota_orig); /* free previous string */
            args_info->fake_iota_orig = gengetopt_strdup (optarg);
          }
          /* orientation of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-psi") == 0)
          {
            if (local_args_info.fake_psi_given)
              {
                fprintf (stderr, "%s: `--fake-psi' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_psi_given && ! override)
              continue;
            local_args_info.fake_psi_given = 1;
            args_info->fake_psi_given = 1;
            args_info->fake_psi_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fake_psi_orig)
              free (args_info->fake_psi_orig); /* free previous string */
            args_info->fake_psi_orig = gengetopt_strdup (optarg);
          }
          /* phase of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-phi") == 0)
          {
            if (local_args_info.fake_phi_given)
              {
                fprintf (stderr, "%s: `--fake-phi' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_phi_given && ! override)
              continue;
            local_args_info.fake_phi_given = 1;
            args_info->fake_phi_given = 1;
            args_info->fake_phi_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fake_phi_orig)
              free (args_info->fake_phi_orig); /* free previous string */
            args_info->fake_phi_orig = gengetopt_strdup (optarg);
          }
          /* spindown of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-spindown") == 0)
          {
            if (local_args_info.fake_spindown_given)
              {
                fprintf (stderr, "%s: `--fake-spindown' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_spindown_given && ! override)
              continue;
            local_args_info.fake_spindown_given = 1;
            args_info->fake_spindown_given = 1;
            args_info->fake_spindown_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fake_spindown_orig)
              free (args_info->fake_spindown_orig); /* free previous string */
            args_info->fake_spindown_orig = gengetopt_strdup (optarg);
          }
          /* amplitude of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-strain") == 0)
          {
            if (local_args_info.fake_strain_given)
              {
                fprintf (stderr, "%s: `--fake-strain' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_strain_given && ! override)
              continue;
            local_args_info.fake_strain_given = 1;
            args_info->fake_strain_given = 1;
            args_info->fake_strain_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fake_strain_orig)
              free (args_info->fake_strain_orig); /* free previous string */
            args_info->fake_strain_orig = gengetopt_strdup (optarg);
          }
          /* frequency of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-freq") == 0)
          {
            if (local_args_info.fake_freq_given)
              {
                fprintf (stderr, "%s: `--fake-freq' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->fake_freq_given && ! override)
              continue;
            local_args_info.fake_freq_given = 1;
            args_info->fake_freq_given = 1;
            args_info->fake_freq_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->fake_freq_orig)
              free (args_info->fake_freq_orig); /* free previous string */
            args_info->fake_freq_orig = gengetopt_strdup (optarg);
          }
          /* Assumed level of error in detection strength - used for listing candidates.  */
          else if (strcmp (long_options[option_index].name, "snr-precision") == 0)
          {
            if (local_args_info.snr_precision_given)
              {
                fprintf (stderr, "%s: `--snr-precision' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->snr_precision_given && ! override)
              continue;
            local_args_info.snr_precision_given = 1;
            args_info->snr_precision_given = 1;
            args_info->snr_precision_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->snr_precision_orig)
              free (args_info->snr_precision_orig); /* free previous string */
            args_info->snr_precision_orig = gengetopt_strdup (optarg);
          }
          /* Do not optimize more than this number of candidates.  */
          else if (strcmp (long_options[option_index].name, "max-candidates") == 0)
          {
            if (local_args_info.max_candidates_given)
              {
                fprintf (stderr, "%s: `--max-candidates' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->max_candidates_given && ! override)
              continue;
            local_args_info.max_candidates_given = 1;
            args_info->max_candidates_given = 1;
            args_info->max_candidates_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->max_candidates_orig)
              free (args_info->max_candidates_orig); /* free previous string */
            args_info->max_candidates_orig = gengetopt_strdup (optarg);
          }
          /* Do not optimize candidates with SNR below this level.  */
          else if (strcmp (long_options[option_index].name, "min-candidate-snr") == 0)
          {
            if (local_args_info.min_candidate_snr_given)
              {
                fprintf (stderr, "%s: `--min-candidate-snr' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->min_candidate_snr_given && ! override)
              continue;
            local_args_info.min_candidate_snr_given = 1;
            args_info->min_candidate_snr_given = 1;
            args_info->min_candidate_snr_arg = strtod (optarg, &stop_char);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->min_candidate_snr_orig)
              free (args_info->min_candidate_snr_orig); /* free previous string */
            args_info->min_candidate_snr_orig = gengetopt_strdup (optarg);
          }
          /* write initial candidates into log file.  */
          else if (strcmp (long_options[option_index].name, "output-initial") == 0)
          {
            if (local_args_info.output_initial_given)
              {
                fprintf (stderr, "%s: `--output-initial' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->output_initial_given && ! override)
              continue;
            local_args_info.output_initial_given = 1;
            args_info->output_initial_given = 1;
            args_info->output_initial_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->output_initial_orig)
              free (args_info->output_initial_orig); /* free previous string */
            args_info->output_initial_orig = gengetopt_strdup (optarg);
          }
          /* write optimized (second pass) candidates into log file.  */
          else if (strcmp (long_options[option_index].name, "output-optimized") == 0)
          {
            if (local_args_info.output_optimized_given)
              {
                fprintf (stderr, "%s: `--output-optimized' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->output_optimized_given && ! override)
              continue;
            local_args_info.output_optimized_given = 1;
            args_info->output_optimized_given = 1;
            args_info->output_optimized_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->output_optimized_orig)
              free (args_info->output_optimized_orig); /* free previous string */
            args_info->output_optimized_orig = gengetopt_strdup (optarg);
          }
          /* write out all candidates in cache to log file.  */
          else if (strcmp (long_options[option_index].name, "output-cache") == 0)
          {
            if (local_args_info.output_cache_given)
              {
                fprintf (stderr, "%s: `--output-cache' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->output_cache_given && ! override)
              continue;
            local_args_info.output_cache_given = 1;
            args_info->output_cache_given = 1;
            args_info->output_cache_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->output_cache_orig)
              free (args_info->output_cache_orig); /* free previous string */
            args_info->output_cache_orig = gengetopt_strdup (optarg);
          }
          /* Perform extended self test.  */
          else if (strcmp (long_options[option_index].name, "extended-test") == 0)
          {
            if (local_args_info.extended_test_given)
              {
                fprintf (stderr, "%s: `--extended-test' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->extended_test_given && ! override)
              continue;
            local_args_info.extended_test_given = 1;
            args_info->extended_test_given = 1;
            args_info->extended_test_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->extended_test_orig)
              free (args_info->extended_test_orig); /* free previous string */
            args_info->extended_test_orig = gengetopt_strdup (optarg);
          }
          /* Maximum count of SFTs to report with veto information.  */
          else if (strcmp (long_options[option_index].name, "max-sft-report") == 0)
          {
            if (local_args_info.max_sft_report_given)
              {
                fprintf (stderr, "%s: `--max-sft-report' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->max_sft_report_given && ! override)
              continue;
            local_args_info.max_sft_report_given = 1;
            args_info->max_sft_report_given = 1;
            args_info->max_sft_report_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->max_sft_report_orig)
              free (args_info->max_sft_report_orig); /* free previous string */
            args_info->max_sft_report_orig = gengetopt_strdup (optarg);
          }
          /* Use that many threads for computation.  */
          else if (strcmp (long_options[option_index].name, "num-threads") == 0)
          {
            if (local_args_info.num_threads_given)
              {
                fprintf (stderr, "%s: `--num-threads' option given more than once%s\n", argv[0], (additional_error ? additional_error : ""));
                goto failure;
              }
            if (args_info->num_threads_given && ! override)
              continue;
            local_args_info.num_threads_given = 1;
            args_info->num_threads_given = 1;
            args_info->num_threads_arg = strtol (optarg, &stop_char, 0);
            if (!(stop_char && *stop_char == '\0')) {
              fprintf(stderr, "%s: invalid numeric value: %s\n", argv[0], optarg);
              goto failure;
            }
            if (args_info->num_threads_orig)
              free (args_info->num_threads_orig); /* free previous string */
            args_info->num_threads_orig = gengetopt_strdup (optarg);
          }
          
          break;
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */

  if (args_info->injection_group_counter > 1)
    {
      fprintf (stderr, "%s: %d options of group injection were given. At most one is required.%s\n", argv[0], args_info->injection_group_counter, (additional_error ? additional_error : ""));
      error = 1;
    }
  

  if (local_args_info.config_given && config_list)
    {
      struct string_list *tmp;
      args_info->config_arg = (char * *) realloc (args_info->config_arg, (args_info->config_given + local_args_info.config_given) * sizeof (char *));
      args_info->config_orig = (char **) realloc (args_info->config_orig, (args_info->config_given + local_args_info.config_given) * sizeof (char *));
      for (i = (local_args_info.config_given - 1); i >= 0; --i)
        {
          tmp = config_list;
          args_info->config_arg [i + args_info->config_given] = config_list->arg;
          args_info->config_orig [i + args_info->config_given] = config_list->orig;
          config_list = config_list->next;
          free (tmp);
        }
    }
  

  args_info->config_given += local_args_info.config_given;
  local_args_info.config_given = 0;
  
  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_release (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  if (config_list)
    {
      struct string_list *tmp;
      while (config_list)
        {
          tmp = config_list;
          free (config_list->arg);
          free (config_list->orig);
          config_list = config_list->next;
          free (tmp);
        }
    }
  
  cmdline_parser_release (&local_args_info);
  return (EXIT_FAILURE);
}

#ifndef CONFIG_FILE_LINE_SIZE
#define CONFIG_FILE_LINE_SIZE 2048
#endif
#define ADDITIONAL_ERROR " in configuration file "

#define CONFIG_FILE_LINE_BUFFER_SIZE (CONFIG_FILE_LINE_SIZE+3)
/* 3 is for "--" and "=" */

char my_argv[CONFIG_FILE_LINE_BUFFER_SIZE+1];

int
cmdline_parser_configfile (char * const filename, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  FILE* file;
  char linebuf[CONFIG_FILE_LINE_SIZE];
  int line_num = 0;
  int i, result, equal;
  char *fopt, *farg;
  char *str_index;
  size_t len, next_token;
  char delimiter;
  int my_argc = 0;
  char **my_argv_arg;
  char *additional_error;

  /* store the program name */
  cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
  cmd_line_list_tmp->next = cmd_line_list;
  cmd_line_list = cmd_line_list_tmp;
  cmd_line_list->string_arg = gengetopt_strdup (CMDLINE_PARSER_PACKAGE);

  if ((file = fopen(filename, "r")) == NULL)
    {
      fprintf (stderr, "%s: Error opening configuration file '%s'\n",
               CMDLINE_PARSER_PACKAGE, filename);
      result = EXIT_FAILURE;
      goto conf_failure;
    }

  while ((fgets(linebuf, CONFIG_FILE_LINE_SIZE, file)) != NULL)
    {
      ++line_num;
      my_argv[0] = '\0';
      len = strlen(linebuf);
      if (len > (CONFIG_FILE_LINE_BUFFER_SIZE-1))
        {
          fprintf (stderr, "%s:%s:%d: Line too long in configuration file\n",
                   CMDLINE_PARSER_PACKAGE, filename, line_num);
          result = EXIT_FAILURE;
          goto conf_failure;
        }

      /* find first non-whitespace character in the line */
      next_token = strspn ( linebuf, " \t\r\n");
      str_index  = linebuf + next_token;

      if ( str_index[0] == '\0' || str_index[0] == '#')
        continue; /* empty line or comment line is skipped */

      fopt = str_index;

      /* truncate fopt at the end of the first non-valid character */
      next_token = strcspn (fopt, " \t\r\n=");

      if (fopt[next_token] == '\0') /* the line is over */
        {
          farg  = NULL;
          equal = 0;
          goto noarg;
        }

      /* remember if equal sign is present */
      equal = (fopt[next_token] == '=');
      fopt[next_token++] = '\0';

      /* advance pointers to the next token after the end of fopt */
      next_token += strspn (fopt + next_token, " \t\r\n");
      /* check for the presence of equal sign, and if so, skip it */
      if ( !equal )
        if ((equal = (fopt[next_token] == '=')))
          {
            next_token++;
            next_token += strspn (fopt + next_token, " \t\r\n");
          }
      str_index  += next_token;

      /* find argument */
      farg = str_index;
      if ( farg[0] == '\"' || farg[0] == '\'' )
        { /* quoted argument */
          str_index = strchr (++farg, str_index[0] ); /* skip opening quote */
          if (! str_index)
            {
              fprintf
                (stderr,
                 "%s:%s:%d: unterminated string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              goto conf_failure;
            }
        }
      else
        { /* read up the remaining part up to a delimiter */
          next_token = strcspn (farg, " \t\r\n#\'\"");
          str_index += next_token;
        }

      /* truncate farg at the delimiter and store it for further check */
      delimiter = *str_index, *str_index++ = '\0';

      /* everything but comment is illegal at the end of line */
      if (delimiter != '\0' && delimiter != '#')
        {
          str_index += strspn(str_index, " \t\r\n");
          if (*str_index != '\0' && *str_index != '#')
            {
              fprintf
                (stderr,
                 "%s:%s:%d: malformed string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              goto conf_failure;
            }
        }

    noarg:
      ++my_argc;
      len = strlen(fopt);

      strcat (my_argv, len > 1 ? "--" : "-");
      strcat (my_argv, fopt);
      if (len > 1 && ((farg &&*farg) || equal))
          strcat (my_argv, "=");
      if (farg && *farg)
          strcat (my_argv, farg);

      cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
      cmd_line_list_tmp->next = cmd_line_list;
      cmd_line_list = cmd_line_list_tmp;
      cmd_line_list->string_arg = gengetopt_strdup(my_argv);
    } /* while */

  ++my_argc; /* for program name */
  my_argv_arg = (char **) malloc((my_argc+1) * sizeof(char *));
  cmd_line_list_tmp = cmd_line_list;
  for (i = my_argc - 1; i >= 0; --i) {
    my_argv_arg[i] = cmd_line_list_tmp->string_arg;
    cmd_line_list_tmp = cmd_line_list_tmp->next;
  }
  my_argv_arg[my_argc] = 0;

  additional_error = (char *)malloc(strlen(filename) + strlen(ADDITIONAL_ERROR) + 1);
  strcpy (additional_error, ADDITIONAL_ERROR);
  strcat (additional_error, filename);
  result =
    cmdline_parser_internal (my_argc, my_argv_arg, args_info, override, initialize, check_required, additional_error);

  free (additional_error);
  free (my_argv_arg);

conf_failure:
  if (file)
    fclose(file);

  free_cmd_list();
  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

static unsigned int
cmdline_parser_create_argv(const char *cmdline_, char ***argv_ptr, const char *prog_name)
{
  char *cmdline, *p;
  size_t n = 0, j;
  int i;

  if (prog_name) {
    cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
    cmd_line_list_tmp->next = cmd_line_list;
    cmd_line_list = cmd_line_list_tmp;
    cmd_line_list->string_arg = gengetopt_strdup (prog_name);

    ++n;
  }

  cmdline = gengetopt_strdup(cmdline_);
  p = cmdline;

  while (p && strlen(p))
    {
      j = strcspn(p, " \t");
      ++n;
      if (j && j < strlen(p))
        {
          p[j] = '\0';

          cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
          cmd_line_list_tmp->next = cmd_line_list;
          cmd_line_list = cmd_line_list_tmp;
          cmd_line_list->string_arg = gengetopt_strdup (p);

          p += (j+1);
          p += strspn(p, " \t");
        }
      else
        {
          cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
          cmd_line_list_tmp->next = cmd_line_list;
          cmd_line_list = cmd_line_list_tmp;
          cmd_line_list->string_arg = gengetopt_strdup (p);

          break;
        }
    }

  *argv_ptr = (char **) malloc((n + 1) * sizeof(char *));
  cmd_line_list_tmp = cmd_line_list;
  for (i = (n-1); i >= 0; --i)
    {
      (*argv_ptr)[i] = cmd_line_list_tmp->string_arg;
      cmd_line_list_tmp = cmd_line_list_tmp->next;
    }

  (*argv_ptr)[n] = NULL;

  free(cmdline);
  return n;
}

int
cmdline_parser_string(const char *cmdline, struct gengetopt_args_info *args_info, const char *prog_name)
{
  return cmdline_parser_string2(cmdline, args_info, prog_name, 0, 1, 1);
}

int
cmdline_parser_string2(const char *cmdline, struct gengetopt_args_info *args_info, const char *prog_name,
    int override, int initialize, int check_required)
{
  char **argv_ptr = 0;
  int result;
  unsigned int argc;
  
  argc = cmdline_parser_create_argv(cmdline, &argv_ptr, prog_name);
  
  result =
    cmdline_parser_internal (argc, argv_ptr, args_info, override, initialize, check_required, 0);
  
  if (argv_ptr)
    {
      free (argv_ptr);
    }

  free_cmd_list();
  
  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}


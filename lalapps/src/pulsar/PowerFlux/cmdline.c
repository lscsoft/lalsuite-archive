/*
  File autogenerated by gengetopt version 2.22
  generated with the following command:
  gengetopt --string-parser --conf-parser 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "getopt.h"

#include "cmdline.h"

const char *gengetopt_args_info_purpose = "Powerflux analysis program";

const char *gengetopt_args_info_usage = "Usage: " CMDLINE_PARSER_PACKAGE " [OPTIONS]...";

const char *gengetopt_args_info_description = "";

const char *gengetopt_args_info_help[] = {
  "  -h, --help                    Print help and exit",
  "  -V, --version                 Print version and exit",
  "  -c, --config=STRING           configuration file (in gengetopt format) to \n                                  pass parameters",
  "      --label=STRING            arbitrary string to be printed in the beginning \n                                  of PowerFlux log file  (default=`')",
  "      --sky-grid=STRING         sky grid type (arcsin, plain_rectangular, \n                                  sin_theta)  (default=`sin_theta')",
  "      --skymap-orientation=STRING\n                                orientation of produced skymaps: equatorial, \n                                  ecliptic, band_axis  (default=`equatorial')",
  "      --skyband-method=STRING   method of assigning band numbers: angle, S  \n                                  (default=`S')",
  "      --nskybands=INT           split sky in this many bands for logging \n                                  maximum upper limits  (default=`11')",
  "      --large-S=DOUBLE          value of S to consider good enough",
  "      --band-axis=STRING        which band axis to use for splitting sky into \n                                  bands (perpendicular to band axis) (possible \n                                  values: equatorial, auto, \n                                  explicit(float,float,float)  (default=`auto')",
  "      --band-axis-norm=DOUBLE   norm of band axis vector to use in S value \n                                  calculation",
  "      --sky-marks-file=STRING   file describing how to mark up a sky",
  "      --fine-factor=INT         make fine grid this times finer  (default=`5')",
  "      --skymap-resolution=DOUBLE\n                                specify skymap resolution explicitly",
  "      --skymap-resolution-ratio=DOUBLE\n                                adjust default coarseness of the grid by this \n                                  factor  (default=`1.0')",
  "      --small-weight-ratio=DOUBLE\n                                ratio that determines which weight is too small \n                                  to include in max statistics  (default=`0.2')",
  "      --strain-norm-factor=DOUBLE\n                                strain normalization factor to prevent \n                                  overflowing of the exponent  \n                                  (default=`1e-20')",
  "      --lock-file=STRING        file to lock when reading SFTs in order to \n                                  globally serialize disk access",
  "      --enable-dataset-locking=INT\n                                set to 1 to enable dataset level locking  \n                                  (default=`1')",
  "      --retry-delay=INT         number of seconds to wait before retrying I/O  \n                                  (default=`2')",
  "      --lock-retry-delay=INT    number of seconds to wait before trying to \n                                  acquire lock again  (default=`10')",
  "  -s, --dataset=STRING          dataset file",
  "  -i, --initial-dataset-seed=INT\n                                initial seed to use for generating gaussian \n                                  data  (default=`12345')",
  "      --input-format=STRING     format of input files (GEO, SFT, Power)  \n                                  (default=`GEO')",
  "      --dump-data=STRING        file to output loaded SFT data into, for \n                                  testing",
  "      --dump-sftv2=STRING       directory to output loaded data, together with \n                                  dataset description",
  "  -o, --output=STRING           output directory",
  "      --ephemeris-path=STRING   path to detresponse program from lalapps",
  "      --earth-ephemeris=STRING  Earth ephemeris file, overrides ephemeris-path \n                                  argument",
  "      --sun-ephemeris=STRING    Sun ephemeris file, overrides ephemeris-path \n                                  argument",
  "  -f, --first-bin=INT           first frequency bin in the band to be analyzed",
  "  -n, --nbins=INT               number of frequency bins to analyze  \n                                  (default=`501')",
  "      --side-cut=INT            number of bins to cut from each side due to \n                                  corruption from doppler shifts",
  "      --expected-timebase=INT   expected timebase in months  (default=`6')",
  "      --hist-bins=INT           number of bins to use when producing histograms \n                                   (default=`200')",
  "  -d, --detector=STRING         detector location (i.e. LHO or LLO), passed to \n                                  detresponse",
  "      --doppler-multiplier=DOUBLE\n                                a constant to multiply Doppler shifts by (1.0 \n                                  corresponds to standard physics)  \n                                  (default=`1.0')",
  "      --spindown-start-time=DOUBLE\n                                specify spindown start time in GPS sec. Assumed \n                                  to be the first SFT segment by default",
  "      --frequency-offset=DOUBLE (small) frequency offset - used to achieve \n                                  fractional bin shifts  (default=`0.0')",
  "      --spindown-start=DOUBLE   first spindown value to process  \n                                  (default=`0.0')",
  "      --spindown-step=DOUBLE    step for processing multiple spindown values  \n                                  (default=`5e-10')",
  "      --spindown-count=INT      how many separate spindown values to process  \n                                  (default=`1')",
  "      --fdotdot=DOUBLE          second frequency derivative  (default=`0.0')",
  "      --orientation=DOUBLE      additional orientation phase, specifying 0.7853 \n                                  will turn plus into cross  (default=`0')",
  "      --nlinear-polarizations=INT\n                                even number of linear polarizations to profile, \n                                  distributed uniformly between 0 and PI/2  \n                                  (default=`4')",
  "      --no-demodulation=INT     do not perform demodulation stage, analyze \n                                  background only  (default=`0')",
  "      --no-decomposition=INT    do not perform noise decomposition stage, \n                                  output simple statistics only  (default=`0')",
  "      --no-candidates=INT       do not perform analysis to identify candidates  \n                                  (default=`0')",
  "      --no-am-response=INT      force AM_response() function to return 1.0 \n                                  irrespective of the arguments  (default=`0')",
  "      --no-secondary-skymaps=INT\n                                do not store values not essential for upper \n                                  limits and followup  (default=`0')",
  "      --averaging-mode=STRING   1 - use one bin, 3 - average 3, matched - use 7 \n                                  bin matched filter  (default=`1')",
  "      --subtract-background=INT subtract rank 1 matrix in order to flatten \n                                  noise spectrum  (default=`0')",
  "      --do-cutoff=INT           neglect contribution from SFT with high \n                                  effective noise level  (default=`1')",
  "      --filter-lines=INT        perform detection of lines in background noise \n                                  and veto corresponding frequency bins  \n                                  (default=`1')",
  "      --ks-test=INT             perform Kolmogorov-Smirnov test for normality \n                                  of averaged powers  (default=`1')",
  "      --compute-betas=INT       compute beta coefficients as described in \n                                  PowerFlux polarizations document  \n                                  (default=`0')",
  "      --upper-limit-comp=STRING upper limit compensation factor - used to \n                                  account for windowing in SFTs (possible \n                                  values: Hann, flat, arbitrary number)  \n                                  (default=`Hann')",
  "      --lower-limit-comp=STRING lower limit compensation factor - used to \n                                  account for windowing in SFTs (possible \n                                  values: Hann, flat, arbitrary number)  \n                                  (default=`Hann')",
  "      --write-dat=STRING        regular expression describing which *.dat files \n                                  to write  (default=`.*')",
  "      --write-png=STRING        regular expression describing which *.png files \n                                  to write  (default=`.*')",
  "      --dump-points=INT         output averaged power bins for each point in \n                                  the sky  (default=`0')",
  "      --dump-candidates=INT     output SFT data for first N candidates  \n                                  (default=`0')",
  "      --focus-ra=DOUBLE         focus computation on a circular area with \n                                  center at this RA",
  "      --focus-dec=DOUBLE        focus computation on a circular area with \n                                  center at this DEC",
  "      --focus-radius=DOUBLE     focus computation on a circular area with this \n                                  radius",
  "      --only-large-cos=DOUBLE   restrict computation to points on the sky with \n                                  cos of angle to band axis larger than a given \n                                  number",
  "\n Group: injection",
  "      --fake-linear             Inject linearly polarized fake signal",
  "      --fake-circular           Inject circularly polarized fake signal",
  "      --fake-ref-time=DOUBLE    time of signal start  (default=`0')",
  "      --fake-ra=DOUBLE          RA of fake signal to inject  (default=`3.14')",
  "      --fake-dec=DOUBLE         DEC of fake signal to inject  (default=`0.0')",
  "      --fake-iota=DOUBLE        iota of fake signal to inject  (default=`0.0')",
  "      --fake-psi=DOUBLE         orientation of fake signal to inject  \n                                  (default=`0.0')",
  "      --fake-phi=DOUBLE         phase of fake signal to inject  (default=`0.0')",
  "      --fake-spindown=DOUBLE    spindown of fake signal to inject  \n                                  (default=`0.0')",
  "      --fake-strain=DOUBLE      amplitude of fake signal to inject  \n                                  (default=`1e-23')",
  "      --fake-freq=DOUBLE        frequency of fake signal to inject",
  "      --snr-precision=DOUBLE    Assumed level of error in detection strength - \n                                  used for listing candidates  (default=`0.2')",
  "      --max-candidates=INT      Do not optimize more than this number of \n                                  candidates  (default=`-1')",
  "      --min-candidate-snr=DOUBLE\n                                Do not optimize candidates with SNR below this \n                                  level  (default=`5.0')",
  "      --output-initial=INT      write initial candidates into log file  \n                                  (default=`0')",
  "      --output-optimized=INT    write optimized (second pass) candidates into \n                                  log file  (default=`0')",
  "      --output-cache=INT        write out all candidates in cache to log file  \n                                  (default=`0')",
  "      --extended-test=INT       Perform extended self test  (default=`0')",
  "      --max-sft-report=INT      Maximum count of SFTs to report with veto \n                                  information  (default=`100')",
  "      --num-threads=INT         Use that many threads for computation  \n                                  (default=`-1')",
  "      --niota=INT               Number of iota values to use in alignment grid  \n                                  (default=`3')",
  "      --npsi=INT                Number of psi values to use in alignment grid  \n                                  (default=`6')",
  "      --nfshift=INT             Number of sub-bin frequency shifts to sample  \n                                  (default=`2')",
  "      --nchunks=INT             Partition the timebase into this many chunks \n                                  for sub period analysis  (default=`5')",
  "      --weight-cutoff-fraction=DOUBLE\n                                Discard sfts with small weights that contribute \n                                  this fraction of total weight  \n                                  (default=`0.04')",
  "      --per-dataset-weight-cutoff-fraction=DOUBLE\n                                Discard sfts with small weights that contribute \n                                  this fraction of total weight in each dataset \n                                   (default=`0.04')",
  "      --power-max-median-factor=DOUBLE\n                                This determines scaling factor between median \n                                  and maximum of exponentially distributed \n                                  variable. Used for computing power sum \n                                  weights  (default=`0.1')",
  "      --tmedian-noise-level=INT Use TMedians to estimate noise level (as \n                                  opposed to in-place standard deviation)  \n                                  (default=`1')",
  "      --summing-step=INT        integration step size, in seconds  \n                                  (default=`864000')",
  "      --max-first-shift=INT     larger values accomodate bigger spindown ranges \n                                  but require more bins to be computed in \n                                  uncached function  (default=`10')",
  "      --statistics-function=STRING\n                                specify statistics postprocessing to apply. \n                                  Possible values: linear, sorted  \n                                  (default=`linear')",
  "      --dump-power-sums=INT     Write out all power sum data into data.log \n                                  file. It is recommend to restrict the sky to \n                                  very few pixels  (default=`0')",
  "      --compute-skymaps=INT     allocate memory and compute skymaps with final \n                                  results  (default=`0')",
  "      --fine-grid-skymarks=INT  use sky marks from the fine grid, this uses \n                                  constant spindown  (default=`0')",
  "      --half-window=INT         number of bins to exclude to the left and to \n                                  the right of highest point when computing \n                                  linear statistics  (default=`20')",
  "      --tail-veto=INT           do not report outlier if its frequency is \n                                  within that many bins from the tail - happens \n                                  with steep spectrum  (default=`10')",
  "      --cache-granularity=INT   granularity of power cache frequency shift \n                                  resolution, in fractions of a frequency bin  \n                                  (default=`-1')",
    0
};

typedef enum {ARG_NO
  , ARG_STRING
  , ARG_INT
  , ARG_DOUBLE
} cmdline_parser_arg_type;

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info,
                        struct cmdline_parser_params *params, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);
struct line_list
{
  char * string_arg;
  struct line_list * next;
};

static struct line_list *cmd_line_list = 0;
static struct line_list *cmd_line_list_tmp = 0;

static void
free_cmd_list(void)
{
  /* free the list of a previous call */
  if (cmd_line_list)
    {
      while (cmd_line_list) {
        cmd_line_list_tmp = cmd_line_list;
        cmd_line_list = cmd_line_list->next;
        free (cmd_line_list_tmp->string_arg);
        free (cmd_line_list_tmp);
      }
    }
}


static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->config_given = 0 ;
  args_info->label_given = 0 ;
  args_info->sky_grid_given = 0 ;
  args_info->skymap_orientation_given = 0 ;
  args_info->skyband_method_given = 0 ;
  args_info->nskybands_given = 0 ;
  args_info->large_S_given = 0 ;
  args_info->band_axis_given = 0 ;
  args_info->band_axis_norm_given = 0 ;
  args_info->sky_marks_file_given = 0 ;
  args_info->fine_factor_given = 0 ;
  args_info->skymap_resolution_given = 0 ;
  args_info->skymap_resolution_ratio_given = 0 ;
  args_info->small_weight_ratio_given = 0 ;
  args_info->strain_norm_factor_given = 0 ;
  args_info->lock_file_given = 0 ;
  args_info->enable_dataset_locking_given = 0 ;
  args_info->retry_delay_given = 0 ;
  args_info->lock_retry_delay_given = 0 ;
  args_info->dataset_given = 0 ;
  args_info->initial_dataset_seed_given = 0 ;
  args_info->input_format_given = 0 ;
  args_info->dump_data_given = 0 ;
  args_info->dump_sftv2_given = 0 ;
  args_info->output_given = 0 ;
  args_info->ephemeris_path_given = 0 ;
  args_info->earth_ephemeris_given = 0 ;
  args_info->sun_ephemeris_given = 0 ;
  args_info->first_bin_given = 0 ;
  args_info->nbins_given = 0 ;
  args_info->side_cut_given = 0 ;
  args_info->expected_timebase_given = 0 ;
  args_info->hist_bins_given = 0 ;
  args_info->detector_given = 0 ;
  args_info->doppler_multiplier_given = 0 ;
  args_info->spindown_start_time_given = 0 ;
  args_info->frequency_offset_given = 0 ;
  args_info->spindown_start_given = 0 ;
  args_info->spindown_step_given = 0 ;
  args_info->spindown_count_given = 0 ;
  args_info->fdotdot_given = 0 ;
  args_info->orientation_given = 0 ;
  args_info->nlinear_polarizations_given = 0 ;
  args_info->no_demodulation_given = 0 ;
  args_info->no_decomposition_given = 0 ;
  args_info->no_candidates_given = 0 ;
  args_info->no_am_response_given = 0 ;
  args_info->no_secondary_skymaps_given = 0 ;
  args_info->averaging_mode_given = 0 ;
  args_info->subtract_background_given = 0 ;
  args_info->do_cutoff_given = 0 ;
  args_info->filter_lines_given = 0 ;
  args_info->ks_test_given = 0 ;
  args_info->compute_betas_given = 0 ;
  args_info->upper_limit_comp_given = 0 ;
  args_info->lower_limit_comp_given = 0 ;
  args_info->write_dat_given = 0 ;
  args_info->write_png_given = 0 ;
  args_info->dump_points_given = 0 ;
  args_info->dump_candidates_given = 0 ;
  args_info->focus_ra_given = 0 ;
  args_info->focus_dec_given = 0 ;
  args_info->focus_radius_given = 0 ;
  args_info->only_large_cos_given = 0 ;
  args_info->fake_linear_given = 0 ;
  args_info->fake_circular_given = 0 ;
  args_info->fake_ref_time_given = 0 ;
  args_info->fake_ra_given = 0 ;
  args_info->fake_dec_given = 0 ;
  args_info->fake_iota_given = 0 ;
  args_info->fake_psi_given = 0 ;
  args_info->fake_phi_given = 0 ;
  args_info->fake_spindown_given = 0 ;
  args_info->fake_strain_given = 0 ;
  args_info->fake_freq_given = 0 ;
  args_info->snr_precision_given = 0 ;
  args_info->max_candidates_given = 0 ;
  args_info->min_candidate_snr_given = 0 ;
  args_info->output_initial_given = 0 ;
  args_info->output_optimized_given = 0 ;
  args_info->output_cache_given = 0 ;
  args_info->extended_test_given = 0 ;
  args_info->max_sft_report_given = 0 ;
  args_info->num_threads_given = 0 ;
  args_info->niota_given = 0 ;
  args_info->npsi_given = 0 ;
  args_info->nfshift_given = 0 ;
  args_info->nchunks_given = 0 ;
  args_info->weight_cutoff_fraction_given = 0 ;
  args_info->per_dataset_weight_cutoff_fraction_given = 0 ;
  args_info->power_max_median_factor_given = 0 ;
  args_info->tmedian_noise_level_given = 0 ;
  args_info->summing_step_given = 0 ;
  args_info->max_first_shift_given = 0 ;
  args_info->statistics_function_given = 0 ;
  args_info->dump_power_sums_given = 0 ;
  args_info->compute_skymaps_given = 0 ;
  args_info->fine_grid_skymarks_given = 0 ;
  args_info->half_window_given = 0 ;
  args_info->tail_veto_given = 0 ;
  args_info->cache_granularity_given = 0 ;
  args_info->injection_group_counter = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  args_info->config_arg = NULL;
  args_info->config_orig = NULL;
  args_info->label_arg = gengetopt_strdup ("");
  args_info->label_orig = NULL;
  args_info->sky_grid_arg = gengetopt_strdup ("sin_theta");
  args_info->sky_grid_orig = NULL;
  args_info->skymap_orientation_arg = gengetopt_strdup ("equatorial");
  args_info->skymap_orientation_orig = NULL;
  args_info->skyband_method_arg = gengetopt_strdup ("S");
  args_info->skyband_method_orig = NULL;
  args_info->nskybands_arg = 11;
  args_info->nskybands_orig = NULL;
  args_info->large_S_orig = NULL;
  args_info->band_axis_arg = gengetopt_strdup ("auto");
  args_info->band_axis_orig = NULL;
  args_info->band_axis_norm_orig = NULL;
  args_info->sky_marks_file_arg = NULL;
  args_info->sky_marks_file_orig = NULL;
  args_info->fine_factor_arg = 5;
  args_info->fine_factor_orig = NULL;
  args_info->skymap_resolution_orig = NULL;
  args_info->skymap_resolution_ratio_arg = 1.0;
  args_info->skymap_resolution_ratio_orig = NULL;
  args_info->small_weight_ratio_arg = 0.2;
  args_info->small_weight_ratio_orig = NULL;
  args_info->strain_norm_factor_arg = 1e-20;
  args_info->strain_norm_factor_orig = NULL;
  args_info->lock_file_arg = NULL;
  args_info->lock_file_orig = NULL;
  args_info->enable_dataset_locking_arg = 1;
  args_info->enable_dataset_locking_orig = NULL;
  args_info->retry_delay_arg = 2;
  args_info->retry_delay_orig = NULL;
  args_info->lock_retry_delay_arg = 10;
  args_info->lock_retry_delay_orig = NULL;
  args_info->dataset_arg = NULL;
  args_info->dataset_orig = NULL;
  args_info->initial_dataset_seed_arg = 12345;
  args_info->initial_dataset_seed_orig = NULL;
  args_info->input_format_arg = gengetopt_strdup ("GEO");
  args_info->input_format_orig = NULL;
  args_info->dump_data_arg = NULL;
  args_info->dump_data_orig = NULL;
  args_info->dump_sftv2_arg = NULL;
  args_info->dump_sftv2_orig = NULL;
  args_info->output_arg = NULL;
  args_info->output_orig = NULL;
  args_info->ephemeris_path_arg = NULL;
  args_info->ephemeris_path_orig = NULL;
  args_info->earth_ephemeris_arg = NULL;
  args_info->earth_ephemeris_orig = NULL;
  args_info->sun_ephemeris_arg = NULL;
  args_info->sun_ephemeris_orig = NULL;
  args_info->first_bin_orig = NULL;
  args_info->nbins_arg = 501;
  args_info->nbins_orig = NULL;
  args_info->side_cut_orig = NULL;
  args_info->expected_timebase_arg = 6;
  args_info->expected_timebase_orig = NULL;
  args_info->hist_bins_arg = 200;
  args_info->hist_bins_orig = NULL;
  args_info->detector_arg = NULL;
  args_info->detector_orig = NULL;
  args_info->doppler_multiplier_arg = 1.0;
  args_info->doppler_multiplier_orig = NULL;
  args_info->spindown_start_time_orig = NULL;
  args_info->frequency_offset_arg = 0.0;
  args_info->frequency_offset_orig = NULL;
  args_info->spindown_start_arg = 0.0;
  args_info->spindown_start_orig = NULL;
  args_info->spindown_step_arg = 5e-10;
  args_info->spindown_step_orig = NULL;
  args_info->spindown_count_arg = 1;
  args_info->spindown_count_orig = NULL;
  args_info->fdotdot_arg = 0.0;
  args_info->fdotdot_orig = NULL;
  args_info->orientation_arg = 0;
  args_info->orientation_orig = NULL;
  args_info->nlinear_polarizations_arg = 4;
  args_info->nlinear_polarizations_orig = NULL;
  args_info->no_demodulation_arg = 0;
  args_info->no_demodulation_orig = NULL;
  args_info->no_decomposition_arg = 0;
  args_info->no_decomposition_orig = NULL;
  args_info->no_candidates_arg = 0;
  args_info->no_candidates_orig = NULL;
  args_info->no_am_response_arg = 0;
  args_info->no_am_response_orig = NULL;
  args_info->no_secondary_skymaps_arg = 0;
  args_info->no_secondary_skymaps_orig = NULL;
  args_info->averaging_mode_arg = gengetopt_strdup ("1");
  args_info->averaging_mode_orig = NULL;
  args_info->subtract_background_arg = 0;
  args_info->subtract_background_orig = NULL;
  args_info->do_cutoff_arg = 1;
  args_info->do_cutoff_orig = NULL;
  args_info->filter_lines_arg = 1;
  args_info->filter_lines_orig = NULL;
  args_info->ks_test_arg = 1;
  args_info->ks_test_orig = NULL;
  args_info->compute_betas_arg = 0;
  args_info->compute_betas_orig = NULL;
  args_info->upper_limit_comp_arg = gengetopt_strdup ("Hann");
  args_info->upper_limit_comp_orig = NULL;
  args_info->lower_limit_comp_arg = gengetopt_strdup ("Hann");
  args_info->lower_limit_comp_orig = NULL;
  args_info->write_dat_arg = gengetopt_strdup (".*");
  args_info->write_dat_orig = NULL;
  args_info->write_png_arg = gengetopt_strdup (".*");
  args_info->write_png_orig = NULL;
  args_info->dump_points_arg = 0;
  args_info->dump_points_orig = NULL;
  args_info->dump_candidates_arg = 0;
  args_info->dump_candidates_orig = NULL;
  args_info->focus_ra_orig = NULL;
  args_info->focus_dec_orig = NULL;
  args_info->focus_radius_orig = NULL;
  args_info->only_large_cos_orig = NULL;
  args_info->fake_ref_time_arg = 0;
  args_info->fake_ref_time_orig = NULL;
  args_info->fake_ra_arg = 3.14;
  args_info->fake_ra_orig = NULL;
  args_info->fake_dec_arg = 0.0;
  args_info->fake_dec_orig = NULL;
  args_info->fake_iota_arg = 0.0;
  args_info->fake_iota_orig = NULL;
  args_info->fake_psi_arg = 0.0;
  args_info->fake_psi_orig = NULL;
  args_info->fake_phi_arg = 0.0;
  args_info->fake_phi_orig = NULL;
  args_info->fake_spindown_arg = 0.0;
  args_info->fake_spindown_orig = NULL;
  args_info->fake_strain_arg = 1e-23;
  args_info->fake_strain_orig = NULL;
  args_info->fake_freq_orig = NULL;
  args_info->snr_precision_arg = 0.2;
  args_info->snr_precision_orig = NULL;
  args_info->max_candidates_arg = -1;
  args_info->max_candidates_orig = NULL;
  args_info->min_candidate_snr_arg = 5.0;
  args_info->min_candidate_snr_orig = NULL;
  args_info->output_initial_arg = 0;
  args_info->output_initial_orig = NULL;
  args_info->output_optimized_arg = 0;
  args_info->output_optimized_orig = NULL;
  args_info->output_cache_arg = 0;
  args_info->output_cache_orig = NULL;
  args_info->extended_test_arg = 0;
  args_info->extended_test_orig = NULL;
  args_info->max_sft_report_arg = 100;
  args_info->max_sft_report_orig = NULL;
  args_info->num_threads_arg = -1;
  args_info->num_threads_orig = NULL;
  args_info->niota_arg = 3;
  args_info->niota_orig = NULL;
  args_info->npsi_arg = 6;
  args_info->npsi_orig = NULL;
  args_info->nfshift_arg = 2;
  args_info->nfshift_orig = NULL;
  args_info->nchunks_arg = 5;
  args_info->nchunks_orig = NULL;
  args_info->weight_cutoff_fraction_arg = 0.04;
  args_info->weight_cutoff_fraction_orig = NULL;
  args_info->per_dataset_weight_cutoff_fraction_arg = 0.04;
  args_info->per_dataset_weight_cutoff_fraction_orig = NULL;
  args_info->power_max_median_factor_arg = 0.1;
  args_info->power_max_median_factor_orig = NULL;
  args_info->tmedian_noise_level_arg = 1;
  args_info->tmedian_noise_level_orig = NULL;
  args_info->summing_step_arg = 864000;
  args_info->summing_step_orig = NULL;
  args_info->max_first_shift_arg = 10;
  args_info->max_first_shift_orig = NULL;
  args_info->statistics_function_arg = gengetopt_strdup ("linear");
  args_info->statistics_function_orig = NULL;
  args_info->dump_power_sums_arg = 0;
  args_info->dump_power_sums_orig = NULL;
  args_info->compute_skymaps_arg = 0;
  args_info->compute_skymaps_orig = NULL;
  args_info->fine_grid_skymarks_arg = 0;
  args_info->fine_grid_skymarks_orig = NULL;
  args_info->half_window_arg = 20;
  args_info->half_window_orig = NULL;
  args_info->tail_veto_arg = 10;
  args_info->tail_veto_orig = NULL;
  args_info->cache_granularity_arg = -1;
  args_info->cache_granularity_orig = NULL;
  
}

static
void init_args_info(struct gengetopt_args_info *args_info)
{


  args_info->help_help = gengetopt_args_info_help[0] ;
  args_info->version_help = gengetopt_args_info_help[1] ;
  args_info->config_help = gengetopt_args_info_help[2] ;
  args_info->config_min = -1;
  args_info->config_max = -1;
  args_info->label_help = gengetopt_args_info_help[3] ;
  args_info->sky_grid_help = gengetopt_args_info_help[4] ;
  args_info->skymap_orientation_help = gengetopt_args_info_help[5] ;
  args_info->skyband_method_help = gengetopt_args_info_help[6] ;
  args_info->nskybands_help = gengetopt_args_info_help[7] ;
  args_info->large_S_help = gengetopt_args_info_help[8] ;
  args_info->band_axis_help = gengetopt_args_info_help[9] ;
  args_info->band_axis_norm_help = gengetopt_args_info_help[10] ;
  args_info->sky_marks_file_help = gengetopt_args_info_help[11] ;
  args_info->fine_factor_help = gengetopt_args_info_help[12] ;
  args_info->skymap_resolution_help = gengetopt_args_info_help[13] ;
  args_info->skymap_resolution_ratio_help = gengetopt_args_info_help[14] ;
  args_info->small_weight_ratio_help = gengetopt_args_info_help[15] ;
  args_info->strain_norm_factor_help = gengetopt_args_info_help[16] ;
  args_info->lock_file_help = gengetopt_args_info_help[17] ;
  args_info->enable_dataset_locking_help = gengetopt_args_info_help[18] ;
  args_info->retry_delay_help = gengetopt_args_info_help[19] ;
  args_info->lock_retry_delay_help = gengetopt_args_info_help[20] ;
  args_info->dataset_help = gengetopt_args_info_help[21] ;
  args_info->initial_dataset_seed_help = gengetopt_args_info_help[22] ;
  args_info->input_format_help = gengetopt_args_info_help[23] ;
  args_info->dump_data_help = gengetopt_args_info_help[24] ;
  args_info->dump_sftv2_help = gengetopt_args_info_help[25] ;
  args_info->output_help = gengetopt_args_info_help[26] ;
  args_info->ephemeris_path_help = gengetopt_args_info_help[27] ;
  args_info->earth_ephemeris_help = gengetopt_args_info_help[28] ;
  args_info->sun_ephemeris_help = gengetopt_args_info_help[29] ;
  args_info->first_bin_help = gengetopt_args_info_help[30] ;
  args_info->nbins_help = gengetopt_args_info_help[31] ;
  args_info->side_cut_help = gengetopt_args_info_help[32] ;
  args_info->expected_timebase_help = gengetopt_args_info_help[33] ;
  args_info->hist_bins_help = gengetopt_args_info_help[34] ;
  args_info->detector_help = gengetopt_args_info_help[35] ;
  args_info->doppler_multiplier_help = gengetopt_args_info_help[36] ;
  args_info->spindown_start_time_help = gengetopt_args_info_help[37] ;
  args_info->frequency_offset_help = gengetopt_args_info_help[38] ;
  args_info->spindown_start_help = gengetopt_args_info_help[39] ;
  args_info->spindown_step_help = gengetopt_args_info_help[40] ;
  args_info->spindown_count_help = gengetopt_args_info_help[41] ;
  args_info->fdotdot_help = gengetopt_args_info_help[42] ;
  args_info->orientation_help = gengetopt_args_info_help[43] ;
  args_info->nlinear_polarizations_help = gengetopt_args_info_help[44] ;
  args_info->no_demodulation_help = gengetopt_args_info_help[45] ;
  args_info->no_decomposition_help = gengetopt_args_info_help[46] ;
  args_info->no_candidates_help = gengetopt_args_info_help[47] ;
  args_info->no_am_response_help = gengetopt_args_info_help[48] ;
  args_info->no_secondary_skymaps_help = gengetopt_args_info_help[49] ;
  args_info->averaging_mode_help = gengetopt_args_info_help[50] ;
  args_info->subtract_background_help = gengetopt_args_info_help[51] ;
  args_info->do_cutoff_help = gengetopt_args_info_help[52] ;
  args_info->filter_lines_help = gengetopt_args_info_help[53] ;
  args_info->ks_test_help = gengetopt_args_info_help[54] ;
  args_info->compute_betas_help = gengetopt_args_info_help[55] ;
  args_info->upper_limit_comp_help = gengetopt_args_info_help[56] ;
  args_info->lower_limit_comp_help = gengetopt_args_info_help[57] ;
  args_info->write_dat_help = gengetopt_args_info_help[58] ;
  args_info->write_png_help = gengetopt_args_info_help[59] ;
  args_info->dump_points_help = gengetopt_args_info_help[60] ;
  args_info->dump_candidates_help = gengetopt_args_info_help[61] ;
  args_info->focus_ra_help = gengetopt_args_info_help[62] ;
  args_info->focus_dec_help = gengetopt_args_info_help[63] ;
  args_info->focus_radius_help = gengetopt_args_info_help[64] ;
  args_info->only_large_cos_help = gengetopt_args_info_help[65] ;
  args_info->fake_linear_help = gengetopt_args_info_help[67] ;
  args_info->fake_circular_help = gengetopt_args_info_help[68] ;
  args_info->fake_ref_time_help = gengetopt_args_info_help[69] ;
  args_info->fake_ra_help = gengetopt_args_info_help[70] ;
  args_info->fake_dec_help = gengetopt_args_info_help[71] ;
  args_info->fake_iota_help = gengetopt_args_info_help[72] ;
  args_info->fake_psi_help = gengetopt_args_info_help[73] ;
  args_info->fake_phi_help = gengetopt_args_info_help[74] ;
  args_info->fake_spindown_help = gengetopt_args_info_help[75] ;
  args_info->fake_strain_help = gengetopt_args_info_help[76] ;
  args_info->fake_freq_help = gengetopt_args_info_help[77] ;
  args_info->snr_precision_help = gengetopt_args_info_help[78] ;
  args_info->max_candidates_help = gengetopt_args_info_help[79] ;
  args_info->min_candidate_snr_help = gengetopt_args_info_help[80] ;
  args_info->output_initial_help = gengetopt_args_info_help[81] ;
  args_info->output_optimized_help = gengetopt_args_info_help[82] ;
  args_info->output_cache_help = gengetopt_args_info_help[83] ;
  args_info->extended_test_help = gengetopt_args_info_help[84] ;
  args_info->max_sft_report_help = gengetopt_args_info_help[85] ;
  args_info->num_threads_help = gengetopt_args_info_help[86] ;
  args_info->niota_help = gengetopt_args_info_help[87] ;
  args_info->npsi_help = gengetopt_args_info_help[88] ;
  args_info->nfshift_help = gengetopt_args_info_help[89] ;
  args_info->nchunks_help = gengetopt_args_info_help[90] ;
  args_info->weight_cutoff_fraction_help = gengetopt_args_info_help[91] ;
  args_info->per_dataset_weight_cutoff_fraction_help = gengetopt_args_info_help[92] ;
  args_info->power_max_median_factor_help = gengetopt_args_info_help[93] ;
  args_info->tmedian_noise_level_help = gengetopt_args_info_help[94] ;
  args_info->summing_step_help = gengetopt_args_info_help[95] ;
  args_info->max_first_shift_help = gengetopt_args_info_help[96] ;
  args_info->statistics_function_help = gengetopt_args_info_help[97] ;
  args_info->dump_power_sums_help = gengetopt_args_info_help[98] ;
  args_info->compute_skymaps_help = gengetopt_args_info_help[99] ;
  args_info->fine_grid_skymarks_help = gengetopt_args_info_help[100] ;
  args_info->half_window_help = gengetopt_args_info_help[101] ;
  args_info->tail_veto_help = gengetopt_args_info_help[102] ;
  args_info->cache_granularity_help = gengetopt_args_info_help[103] ;
  
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n", CMDLINE_PARSER_PACKAGE, CMDLINE_PARSER_VERSION);
}

static void print_help_common(void) {
  cmdline_parser_print_version ();

  if (strlen(gengetopt_args_info_purpose) > 0)
    printf("\n%s\n", gengetopt_args_info_purpose);

  if (strlen(gengetopt_args_info_usage) > 0)
    printf("\n%s\n", gengetopt_args_info_usage);

  printf("\n");

  if (strlen(gengetopt_args_info_description) > 0)
    printf("%s\n", gengetopt_args_info_description);
}

void
cmdline_parser_print_help (void)
{
  int i = 0;
  print_help_common();
  while (gengetopt_args_info_help[i])
    printf("%s\n", gengetopt_args_info_help[i++]);
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
  init_args_info (args_info);
}

void
cmdline_parser_params_init(struct cmdline_parser_params *params)
{
  if (params)
    { 
      params->override = 0;
      params->initialize = 1;
      params->check_required = 1;
      params->check_ambiguity = 0;
      params->print_errors = 1;
    }
}

struct cmdline_parser_params *
cmdline_parser_params_create(void)
{
  struct cmdline_parser_params *params = 
    (struct cmdline_parser_params *)malloc(sizeof(struct cmdline_parser_params));
  cmdline_parser_params_init(params);  
  return params;
}

static void
free_string_field (char **s)
{
  if (*s)
    {
      free (*s);
      *s = 0;
    }
}

/** @brief generic value variable */
union generic_value {
    int int_arg;
    double double_arg;
    char *string_arg;
};

/** @brief holds temporary values for multiple options */
struct generic_list
{
  union generic_value arg;
  char *orig;
  struct generic_list *next;
};

/**
 * @brief add a node at the head of the list 
 */
static void add_node(struct generic_list **list) {
  struct generic_list *new_node = (struct generic_list *) malloc (sizeof (struct generic_list));
  new_node->next = *list;
  *list = new_node;
  new_node->arg.string_arg = NULL;
  new_node->orig = NULL;
}


static void
free_multiple_string_field(unsigned int len, char ***arg, char ***orig)
{
  unsigned int i;
  if (*arg) {
    for (i = 0; i < len; ++i)
      {
        free_string_field(&((*arg)[i]));
        free_string_field(&((*orig)[i]));
      }
    free_string_field(&((*arg)[0])); /* free default string */

    free (*arg);
    *arg = 0;
    free (*orig);
    *orig = 0;
  }
}

static void
cmdline_parser_release (struct gengetopt_args_info *args_info)
{

  free_multiple_string_field (args_info->config_given, &(args_info->config_arg), &(args_info->config_orig));
  free_string_field (&(args_info->label_arg));
  free_string_field (&(args_info->label_orig));
  free_string_field (&(args_info->sky_grid_arg));
  free_string_field (&(args_info->sky_grid_orig));
  free_string_field (&(args_info->skymap_orientation_arg));
  free_string_field (&(args_info->skymap_orientation_orig));
  free_string_field (&(args_info->skyband_method_arg));
  free_string_field (&(args_info->skyband_method_orig));
  free_string_field (&(args_info->nskybands_orig));
  free_string_field (&(args_info->large_S_orig));
  free_string_field (&(args_info->band_axis_arg));
  free_string_field (&(args_info->band_axis_orig));
  free_string_field (&(args_info->band_axis_norm_orig));
  free_string_field (&(args_info->sky_marks_file_arg));
  free_string_field (&(args_info->sky_marks_file_orig));
  free_string_field (&(args_info->fine_factor_orig));
  free_string_field (&(args_info->skymap_resolution_orig));
  free_string_field (&(args_info->skymap_resolution_ratio_orig));
  free_string_field (&(args_info->small_weight_ratio_orig));
  free_string_field (&(args_info->strain_norm_factor_orig));
  free_string_field (&(args_info->lock_file_arg));
  free_string_field (&(args_info->lock_file_orig));
  free_string_field (&(args_info->enable_dataset_locking_orig));
  free_string_field (&(args_info->retry_delay_orig));
  free_string_field (&(args_info->lock_retry_delay_orig));
  free_string_field (&(args_info->dataset_arg));
  free_string_field (&(args_info->dataset_orig));
  free_string_field (&(args_info->initial_dataset_seed_orig));
  free_string_field (&(args_info->input_format_arg));
  free_string_field (&(args_info->input_format_orig));
  free_string_field (&(args_info->dump_data_arg));
  free_string_field (&(args_info->dump_data_orig));
  free_string_field (&(args_info->dump_sftv2_arg));
  free_string_field (&(args_info->dump_sftv2_orig));
  free_string_field (&(args_info->output_arg));
  free_string_field (&(args_info->output_orig));
  free_string_field (&(args_info->ephemeris_path_arg));
  free_string_field (&(args_info->ephemeris_path_orig));
  free_string_field (&(args_info->earth_ephemeris_arg));
  free_string_field (&(args_info->earth_ephemeris_orig));
  free_string_field (&(args_info->sun_ephemeris_arg));
  free_string_field (&(args_info->sun_ephemeris_orig));
  free_string_field (&(args_info->first_bin_orig));
  free_string_field (&(args_info->nbins_orig));
  free_string_field (&(args_info->side_cut_orig));
  free_string_field (&(args_info->expected_timebase_orig));
  free_string_field (&(args_info->hist_bins_orig));
  free_string_field (&(args_info->detector_arg));
  free_string_field (&(args_info->detector_orig));
  free_string_field (&(args_info->doppler_multiplier_orig));
  free_string_field (&(args_info->spindown_start_time_orig));
  free_string_field (&(args_info->frequency_offset_orig));
  free_string_field (&(args_info->spindown_start_orig));
  free_string_field (&(args_info->spindown_step_orig));
  free_string_field (&(args_info->spindown_count_orig));
  free_string_field (&(args_info->fdotdot_orig));
  free_string_field (&(args_info->orientation_orig));
  free_string_field (&(args_info->nlinear_polarizations_orig));
  free_string_field (&(args_info->no_demodulation_orig));
  free_string_field (&(args_info->no_decomposition_orig));
  free_string_field (&(args_info->no_candidates_orig));
  free_string_field (&(args_info->no_am_response_orig));
  free_string_field (&(args_info->no_secondary_skymaps_orig));
  free_string_field (&(args_info->averaging_mode_arg));
  free_string_field (&(args_info->averaging_mode_orig));
  free_string_field (&(args_info->subtract_background_orig));
  free_string_field (&(args_info->do_cutoff_orig));
  free_string_field (&(args_info->filter_lines_orig));
  free_string_field (&(args_info->ks_test_orig));
  free_string_field (&(args_info->compute_betas_orig));
  free_string_field (&(args_info->upper_limit_comp_arg));
  free_string_field (&(args_info->upper_limit_comp_orig));
  free_string_field (&(args_info->lower_limit_comp_arg));
  free_string_field (&(args_info->lower_limit_comp_orig));
  free_string_field (&(args_info->write_dat_arg));
  free_string_field (&(args_info->write_dat_orig));
  free_string_field (&(args_info->write_png_arg));
  free_string_field (&(args_info->write_png_orig));
  free_string_field (&(args_info->dump_points_orig));
  free_string_field (&(args_info->dump_candidates_orig));
  free_string_field (&(args_info->focus_ra_orig));
  free_string_field (&(args_info->focus_dec_orig));
  free_string_field (&(args_info->focus_radius_orig));
  free_string_field (&(args_info->only_large_cos_orig));
  free_string_field (&(args_info->fake_ref_time_orig));
  free_string_field (&(args_info->fake_ra_orig));
  free_string_field (&(args_info->fake_dec_orig));
  free_string_field (&(args_info->fake_iota_orig));
  free_string_field (&(args_info->fake_psi_orig));
  free_string_field (&(args_info->fake_phi_orig));
  free_string_field (&(args_info->fake_spindown_orig));
  free_string_field (&(args_info->fake_strain_orig));
  free_string_field (&(args_info->fake_freq_orig));
  free_string_field (&(args_info->snr_precision_orig));
  free_string_field (&(args_info->max_candidates_orig));
  free_string_field (&(args_info->min_candidate_snr_orig));
  free_string_field (&(args_info->output_initial_orig));
  free_string_field (&(args_info->output_optimized_orig));
  free_string_field (&(args_info->output_cache_orig));
  free_string_field (&(args_info->extended_test_orig));
  free_string_field (&(args_info->max_sft_report_orig));
  free_string_field (&(args_info->num_threads_orig));
  free_string_field (&(args_info->niota_orig));
  free_string_field (&(args_info->npsi_orig));
  free_string_field (&(args_info->nfshift_orig));
  free_string_field (&(args_info->nchunks_orig));
  free_string_field (&(args_info->weight_cutoff_fraction_orig));
  free_string_field (&(args_info->per_dataset_weight_cutoff_fraction_orig));
  free_string_field (&(args_info->power_max_median_factor_orig));
  free_string_field (&(args_info->tmedian_noise_level_orig));
  free_string_field (&(args_info->summing_step_orig));
  free_string_field (&(args_info->max_first_shift_orig));
  free_string_field (&(args_info->statistics_function_arg));
  free_string_field (&(args_info->statistics_function_orig));
  free_string_field (&(args_info->dump_power_sums_orig));
  free_string_field (&(args_info->compute_skymaps_orig));
  free_string_field (&(args_info->fine_grid_skymarks_orig));
  free_string_field (&(args_info->half_window_orig));
  free_string_field (&(args_info->tail_veto_orig));
  free_string_field (&(args_info->cache_granularity_orig));
  
  

  clear_given (args_info);
}


static void
write_into_file(FILE *outfile, const char *opt, const char *arg, char *values[])
{
  if (arg) {
    fprintf(outfile, "%s=\"%s\"\n", opt, arg);
  } else {
    fprintf(outfile, "%s\n", opt);
  }
}

static void
write_multiple_into_file(FILE *outfile, int len, const char *opt, char **arg, char *values[])
{
  int i;
  
  for (i = 0; i < len; ++i)
    write_into_file(outfile, opt, (arg ? arg[i] : 0), values);
}

int
cmdline_parser_dump(FILE *outfile, struct gengetopt_args_info *args_info)
{
  int i = 0;

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot dump options to stream\n", CMDLINE_PARSER_PACKAGE);
      return EXIT_FAILURE;
    }

  if (args_info->help_given)
    write_into_file(outfile, "help", 0, 0 );
  if (args_info->version_given)
    write_into_file(outfile, "version", 0, 0 );
  write_multiple_into_file(outfile, args_info->config_given, "config", args_info->config_orig, 0);
  if (args_info->label_given)
    write_into_file(outfile, "label", args_info->label_orig, 0);
  if (args_info->sky_grid_given)
    write_into_file(outfile, "sky-grid", args_info->sky_grid_orig, 0);
  if (args_info->skymap_orientation_given)
    write_into_file(outfile, "skymap-orientation", args_info->skymap_orientation_orig, 0);
  if (args_info->skyband_method_given)
    write_into_file(outfile, "skyband-method", args_info->skyband_method_orig, 0);
  if (args_info->nskybands_given)
    write_into_file(outfile, "nskybands", args_info->nskybands_orig, 0);
  if (args_info->large_S_given)
    write_into_file(outfile, "large-S", args_info->large_S_orig, 0);
  if (args_info->band_axis_given)
    write_into_file(outfile, "band-axis", args_info->band_axis_orig, 0);
  if (args_info->band_axis_norm_given)
    write_into_file(outfile, "band-axis-norm", args_info->band_axis_norm_orig, 0);
  if (args_info->sky_marks_file_given)
    write_into_file(outfile, "sky-marks-file", args_info->sky_marks_file_orig, 0);
  if (args_info->fine_factor_given)
    write_into_file(outfile, "fine-factor", args_info->fine_factor_orig, 0);
  if (args_info->skymap_resolution_given)
    write_into_file(outfile, "skymap-resolution", args_info->skymap_resolution_orig, 0);
  if (args_info->skymap_resolution_ratio_given)
    write_into_file(outfile, "skymap-resolution-ratio", args_info->skymap_resolution_ratio_orig, 0);
  if (args_info->small_weight_ratio_given)
    write_into_file(outfile, "small-weight-ratio", args_info->small_weight_ratio_orig, 0);
  if (args_info->strain_norm_factor_given)
    write_into_file(outfile, "strain-norm-factor", args_info->strain_norm_factor_orig, 0);
  if (args_info->lock_file_given)
    write_into_file(outfile, "lock-file", args_info->lock_file_orig, 0);
  if (args_info->enable_dataset_locking_given)
    write_into_file(outfile, "enable-dataset-locking", args_info->enable_dataset_locking_orig, 0);
  if (args_info->retry_delay_given)
    write_into_file(outfile, "retry-delay", args_info->retry_delay_orig, 0);
  if (args_info->lock_retry_delay_given)
    write_into_file(outfile, "lock-retry-delay", args_info->lock_retry_delay_orig, 0);
  if (args_info->dataset_given)
    write_into_file(outfile, "dataset", args_info->dataset_orig, 0);
  if (args_info->initial_dataset_seed_given)
    write_into_file(outfile, "initial-dataset-seed", args_info->initial_dataset_seed_orig, 0);
  if (args_info->input_format_given)
    write_into_file(outfile, "input-format", args_info->input_format_orig, 0);
  if (args_info->dump_data_given)
    write_into_file(outfile, "dump-data", args_info->dump_data_orig, 0);
  if (args_info->dump_sftv2_given)
    write_into_file(outfile, "dump-sftv2", args_info->dump_sftv2_orig, 0);
  if (args_info->output_given)
    write_into_file(outfile, "output", args_info->output_orig, 0);
  if (args_info->ephemeris_path_given)
    write_into_file(outfile, "ephemeris-path", args_info->ephemeris_path_orig, 0);
  if (args_info->earth_ephemeris_given)
    write_into_file(outfile, "earth-ephemeris", args_info->earth_ephemeris_orig, 0);
  if (args_info->sun_ephemeris_given)
    write_into_file(outfile, "sun-ephemeris", args_info->sun_ephemeris_orig, 0);
  if (args_info->first_bin_given)
    write_into_file(outfile, "first-bin", args_info->first_bin_orig, 0);
  if (args_info->nbins_given)
    write_into_file(outfile, "nbins", args_info->nbins_orig, 0);
  if (args_info->side_cut_given)
    write_into_file(outfile, "side-cut", args_info->side_cut_orig, 0);
  if (args_info->expected_timebase_given)
    write_into_file(outfile, "expected-timebase", args_info->expected_timebase_orig, 0);
  if (args_info->hist_bins_given)
    write_into_file(outfile, "hist-bins", args_info->hist_bins_orig, 0);
  if (args_info->detector_given)
    write_into_file(outfile, "detector", args_info->detector_orig, 0);
  if (args_info->doppler_multiplier_given)
    write_into_file(outfile, "doppler-multiplier", args_info->doppler_multiplier_orig, 0);
  if (args_info->spindown_start_time_given)
    write_into_file(outfile, "spindown-start-time", args_info->spindown_start_time_orig, 0);
  if (args_info->frequency_offset_given)
    write_into_file(outfile, "frequency-offset", args_info->frequency_offset_orig, 0);
  if (args_info->spindown_start_given)
    write_into_file(outfile, "spindown-start", args_info->spindown_start_orig, 0);
  if (args_info->spindown_step_given)
    write_into_file(outfile, "spindown-step", args_info->spindown_step_orig, 0);
  if (args_info->spindown_count_given)
    write_into_file(outfile, "spindown-count", args_info->spindown_count_orig, 0);
  if (args_info->fdotdot_given)
    write_into_file(outfile, "fdotdot", args_info->fdotdot_orig, 0);
  if (args_info->orientation_given)
    write_into_file(outfile, "orientation", args_info->orientation_orig, 0);
  if (args_info->nlinear_polarizations_given)
    write_into_file(outfile, "nlinear-polarizations", args_info->nlinear_polarizations_orig, 0);
  if (args_info->no_demodulation_given)
    write_into_file(outfile, "no-demodulation", args_info->no_demodulation_orig, 0);
  if (args_info->no_decomposition_given)
    write_into_file(outfile, "no-decomposition", args_info->no_decomposition_orig, 0);
  if (args_info->no_candidates_given)
    write_into_file(outfile, "no-candidates", args_info->no_candidates_orig, 0);
  if (args_info->no_am_response_given)
    write_into_file(outfile, "no-am-response", args_info->no_am_response_orig, 0);
  if (args_info->no_secondary_skymaps_given)
    write_into_file(outfile, "no-secondary-skymaps", args_info->no_secondary_skymaps_orig, 0);
  if (args_info->averaging_mode_given)
    write_into_file(outfile, "averaging-mode", args_info->averaging_mode_orig, 0);
  if (args_info->subtract_background_given)
    write_into_file(outfile, "subtract-background", args_info->subtract_background_orig, 0);
  if (args_info->do_cutoff_given)
    write_into_file(outfile, "do-cutoff", args_info->do_cutoff_orig, 0);
  if (args_info->filter_lines_given)
    write_into_file(outfile, "filter-lines", args_info->filter_lines_orig, 0);
  if (args_info->ks_test_given)
    write_into_file(outfile, "ks-test", args_info->ks_test_orig, 0);
  if (args_info->compute_betas_given)
    write_into_file(outfile, "compute-betas", args_info->compute_betas_orig, 0);
  if (args_info->upper_limit_comp_given)
    write_into_file(outfile, "upper-limit-comp", args_info->upper_limit_comp_orig, 0);
  if (args_info->lower_limit_comp_given)
    write_into_file(outfile, "lower-limit-comp", args_info->lower_limit_comp_orig, 0);
  if (args_info->write_dat_given)
    write_into_file(outfile, "write-dat", args_info->write_dat_orig, 0);
  if (args_info->write_png_given)
    write_into_file(outfile, "write-png", args_info->write_png_orig, 0);
  if (args_info->dump_points_given)
    write_into_file(outfile, "dump-points", args_info->dump_points_orig, 0);
  if (args_info->dump_candidates_given)
    write_into_file(outfile, "dump-candidates", args_info->dump_candidates_orig, 0);
  if (args_info->focus_ra_given)
    write_into_file(outfile, "focus-ra", args_info->focus_ra_orig, 0);
  if (args_info->focus_dec_given)
    write_into_file(outfile, "focus-dec", args_info->focus_dec_orig, 0);
  if (args_info->focus_radius_given)
    write_into_file(outfile, "focus-radius", args_info->focus_radius_orig, 0);
  if (args_info->only_large_cos_given)
    write_into_file(outfile, "only-large-cos", args_info->only_large_cos_orig, 0);
  if (args_info->fake_linear_given)
    write_into_file(outfile, "fake-linear", 0, 0 );
  if (args_info->fake_circular_given)
    write_into_file(outfile, "fake-circular", 0, 0 );
  if (args_info->fake_ref_time_given)
    write_into_file(outfile, "fake-ref-time", args_info->fake_ref_time_orig, 0);
  if (args_info->fake_ra_given)
    write_into_file(outfile, "fake-ra", args_info->fake_ra_orig, 0);
  if (args_info->fake_dec_given)
    write_into_file(outfile, "fake-dec", args_info->fake_dec_orig, 0);
  if (args_info->fake_iota_given)
    write_into_file(outfile, "fake-iota", args_info->fake_iota_orig, 0);
  if (args_info->fake_psi_given)
    write_into_file(outfile, "fake-psi", args_info->fake_psi_orig, 0);
  if (args_info->fake_phi_given)
    write_into_file(outfile, "fake-phi", args_info->fake_phi_orig, 0);
  if (args_info->fake_spindown_given)
    write_into_file(outfile, "fake-spindown", args_info->fake_spindown_orig, 0);
  if (args_info->fake_strain_given)
    write_into_file(outfile, "fake-strain", args_info->fake_strain_orig, 0);
  if (args_info->fake_freq_given)
    write_into_file(outfile, "fake-freq", args_info->fake_freq_orig, 0);
  if (args_info->snr_precision_given)
    write_into_file(outfile, "snr-precision", args_info->snr_precision_orig, 0);
  if (args_info->max_candidates_given)
    write_into_file(outfile, "max-candidates", args_info->max_candidates_orig, 0);
  if (args_info->min_candidate_snr_given)
    write_into_file(outfile, "min-candidate-snr", args_info->min_candidate_snr_orig, 0);
  if (args_info->output_initial_given)
    write_into_file(outfile, "output-initial", args_info->output_initial_orig, 0);
  if (args_info->output_optimized_given)
    write_into_file(outfile, "output-optimized", args_info->output_optimized_orig, 0);
  if (args_info->output_cache_given)
    write_into_file(outfile, "output-cache", args_info->output_cache_orig, 0);
  if (args_info->extended_test_given)
    write_into_file(outfile, "extended-test", args_info->extended_test_orig, 0);
  if (args_info->max_sft_report_given)
    write_into_file(outfile, "max-sft-report", args_info->max_sft_report_orig, 0);
  if (args_info->num_threads_given)
    write_into_file(outfile, "num-threads", args_info->num_threads_orig, 0);
  if (args_info->niota_given)
    write_into_file(outfile, "niota", args_info->niota_orig, 0);
  if (args_info->npsi_given)
    write_into_file(outfile, "npsi", args_info->npsi_orig, 0);
  if (args_info->nfshift_given)
    write_into_file(outfile, "nfshift", args_info->nfshift_orig, 0);
  if (args_info->nchunks_given)
    write_into_file(outfile, "nchunks", args_info->nchunks_orig, 0);
  if (args_info->weight_cutoff_fraction_given)
    write_into_file(outfile, "weight-cutoff-fraction", args_info->weight_cutoff_fraction_orig, 0);
  if (args_info->per_dataset_weight_cutoff_fraction_given)
    write_into_file(outfile, "per-dataset-weight-cutoff-fraction", args_info->per_dataset_weight_cutoff_fraction_orig, 0);
  if (args_info->power_max_median_factor_given)
    write_into_file(outfile, "power-max-median-factor", args_info->power_max_median_factor_orig, 0);
  if (args_info->tmedian_noise_level_given)
    write_into_file(outfile, "tmedian-noise-level", args_info->tmedian_noise_level_orig, 0);
  if (args_info->summing_step_given)
    write_into_file(outfile, "summing-step", args_info->summing_step_orig, 0);
  if (args_info->max_first_shift_given)
    write_into_file(outfile, "max-first-shift", args_info->max_first_shift_orig, 0);
  if (args_info->statistics_function_given)
    write_into_file(outfile, "statistics-function", args_info->statistics_function_orig, 0);
  if (args_info->dump_power_sums_given)
    write_into_file(outfile, "dump-power-sums", args_info->dump_power_sums_orig, 0);
  if (args_info->compute_skymaps_given)
    write_into_file(outfile, "compute-skymaps", args_info->compute_skymaps_orig, 0);
  if (args_info->fine_grid_skymarks_given)
    write_into_file(outfile, "fine-grid-skymarks", args_info->fine_grid_skymarks_orig, 0);
  if (args_info->half_window_given)
    write_into_file(outfile, "half-window", args_info->half_window_orig, 0);
  if (args_info->tail_veto_given)
    write_into_file(outfile, "tail-veto", args_info->tail_veto_orig, 0);
  if (args_info->cache_granularity_given)
    write_into_file(outfile, "cache-granularity", args_info->cache_granularity_orig, 0);
  

  i = EXIT_SUCCESS;
  return i;
}

int
cmdline_parser_file_save(const char *filename, struct gengetopt_args_info *args_info)
{
  FILE *outfile;
  int i = 0;

  outfile = fopen(filename, "w");

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot open file for writing: %s\n", CMDLINE_PARSER_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  i = cmdline_parser_dump(outfile, args_info);
  fclose (outfile);

  return i;
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  cmdline_parser_release (args_info);
}

/** @brief replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = NULL;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

static char *
get_multiple_arg_token(const char *arg)
{
  char *tok, *ret;
  size_t len, num_of_escape, i, j;

  if (!arg)
    return NULL;

  tok = strchr (arg, ',');
  num_of_escape = 0;

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
          ++num_of_escape;
        }
      else
        break;
    }

  if (tok)
    len = (size_t)(tok - arg + 1);
  else
    len = strlen (arg) + 1;

  len -= num_of_escape;

  ret = (char *) malloc (len);

  i = 0;
  j = 0;
  while (arg[i] && (j < len-1))
    {
      if (arg[i] == '\\' && 
	  arg[ i + 1 ] && 
	  arg[ i + 1 ] == ',')
        ++i;

      ret[j++] = arg[i++];
    }

  ret[len-1] = '\0';

  return ret;
}

static char *
get_multiple_arg_token_next(const char *arg)
{
  char *tok;

  if (!arg)
    return NULL;

  tok = strchr (arg, ',');

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
        }
      else
        break;
    }

  if (! tok || strlen(tok) == 1)
    return 0;

  return tok+1;
}

static int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, int min, int max, const char *option_desc);

int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, int min, int max, const char *option_desc)
{
  int error = 0;

  if (option_given && ! (min < 0 && max < 0))
    {
      if (min >= 0 && max >= 0)
        {
          if (min == max)
            {
              /* specific occurrences */
              if (option_given != min)
                {
                  fprintf (stderr, "%s: %s option occurrences must be %d\n",
                    prog_name, option_desc, min);
                  error = 1;
                }
            }
          else if (option_given < min
              || option_given > max)
            {
              /* range occurrences */
              fprintf (stderr, "%s: %s option occurrences must be between %d and %d\n",
                prog_name, option_desc, min, max);
              error = 1;
            }
        }
      else if (min >= 0)
        {
          /* at least check */
          if (option_given < min)
            {
              fprintf (stderr, "%s: %s option occurrences must be at least %d\n",
                prog_name, option_desc, min);
              error = 1;
            }
        }
      else if (max >= 0)
        {
          /* at most check */
          if (option_given > max)
            {
              fprintf (stderr, "%s: %s option occurrences must be at most %d\n",
                prog_name, option_desc, max);
              error = 1;
            }
        }
    }
    
  return error;
}
static void
reset_group_injection(struct gengetopt_args_info *args_info)
{
  if (! args_info->injection_group_counter)
    return;
  
  args_info->fake_linear_given = 0 ;
  args_info->fake_circular_given = 0 ;

  args_info->injection_group_counter = 0;
}

int
cmdline_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser_ext (int argc, char * const *argv, struct gengetopt_args_info *args_info,
                   struct cmdline_parser_params *params)
{
  int result;
  result = cmdline_parser_internal (argc, argv, args_info, params, NULL);

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser2 (int argc, char * const *argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;
  struct cmdline_parser_params params;
  
  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;

  result = cmdline_parser_internal (argc, argv, args_info, &params, NULL);

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, NULL) > 0)
    result = EXIT_FAILURE;

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;

  /* checks for required options */
  if (check_multiple_option_occurrences(prog_name, args_info->config_given, args_info->config_min, args_info->config_max, "'--config' ('-c')"))
     error = 1;
  
  
  /* checks for dependences among options */

  return error;
}


static char *package_name = 0;

/**
 * @brief updates an option
 * @param field the generic pointer to the field to update
 * @param orig_field the pointer to the orig field
 * @param field_given the pointer to the number of occurrence of this option
 * @param prev_given the pointer to the number of occurrence already seen
 * @param value the argument for this option (if null no arg was specified)
 * @param possible_values the possible values for this option (if specified)
 * @param default_value the default value (in case the option only accepts fixed values)
 * @param arg_type the type of this option
 * @param check_ambiguity @see cmdline_parser_params.check_ambiguity
 * @param override @see cmdline_parser_params.override
 * @param no_free whether to free a possible previous value
 * @param multiple_option whether this is a multiple option
 * @param long_opt the corresponding long option
 * @param short_opt the corresponding short option (or '-' if none)
 * @param additional_error possible further error specification
 */
static
int update_arg(void *field, char **orig_field,
               unsigned int *field_given, unsigned int *prev_given, 
               char *value, char *possible_values[], const char *default_value,
               cmdline_parser_arg_type arg_type,
               int check_ambiguity, int override,
               int no_free, int multiple_option,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  char *stop_char = 0;
  const char *val = value;
  int found;
  char **string_field;

  stop_char = 0;
  found = 0;

  if (!multiple_option && prev_given && (*prev_given || (check_ambiguity && *field_given)))
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: `--%s' (`-%c') option given more than once%s\n", 
               package_name, long_opt, short_opt,
               (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: `--%s' option given more than once%s\n", 
               package_name, long_opt,
               (additional_error ? additional_error : ""));
      return 1; /* failure */
    }

    
  if (field_given && *field_given && ! override)
    return 0;
  if (prev_given)
    (*prev_given)++;
  if (field_given)
    (*field_given)++;
  if (possible_values)
    val = possible_values[found];

  switch(arg_type) {
  case ARG_INT:
    if (val) *((int *)field) = strtol (val, &stop_char, 0);
    break;
  case ARG_DOUBLE:
    if (val) *((double *)field) = strtod (val, &stop_char);
    break;
  case ARG_STRING:
    if (val) {
      string_field = (char **)field;
      if (!no_free && *string_field)
        free (*string_field); /* free previous string */
      *string_field = gengetopt_strdup (val);
    }
    break;
  default:
    break;
  };

  /* check numeric conversion */
  switch(arg_type) {
  case ARG_INT:
  case ARG_DOUBLE:
    if (val && !(stop_char && *stop_char == '\0')) {
      fprintf(stderr, "%s: invalid numeric value: %s\n", package_name, val);
      return 1; /* failure */
    }
    break;
  default:
    ;
  };

  /* store the original value */
  switch(arg_type) {
  case ARG_NO:
    break;
  default:
    if (value && orig_field) {
      if (no_free) {
        *orig_field = value;
      } else {
        if (*orig_field)
          free (*orig_field); /* free previous string */
        *orig_field = gengetopt_strdup (value);
      }
    }
  };

  return 0; /* OK */
}

/**
 * @brief store information about a multiple option in a temporary list
 * @param list where to (temporarily) store multiple options
 */
static
int update_multiple_arg_temp(struct generic_list **list,
               unsigned int *prev_given, const char *val,
               char *possible_values[], const char *default_value,
               cmdline_parser_arg_type arg_type,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  char *multi_token, *multi_next; /* store single arguments */

  if (arg_type == ARG_NO) {
    (*prev_given)++;
    return 0; /* OK */
  }

  multi_token = get_multiple_arg_token(val);
  multi_next = get_multiple_arg_token_next (val);

  while (1)
    {
      add_node (list);
      if (update_arg((void *)&((*list)->arg), &((*list)->orig), 0,
          prev_given, multi_token, possible_values, default_value, 
          arg_type, 0, 1, 1, 1, long_opt, short_opt, additional_error)) {
        if (multi_token) free(multi_token);
        return 1; /* failure */
      }

      if (multi_next)
        {
          multi_token = get_multiple_arg_token(multi_next);
          multi_next = get_multiple_arg_token_next (multi_next);
        }
      else
        break;
    }

  return 0; /* OK */
}

/**
 * @brief free the passed list (including possible string argument)
 */
static
void free_list(struct generic_list *list, short string_arg)
{
  if (list) {
    struct generic_list *tmp;
    while (list)
      {
        tmp = list;
        if (string_arg && list->arg.string_arg)
          free (list->arg.string_arg);
        if (list->orig)
          free (list->orig);
        list = list->next;
        free (tmp);
      }
  }
}

/**
 * @brief updates a multiple option starting from the passed list
 */
static
void update_multiple_arg(void *field, char ***orig_field,
               unsigned int field_given, unsigned int prev_given, union generic_value *default_value,
               cmdline_parser_arg_type arg_type,
               struct generic_list *list)
{
  int i;
  struct generic_list *tmp;

  if (prev_given && list) {
    *orig_field = (char **) realloc (*orig_field, (field_given + prev_given) * sizeof (char *));

    switch(arg_type) {
    case ARG_INT:
      *((int **)field) = (int *)realloc (*((int **)field), (field_given + prev_given) * sizeof (int)); break;
    case ARG_DOUBLE:
      *((double **)field) = (double *)realloc (*((double **)field), (field_given + prev_given) * sizeof (double)); break;
    case ARG_STRING:
      *((char ***)field) = (char **)realloc (*((char ***)field), (field_given + prev_given) * sizeof (char *)); break;
    default:
      break;
    };
    
    for (i = (prev_given - 1); i >= 0; --i)
      {
        tmp = list;
        
        switch(arg_type) {
        case ARG_INT:
          (*((int **)field))[i + field_given] = tmp->arg.int_arg; break;
        case ARG_DOUBLE:
          (*((double **)field))[i + field_given] = tmp->arg.double_arg; break;
        case ARG_STRING:
          (*((char ***)field))[i + field_given] = tmp->arg.string_arg; break;
        default:
          break;
        }        
        (*orig_field) [i + field_given] = list->orig;
        list = list->next;
        free (tmp);
      }
  } else { /* set the default value */
    if (default_value && ! field_given) {
      switch(arg_type) {
      case ARG_INT:
        if (! *((int **)field)) {
          *((int **)field) = (int *)malloc (sizeof (int));
          (*((int **)field))[0] = default_value->int_arg; 
        }
        break;
      case ARG_DOUBLE:
        if (! *((double **)field)) {
          *((double **)field) = (double *)malloc (sizeof (double));
          (*((double **)field))[0] = default_value->double_arg;
        }
        break;
      case ARG_STRING:
        if (! *((char ***)field)) {
          *((char ***)field) = (char **)malloc (sizeof (char *));
          (*((char ***)field))[0] = gengetopt_strdup(default_value->string_arg);
        }
        break;
      default: break;
      }
      if (!(*orig_field)) {
        *orig_field = (char **) malloc (sizeof (char *));
        (*orig_field)[0] = NULL;
      }
    }
  }
}

int
cmdline_parser_internal (int argc, char * const *argv, struct gengetopt_args_info *args_info,
                        struct cmdline_parser_params *params, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  struct generic_list * config_list = NULL;
  int error = 0;
  struct gengetopt_args_info local_args_info;
  
  int override;
  int initialize;
  int check_required;
  int check_ambiguity;
  
  package_name = argv[0];
  
  override = params->override;
  initialize = params->initialize;
  check_required = params->check_required;
  check_ambiguity = params->check_ambiguity;

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 0;
  opterr = params->print_errors;
  optopt = '?';

  while (1)
    {
      int option_index = 0;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "config",	1, NULL, 'c' },
        { "label",	1, NULL, 0 },
        { "sky-grid",	1, NULL, 0 },
        { "skymap-orientation",	1, NULL, 0 },
        { "skyband-method",	1, NULL, 0 },
        { "nskybands",	1, NULL, 0 },
        { "large-S",	1, NULL, 0 },
        { "band-axis",	1, NULL, 0 },
        { "band-axis-norm",	1, NULL, 0 },
        { "sky-marks-file",	1, NULL, 0 },
        { "fine-factor",	1, NULL, 0 },
        { "skymap-resolution",	1, NULL, 0 },
        { "skymap-resolution-ratio",	1, NULL, 0 },
        { "small-weight-ratio",	1, NULL, 0 },
        { "strain-norm-factor",	1, NULL, 0 },
        { "lock-file",	1, NULL, 0 },
        { "enable-dataset-locking",	1, NULL, 0 },
        { "retry-delay",	1, NULL, 0 },
        { "lock-retry-delay",	1, NULL, 0 },
        { "dataset",	1, NULL, 's' },
        { "initial-dataset-seed",	1, NULL, 'i' },
        { "input-format",	1, NULL, 0 },
        { "dump-data",	1, NULL, 0 },
        { "dump-sftv2",	1, NULL, 0 },
        { "output",	1, NULL, 'o' },
        { "ephemeris-path",	1, NULL, 0 },
        { "earth-ephemeris",	1, NULL, 0 },
        { "sun-ephemeris",	1, NULL, 0 },
        { "first-bin",	1, NULL, 'f' },
        { "nbins",	1, NULL, 'n' },
        { "side-cut",	1, NULL, 0 },
        { "expected-timebase",	1, NULL, 0 },
        { "hist-bins",	1, NULL, 0 },
        { "detector",	1, NULL, 'd' },
        { "doppler-multiplier",	1, NULL, 0 },
        { "spindown-start-time",	1, NULL, 0 },
        { "frequency-offset",	1, NULL, 0 },
        { "spindown-start",	1, NULL, 0 },
        { "spindown-step",	1, NULL, 0 },
        { "spindown-count",	1, NULL, 0 },
        { "fdotdot",	1, NULL, 0 },
        { "orientation",	1, NULL, 0 },
        { "nlinear-polarizations",	1, NULL, 0 },
        { "no-demodulation",	1, NULL, 0 },
        { "no-decomposition",	1, NULL, 0 },
        { "no-candidates",	1, NULL, 0 },
        { "no-am-response",	1, NULL, 0 },
        { "no-secondary-skymaps",	1, NULL, 0 },
        { "averaging-mode",	1, NULL, 0 },
        { "subtract-background",	1, NULL, 0 },
        { "do-cutoff",	1, NULL, 0 },
        { "filter-lines",	1, NULL, 0 },
        { "ks-test",	1, NULL, 0 },
        { "compute-betas",	1, NULL, 0 },
        { "upper-limit-comp",	1, NULL, 0 },
        { "lower-limit-comp",	1, NULL, 0 },
        { "write-dat",	1, NULL, 0 },
        { "write-png",	1, NULL, 0 },
        { "dump-points",	1, NULL, 0 },
        { "dump-candidates",	1, NULL, 0 },
        { "focus-ra",	1, NULL, 0 },
        { "focus-dec",	1, NULL, 0 },
        { "focus-radius",	1, NULL, 0 },
        { "only-large-cos",	1, NULL, 0 },
        { "fake-linear",	0, NULL, 0 },
        { "fake-circular",	0, NULL, 0 },
        { "fake-ref-time",	1, NULL, 0 },
        { "fake-ra",	1, NULL, 0 },
        { "fake-dec",	1, NULL, 0 },
        { "fake-iota",	1, NULL, 0 },
        { "fake-psi",	1, NULL, 0 },
        { "fake-phi",	1, NULL, 0 },
        { "fake-spindown",	1, NULL, 0 },
        { "fake-strain",	1, NULL, 0 },
        { "fake-freq",	1, NULL, 0 },
        { "snr-precision",	1, NULL, 0 },
        { "max-candidates",	1, NULL, 0 },
        { "min-candidate-snr",	1, NULL, 0 },
        { "output-initial",	1, NULL, 0 },
        { "output-optimized",	1, NULL, 0 },
        { "output-cache",	1, NULL, 0 },
        { "extended-test",	1, NULL, 0 },
        { "max-sft-report",	1, NULL, 0 },
        { "num-threads",	1, NULL, 0 },
        { "niota",	1, NULL, 0 },
        { "npsi",	1, NULL, 0 },
        { "nfshift",	1, NULL, 0 },
        { "nchunks",	1, NULL, 0 },
        { "weight-cutoff-fraction",	1, NULL, 0 },
        { "per-dataset-weight-cutoff-fraction",	1, NULL, 0 },
        { "power-max-median-factor",	1, NULL, 0 },
        { "tmedian-noise-level",	1, NULL, 0 },
        { "summing-step",	1, NULL, 0 },
        { "max-first-shift",	1, NULL, 0 },
        { "statistics-function",	1, NULL, 0 },
        { "dump-power-sums",	1, NULL, 0 },
        { "compute-skymaps",	1, NULL, 0 },
        { "fine-grid-skymarks",	1, NULL, 0 },
        { "half-window",	1, NULL, 0 },
        { "tail-veto",	1, NULL, 0 },
        { "cache-granularity",	1, NULL, 0 },
        { NULL,	0, NULL, 0 }
      };

      c = getopt_long (argc, argv, "hVc:s:i:o:f:n:d:", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          cmdline_parser_print_version ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'c':	/* configuration file (in gengetopt format) to pass parameters.  */
        
          if (update_multiple_arg_temp(&config_list, 
              &(local_args_info.config_given), optarg, 0, 0, ARG_STRING,
              "config", 'c',
              additional_error))
            goto failure;
        
          break;
        case 's':	/* dataset file.  */
        
        
          if (update_arg( (void *)&(args_info->dataset_arg), 
               &(args_info->dataset_orig), &(args_info->dataset_given),
              &(local_args_info.dataset_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "dataset", 's',
              additional_error))
            goto failure;
        
          break;
        case 'i':	/* initial seed to use for generating gaussian data.  */
        
        
          if (update_arg( (void *)&(args_info->initial_dataset_seed_arg), 
               &(args_info->initial_dataset_seed_orig), &(args_info->initial_dataset_seed_given),
              &(local_args_info.initial_dataset_seed_given), optarg, 0, "12345", ARG_INT,
              check_ambiguity, override, 0, 0,
              "initial-dataset-seed", 'i',
              additional_error))
            goto failure;
        
          break;
        case 'o':	/* output directory.  */
        
        
          if (update_arg( (void *)&(args_info->output_arg), 
               &(args_info->output_orig), &(args_info->output_given),
              &(local_args_info.output_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "output", 'o',
              additional_error))
            goto failure;
        
          break;
        case 'f':	/* first frequency bin in the band to be analyzed.  */
        
        
          if (update_arg( (void *)&(args_info->first_bin_arg), 
               &(args_info->first_bin_orig), &(args_info->first_bin_given),
              &(local_args_info.first_bin_given), optarg, 0, 0, ARG_INT,
              check_ambiguity, override, 0, 0,
              "first-bin", 'f',
              additional_error))
            goto failure;
        
          break;
        case 'n':	/* number of frequency bins to analyze.  */
        
        
          if (update_arg( (void *)&(args_info->nbins_arg), 
               &(args_info->nbins_orig), &(args_info->nbins_given),
              &(local_args_info.nbins_given), optarg, 0, "501", ARG_INT,
              check_ambiguity, override, 0, 0,
              "nbins", 'n',
              additional_error))
            goto failure;
        
          break;
        case 'd':	/* detector location (i.e. LHO or LLO), passed to detresponse.  */
        
        
          if (update_arg( (void *)&(args_info->detector_arg), 
               &(args_info->detector_orig), &(args_info->detector_given),
              &(local_args_info.detector_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "detector", 'd',
              additional_error))
            goto failure;
        
          break;

        case 0:	/* Long option with no short option */
          /* arbitrary string to be printed in the beginning of PowerFlux log file.  */
          if (strcmp (long_options[option_index].name, "label") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->label_arg), 
                 &(args_info->label_orig), &(args_info->label_given),
                &(local_args_info.label_given), optarg, 0, "", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "label", '-',
                additional_error))
              goto failure;
          
          }
          /* sky grid type (arcsin, plain_rectangular, sin_theta).  */
          else if (strcmp (long_options[option_index].name, "sky-grid") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->sky_grid_arg), 
                 &(args_info->sky_grid_orig), &(args_info->sky_grid_given),
                &(local_args_info.sky_grid_given), optarg, 0, "sin_theta", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "sky-grid", '-',
                additional_error))
              goto failure;
          
          }
          /* orientation of produced skymaps: equatorial, ecliptic, band_axis.  */
          else if (strcmp (long_options[option_index].name, "skymap-orientation") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->skymap_orientation_arg), 
                 &(args_info->skymap_orientation_orig), &(args_info->skymap_orientation_given),
                &(local_args_info.skymap_orientation_given), optarg, 0, "equatorial", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "skymap-orientation", '-',
                additional_error))
              goto failure;
          
          }
          /* method of assigning band numbers: angle, S.  */
          else if (strcmp (long_options[option_index].name, "skyband-method") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->skyband_method_arg), 
                 &(args_info->skyband_method_orig), &(args_info->skyband_method_given),
                &(local_args_info.skyband_method_given), optarg, 0, "S", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "skyband-method", '-',
                additional_error))
              goto failure;
          
          }
          /* split sky in this many bands for logging maximum upper limits.  */
          else if (strcmp (long_options[option_index].name, "nskybands") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->nskybands_arg), 
                 &(args_info->nskybands_orig), &(args_info->nskybands_given),
                &(local_args_info.nskybands_given), optarg, 0, "11", ARG_INT,
                check_ambiguity, override, 0, 0,
                "nskybands", '-',
                additional_error))
              goto failure;
          
          }
          /* value of S to consider good enough.  */
          else if (strcmp (long_options[option_index].name, "large-S") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->large_S_arg), 
                 &(args_info->large_S_orig), &(args_info->large_S_given),
                &(local_args_info.large_S_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "large-S", '-',
                additional_error))
              goto failure;
          
          }
          /* which band axis to use for splitting sky into bands (perpendicular to band axis) (possible values: equatorial, auto, explicit(float,float,float).  */
          else if (strcmp (long_options[option_index].name, "band-axis") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->band_axis_arg), 
                 &(args_info->band_axis_orig), &(args_info->band_axis_given),
                &(local_args_info.band_axis_given), optarg, 0, "auto", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "band-axis", '-',
                additional_error))
              goto failure;
          
          }
          /* norm of band axis vector to use in S value calculation.  */
          else if (strcmp (long_options[option_index].name, "band-axis-norm") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->band_axis_norm_arg), 
                 &(args_info->band_axis_norm_orig), &(args_info->band_axis_norm_given),
                &(local_args_info.band_axis_norm_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "band-axis-norm", '-',
                additional_error))
              goto failure;
          
          }
          /* file describing how to mark up a sky.  */
          else if (strcmp (long_options[option_index].name, "sky-marks-file") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->sky_marks_file_arg), 
                 &(args_info->sky_marks_file_orig), &(args_info->sky_marks_file_given),
                &(local_args_info.sky_marks_file_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "sky-marks-file", '-',
                additional_error))
              goto failure;
          
          }
          /* make fine grid this times finer.  */
          else if (strcmp (long_options[option_index].name, "fine-factor") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fine_factor_arg), 
                 &(args_info->fine_factor_orig), &(args_info->fine_factor_given),
                &(local_args_info.fine_factor_given), optarg, 0, "5", ARG_INT,
                check_ambiguity, override, 0, 0,
                "fine-factor", '-',
                additional_error))
              goto failure;
          
          }
          /* specify skymap resolution explicitly.  */
          else if (strcmp (long_options[option_index].name, "skymap-resolution") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->skymap_resolution_arg), 
                 &(args_info->skymap_resolution_orig), &(args_info->skymap_resolution_given),
                &(local_args_info.skymap_resolution_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "skymap-resolution", '-',
                additional_error))
              goto failure;
          
          }
          /* adjust default coarseness of the grid by this factor.  */
          else if (strcmp (long_options[option_index].name, "skymap-resolution-ratio") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->skymap_resolution_ratio_arg), 
                 &(args_info->skymap_resolution_ratio_orig), &(args_info->skymap_resolution_ratio_given),
                &(local_args_info.skymap_resolution_ratio_given), optarg, 0, "1.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "skymap-resolution-ratio", '-',
                additional_error))
              goto failure;
          
          }
          /* ratio that determines which weight is too small to include in max statistics.  */
          else if (strcmp (long_options[option_index].name, "small-weight-ratio") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->small_weight_ratio_arg), 
                 &(args_info->small_weight_ratio_orig), &(args_info->small_weight_ratio_given),
                &(local_args_info.small_weight_ratio_given), optarg, 0, "0.2", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "small-weight-ratio", '-',
                additional_error))
              goto failure;
          
          }
          /* strain normalization factor to prevent overflowing of the exponent.  */
          else if (strcmp (long_options[option_index].name, "strain-norm-factor") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->strain_norm_factor_arg), 
                 &(args_info->strain_norm_factor_orig), &(args_info->strain_norm_factor_given),
                &(local_args_info.strain_norm_factor_given), optarg, 0, "1e-20", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "strain-norm-factor", '-',
                additional_error))
              goto failure;
          
          }
          /* file to lock when reading SFTs in order to globally serialize disk access.  */
          else if (strcmp (long_options[option_index].name, "lock-file") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->lock_file_arg), 
                 &(args_info->lock_file_orig), &(args_info->lock_file_given),
                &(local_args_info.lock_file_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "lock-file", '-',
                additional_error))
              goto failure;
          
          }
          /* set to 1 to enable dataset level locking.  */
          else if (strcmp (long_options[option_index].name, "enable-dataset-locking") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->enable_dataset_locking_arg), 
                 &(args_info->enable_dataset_locking_orig), &(args_info->enable_dataset_locking_given),
                &(local_args_info.enable_dataset_locking_given), optarg, 0, "1", ARG_INT,
                check_ambiguity, override, 0, 0,
                "enable-dataset-locking", '-',
                additional_error))
              goto failure;
          
          }
          /* number of seconds to wait before retrying I/O.  */
          else if (strcmp (long_options[option_index].name, "retry-delay") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->retry_delay_arg), 
                 &(args_info->retry_delay_orig), &(args_info->retry_delay_given),
                &(local_args_info.retry_delay_given), optarg, 0, "2", ARG_INT,
                check_ambiguity, override, 0, 0,
                "retry-delay", '-',
                additional_error))
              goto failure;
          
          }
          /* number of seconds to wait before trying to acquire lock again.  */
          else if (strcmp (long_options[option_index].name, "lock-retry-delay") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->lock_retry_delay_arg), 
                 &(args_info->lock_retry_delay_orig), &(args_info->lock_retry_delay_given),
                &(local_args_info.lock_retry_delay_given), optarg, 0, "10", ARG_INT,
                check_ambiguity, override, 0, 0,
                "lock-retry-delay", '-',
                additional_error))
              goto failure;
          
          }
          /* format of input files (GEO, SFT, Power).  */
          else if (strcmp (long_options[option_index].name, "input-format") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->input_format_arg), 
                 &(args_info->input_format_orig), &(args_info->input_format_given),
                &(local_args_info.input_format_given), optarg, 0, "GEO", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "input-format", '-',
                additional_error))
              goto failure;
          
          }
          /* file to output loaded SFT data into, for testing.  */
          else if (strcmp (long_options[option_index].name, "dump-data") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->dump_data_arg), 
                 &(args_info->dump_data_orig), &(args_info->dump_data_given),
                &(local_args_info.dump_data_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "dump-data", '-',
                additional_error))
              goto failure;
          
          }
          /* directory to output loaded data, together with dataset description.  */
          else if (strcmp (long_options[option_index].name, "dump-sftv2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->dump_sftv2_arg), 
                 &(args_info->dump_sftv2_orig), &(args_info->dump_sftv2_given),
                &(local_args_info.dump_sftv2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "dump-sftv2", '-',
                additional_error))
              goto failure;
          
          }
          /* path to detresponse program from lalapps.  */
          else if (strcmp (long_options[option_index].name, "ephemeris-path") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->ephemeris_path_arg), 
                 &(args_info->ephemeris_path_orig), &(args_info->ephemeris_path_given),
                &(local_args_info.ephemeris_path_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "ephemeris-path", '-',
                additional_error))
              goto failure;
          
          }
          /* Earth ephemeris file, overrides ephemeris-path argument.  */
          else if (strcmp (long_options[option_index].name, "earth-ephemeris") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->earth_ephemeris_arg), 
                 &(args_info->earth_ephemeris_orig), &(args_info->earth_ephemeris_given),
                &(local_args_info.earth_ephemeris_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "earth-ephemeris", '-',
                additional_error))
              goto failure;
          
          }
          /* Sun ephemeris file, overrides ephemeris-path argument.  */
          else if (strcmp (long_options[option_index].name, "sun-ephemeris") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->sun_ephemeris_arg), 
                 &(args_info->sun_ephemeris_orig), &(args_info->sun_ephemeris_given),
                &(local_args_info.sun_ephemeris_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "sun-ephemeris", '-',
                additional_error))
              goto failure;
          
          }
          /* number of bins to cut from each side due to corruption from doppler shifts.  */
          else if (strcmp (long_options[option_index].name, "side-cut") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->side_cut_arg), 
                 &(args_info->side_cut_orig), &(args_info->side_cut_given),
                &(local_args_info.side_cut_given), optarg, 0, 0, ARG_INT,
                check_ambiguity, override, 0, 0,
                "side-cut", '-',
                additional_error))
              goto failure;
          
          }
          /* expected timebase in months.  */
          else if (strcmp (long_options[option_index].name, "expected-timebase") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->expected_timebase_arg), 
                 &(args_info->expected_timebase_orig), &(args_info->expected_timebase_given),
                &(local_args_info.expected_timebase_given), optarg, 0, "6", ARG_INT,
                check_ambiguity, override, 0, 0,
                "expected-timebase", '-',
                additional_error))
              goto failure;
          
          }
          /* number of bins to use when producing histograms.  */
          else if (strcmp (long_options[option_index].name, "hist-bins") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->hist_bins_arg), 
                 &(args_info->hist_bins_orig), &(args_info->hist_bins_given),
                &(local_args_info.hist_bins_given), optarg, 0, "200", ARG_INT,
                check_ambiguity, override, 0, 0,
                "hist-bins", '-',
                additional_error))
              goto failure;
          
          }
          /* a constant to multiply Doppler shifts by (1.0 corresponds to standard physics).  */
          else if (strcmp (long_options[option_index].name, "doppler-multiplier") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->doppler_multiplier_arg), 
                 &(args_info->doppler_multiplier_orig), &(args_info->doppler_multiplier_given),
                &(local_args_info.doppler_multiplier_given), optarg, 0, "1.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "doppler-multiplier", '-',
                additional_error))
              goto failure;
          
          }
          /* specify spindown start time in GPS sec. Assumed to be the first SFT segment by default.  */
          else if (strcmp (long_options[option_index].name, "spindown-start-time") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->spindown_start_time_arg), 
                 &(args_info->spindown_start_time_orig), &(args_info->spindown_start_time_given),
                &(local_args_info.spindown_start_time_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "spindown-start-time", '-',
                additional_error))
              goto failure;
          
          }
          /* (small) frequency offset - used to achieve fractional bin shifts.  */
          else if (strcmp (long_options[option_index].name, "frequency-offset") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->frequency_offset_arg), 
                 &(args_info->frequency_offset_orig), &(args_info->frequency_offset_given),
                &(local_args_info.frequency_offset_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "frequency-offset", '-',
                additional_error))
              goto failure;
          
          }
          /* first spindown value to process.  */
          else if (strcmp (long_options[option_index].name, "spindown-start") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->spindown_start_arg), 
                 &(args_info->spindown_start_orig), &(args_info->spindown_start_given),
                &(local_args_info.spindown_start_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "spindown-start", '-',
                additional_error))
              goto failure;
          
          }
          /* step for processing multiple spindown values.  */
          else if (strcmp (long_options[option_index].name, "spindown-step") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->spindown_step_arg), 
                 &(args_info->spindown_step_orig), &(args_info->spindown_step_given),
                &(local_args_info.spindown_step_given), optarg, 0, "5e-10", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "spindown-step", '-',
                additional_error))
              goto failure;
          
          }
          /* how many separate spindown values to process.  */
          else if (strcmp (long_options[option_index].name, "spindown-count") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->spindown_count_arg), 
                 &(args_info->spindown_count_orig), &(args_info->spindown_count_given),
                &(local_args_info.spindown_count_given), optarg, 0, "1", ARG_INT,
                check_ambiguity, override, 0, 0,
                "spindown-count", '-',
                additional_error))
              goto failure;
          
          }
          /* second frequency derivative.  */
          else if (strcmp (long_options[option_index].name, "fdotdot") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fdotdot_arg), 
                 &(args_info->fdotdot_orig), &(args_info->fdotdot_given),
                &(local_args_info.fdotdot_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fdotdot", '-',
                additional_error))
              goto failure;
          
          }
          /* additional orientation phase, specifying 0.7853 will turn plus into cross.  */
          else if (strcmp (long_options[option_index].name, "orientation") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->orientation_arg), 
                 &(args_info->orientation_orig), &(args_info->orientation_given),
                &(local_args_info.orientation_given), optarg, 0, "0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "orientation", '-',
                additional_error))
              goto failure;
          
          }
          /* even number of linear polarizations to profile, distributed uniformly between 0 and PI/2.  */
          else if (strcmp (long_options[option_index].name, "nlinear-polarizations") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->nlinear_polarizations_arg), 
                 &(args_info->nlinear_polarizations_orig), &(args_info->nlinear_polarizations_given),
                &(local_args_info.nlinear_polarizations_given), optarg, 0, "4", ARG_INT,
                check_ambiguity, override, 0, 0,
                "nlinear-polarizations", '-',
                additional_error))
              goto failure;
          
          }
          /* do not perform demodulation stage, analyze background only.  */
          else if (strcmp (long_options[option_index].name, "no-demodulation") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->no_demodulation_arg), 
                 &(args_info->no_demodulation_orig), &(args_info->no_demodulation_given),
                &(local_args_info.no_demodulation_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "no-demodulation", '-',
                additional_error))
              goto failure;
          
          }
          /* do not perform noise decomposition stage, output simple statistics only.  */
          else if (strcmp (long_options[option_index].name, "no-decomposition") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->no_decomposition_arg), 
                 &(args_info->no_decomposition_orig), &(args_info->no_decomposition_given),
                &(local_args_info.no_decomposition_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "no-decomposition", '-',
                additional_error))
              goto failure;
          
          }
          /* do not perform analysis to identify candidates.  */
          else if (strcmp (long_options[option_index].name, "no-candidates") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->no_candidates_arg), 
                 &(args_info->no_candidates_orig), &(args_info->no_candidates_given),
                &(local_args_info.no_candidates_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "no-candidates", '-',
                additional_error))
              goto failure;
          
          }
          /* force AM_response() function to return 1.0 irrespective of the arguments.  */
          else if (strcmp (long_options[option_index].name, "no-am-response") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->no_am_response_arg), 
                 &(args_info->no_am_response_orig), &(args_info->no_am_response_given),
                &(local_args_info.no_am_response_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "no-am-response", '-',
                additional_error))
              goto failure;
          
          }
          /* do not store values not essential for upper limits and followup.  */
          else if (strcmp (long_options[option_index].name, "no-secondary-skymaps") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->no_secondary_skymaps_arg), 
                 &(args_info->no_secondary_skymaps_orig), &(args_info->no_secondary_skymaps_given),
                &(local_args_info.no_secondary_skymaps_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "no-secondary-skymaps", '-',
                additional_error))
              goto failure;
          
          }
          /* 1 - use one bin, 3 - average 3, matched - use 7 bin matched filter.  */
          else if (strcmp (long_options[option_index].name, "averaging-mode") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->averaging_mode_arg), 
                 &(args_info->averaging_mode_orig), &(args_info->averaging_mode_given),
                &(local_args_info.averaging_mode_given), optarg, 0, "1", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "averaging-mode", '-',
                additional_error))
              goto failure;
          
          }
          /* subtract rank 1 matrix in order to flatten noise spectrum.  */
          else if (strcmp (long_options[option_index].name, "subtract-background") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->subtract_background_arg), 
                 &(args_info->subtract_background_orig), &(args_info->subtract_background_given),
                &(local_args_info.subtract_background_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "subtract-background", '-',
                additional_error))
              goto failure;
          
          }
          /* neglect contribution from SFT with high effective noise level.  */
          else if (strcmp (long_options[option_index].name, "do-cutoff") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->do_cutoff_arg), 
                 &(args_info->do_cutoff_orig), &(args_info->do_cutoff_given),
                &(local_args_info.do_cutoff_given), optarg, 0, "1", ARG_INT,
                check_ambiguity, override, 0, 0,
                "do-cutoff", '-',
                additional_error))
              goto failure;
          
          }
          /* perform detection of lines in background noise and veto corresponding frequency bins.  */
          else if (strcmp (long_options[option_index].name, "filter-lines") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->filter_lines_arg), 
                 &(args_info->filter_lines_orig), &(args_info->filter_lines_given),
                &(local_args_info.filter_lines_given), optarg, 0, "1", ARG_INT,
                check_ambiguity, override, 0, 0,
                "filter-lines", '-',
                additional_error))
              goto failure;
          
          }
          /* perform Kolmogorov-Smirnov test for normality of averaged powers.  */
          else if (strcmp (long_options[option_index].name, "ks-test") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->ks_test_arg), 
                 &(args_info->ks_test_orig), &(args_info->ks_test_given),
                &(local_args_info.ks_test_given), optarg, 0, "1", ARG_INT,
                check_ambiguity, override, 0, 0,
                "ks-test", '-',
                additional_error))
              goto failure;
          
          }
          /* compute beta coefficients as described in PowerFlux polarizations document.  */
          else if (strcmp (long_options[option_index].name, "compute-betas") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->compute_betas_arg), 
                 &(args_info->compute_betas_orig), &(args_info->compute_betas_given),
                &(local_args_info.compute_betas_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "compute-betas", '-',
                additional_error))
              goto failure;
          
          }
          /* upper limit compensation factor - used to account for windowing in SFTs (possible values: Hann, flat, arbitrary number).  */
          else if (strcmp (long_options[option_index].name, "upper-limit-comp") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->upper_limit_comp_arg), 
                 &(args_info->upper_limit_comp_orig), &(args_info->upper_limit_comp_given),
                &(local_args_info.upper_limit_comp_given), optarg, 0, "Hann", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "upper-limit-comp", '-',
                additional_error))
              goto failure;
          
          }
          /* lower limit compensation factor - used to account for windowing in SFTs (possible values: Hann, flat, arbitrary number).  */
          else if (strcmp (long_options[option_index].name, "lower-limit-comp") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->lower_limit_comp_arg), 
                 &(args_info->lower_limit_comp_orig), &(args_info->lower_limit_comp_given),
                &(local_args_info.lower_limit_comp_given), optarg, 0, "Hann", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "lower-limit-comp", '-',
                additional_error))
              goto failure;
          
          }
          /* regular expression describing which *.dat files to write.  */
          else if (strcmp (long_options[option_index].name, "write-dat") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->write_dat_arg), 
                 &(args_info->write_dat_orig), &(args_info->write_dat_given),
                &(local_args_info.write_dat_given), optarg, 0, ".*", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "write-dat", '-',
                additional_error))
              goto failure;
          
          }
          /* regular expression describing which *.png files to write.  */
          else if (strcmp (long_options[option_index].name, "write-png") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->write_png_arg), 
                 &(args_info->write_png_orig), &(args_info->write_png_given),
                &(local_args_info.write_png_given), optarg, 0, ".*", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "write-png", '-',
                additional_error))
              goto failure;
          
          }
          /* output averaged power bins for each point in the sky.  */
          else if (strcmp (long_options[option_index].name, "dump-points") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->dump_points_arg), 
                 &(args_info->dump_points_orig), &(args_info->dump_points_given),
                &(local_args_info.dump_points_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "dump-points", '-',
                additional_error))
              goto failure;
          
          }
          /* output SFT data for first N candidates.  */
          else if (strcmp (long_options[option_index].name, "dump-candidates") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->dump_candidates_arg), 
                 &(args_info->dump_candidates_orig), &(args_info->dump_candidates_given),
                &(local_args_info.dump_candidates_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "dump-candidates", '-',
                additional_error))
              goto failure;
          
          }
          /* focus computation on a circular area with center at this RA.  */
          else if (strcmp (long_options[option_index].name, "focus-ra") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->focus_ra_arg), 
                 &(args_info->focus_ra_orig), &(args_info->focus_ra_given),
                &(local_args_info.focus_ra_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "focus-ra", '-',
                additional_error))
              goto failure;
          
          }
          /* focus computation on a circular area with center at this DEC.  */
          else if (strcmp (long_options[option_index].name, "focus-dec") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->focus_dec_arg), 
                 &(args_info->focus_dec_orig), &(args_info->focus_dec_given),
                &(local_args_info.focus_dec_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "focus-dec", '-',
                additional_error))
              goto failure;
          
          }
          /* focus computation on a circular area with this radius.  */
          else if (strcmp (long_options[option_index].name, "focus-radius") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->focus_radius_arg), 
                 &(args_info->focus_radius_orig), &(args_info->focus_radius_given),
                &(local_args_info.focus_radius_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "focus-radius", '-',
                additional_error))
              goto failure;
          
          }
          /* restrict computation to points on the sky with cos of angle to band axis larger than a given number.  */
          else if (strcmp (long_options[option_index].name, "only-large-cos") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->only_large_cos_arg), 
                 &(args_info->only_large_cos_orig), &(args_info->only_large_cos_given),
                &(local_args_info.only_large_cos_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "only-large-cos", '-',
                additional_error))
              goto failure;
          
          }
          /* Inject linearly polarized fake signal.  */
          else if (strcmp (long_options[option_index].name, "fake-linear") == 0)
          {
          
            if (args_info->injection_group_counter && override)
              reset_group_injection (args_info);
            args_info->injection_group_counter += 1;
          
            if (update_arg( 0 , 
                 0 , &(args_info->fake_linear_given),
                &(local_args_info.fake_linear_given), optarg, 0, 0, ARG_NO,
                check_ambiguity, override, 0, 0,
                "fake-linear", '-',
                additional_error))
              goto failure;
          
          }
          /* Inject circularly polarized fake signal.  */
          else if (strcmp (long_options[option_index].name, "fake-circular") == 0)
          {
          
            if (args_info->injection_group_counter && override)
              reset_group_injection (args_info);
            args_info->injection_group_counter += 1;
          
            if (update_arg( 0 , 
                 0 , &(args_info->fake_circular_given),
                &(local_args_info.fake_circular_given), optarg, 0, 0, ARG_NO,
                check_ambiguity, override, 0, 0,
                "fake-circular", '-',
                additional_error))
              goto failure;
          
          }
          /* time of signal start.  */
          else if (strcmp (long_options[option_index].name, "fake-ref-time") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fake_ref_time_arg), 
                 &(args_info->fake_ref_time_orig), &(args_info->fake_ref_time_given),
                &(local_args_info.fake_ref_time_given), optarg, 0, "0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fake-ref-time", '-',
                additional_error))
              goto failure;
          
          }
          /* RA of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-ra") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fake_ra_arg), 
                 &(args_info->fake_ra_orig), &(args_info->fake_ra_given),
                &(local_args_info.fake_ra_given), optarg, 0, "3.14", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fake-ra", '-',
                additional_error))
              goto failure;
          
          }
          /* DEC of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-dec") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fake_dec_arg), 
                 &(args_info->fake_dec_orig), &(args_info->fake_dec_given),
                &(local_args_info.fake_dec_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fake-dec", '-',
                additional_error))
              goto failure;
          
          }
          /* iota of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-iota") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fake_iota_arg), 
                 &(args_info->fake_iota_orig), &(args_info->fake_iota_given),
                &(local_args_info.fake_iota_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fake-iota", '-',
                additional_error))
              goto failure;
          
          }
          /* orientation of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-psi") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fake_psi_arg), 
                 &(args_info->fake_psi_orig), &(args_info->fake_psi_given),
                &(local_args_info.fake_psi_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fake-psi", '-',
                additional_error))
              goto failure;
          
          }
          /* phase of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-phi") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fake_phi_arg), 
                 &(args_info->fake_phi_orig), &(args_info->fake_phi_given),
                &(local_args_info.fake_phi_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fake-phi", '-',
                additional_error))
              goto failure;
          
          }
          /* spindown of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-spindown") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fake_spindown_arg), 
                 &(args_info->fake_spindown_orig), &(args_info->fake_spindown_given),
                &(local_args_info.fake_spindown_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fake-spindown", '-',
                additional_error))
              goto failure;
          
          }
          /* amplitude of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-strain") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fake_strain_arg), 
                 &(args_info->fake_strain_orig), &(args_info->fake_strain_given),
                &(local_args_info.fake_strain_given), optarg, 0, "1e-23", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fake-strain", '-',
                additional_error))
              goto failure;
          
          }
          /* frequency of fake signal to inject.  */
          else if (strcmp (long_options[option_index].name, "fake-freq") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fake_freq_arg), 
                 &(args_info->fake_freq_orig), &(args_info->fake_freq_given),
                &(local_args_info.fake_freq_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "fake-freq", '-',
                additional_error))
              goto failure;
          
          }
          /* Assumed level of error in detection strength - used for listing candidates.  */
          else if (strcmp (long_options[option_index].name, "snr-precision") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->snr_precision_arg), 
                 &(args_info->snr_precision_orig), &(args_info->snr_precision_given),
                &(local_args_info.snr_precision_given), optarg, 0, "0.2", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "snr-precision", '-',
                additional_error))
              goto failure;
          
          }
          /* Do not optimize more than this number of candidates.  */
          else if (strcmp (long_options[option_index].name, "max-candidates") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->max_candidates_arg), 
                 &(args_info->max_candidates_orig), &(args_info->max_candidates_given),
                &(local_args_info.max_candidates_given), optarg, 0, "-1", ARG_INT,
                check_ambiguity, override, 0, 0,
                "max-candidates", '-',
                additional_error))
              goto failure;
          
          }
          /* Do not optimize candidates with SNR below this level.  */
          else if (strcmp (long_options[option_index].name, "min-candidate-snr") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->min_candidate_snr_arg), 
                 &(args_info->min_candidate_snr_orig), &(args_info->min_candidate_snr_given),
                &(local_args_info.min_candidate_snr_given), optarg, 0, "5.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "min-candidate-snr", '-',
                additional_error))
              goto failure;
          
          }
          /* write initial candidates into log file.  */
          else if (strcmp (long_options[option_index].name, "output-initial") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->output_initial_arg), 
                 &(args_info->output_initial_orig), &(args_info->output_initial_given),
                &(local_args_info.output_initial_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "output-initial", '-',
                additional_error))
              goto failure;
          
          }
          /* write optimized (second pass) candidates into log file.  */
          else if (strcmp (long_options[option_index].name, "output-optimized") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->output_optimized_arg), 
                 &(args_info->output_optimized_orig), &(args_info->output_optimized_given),
                &(local_args_info.output_optimized_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "output-optimized", '-',
                additional_error))
              goto failure;
          
          }
          /* write out all candidates in cache to log file.  */
          else if (strcmp (long_options[option_index].name, "output-cache") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->output_cache_arg), 
                 &(args_info->output_cache_orig), &(args_info->output_cache_given),
                &(local_args_info.output_cache_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "output-cache", '-',
                additional_error))
              goto failure;
          
          }
          /* Perform extended self test.  */
          else if (strcmp (long_options[option_index].name, "extended-test") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->extended_test_arg), 
                 &(args_info->extended_test_orig), &(args_info->extended_test_given),
                &(local_args_info.extended_test_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "extended-test", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum count of SFTs to report with veto information.  */
          else if (strcmp (long_options[option_index].name, "max-sft-report") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->max_sft_report_arg), 
                 &(args_info->max_sft_report_orig), &(args_info->max_sft_report_given),
                &(local_args_info.max_sft_report_given), optarg, 0, "100", ARG_INT,
                check_ambiguity, override, 0, 0,
                "max-sft-report", '-',
                additional_error))
              goto failure;
          
          }
          /* Use that many threads for computation.  */
          else if (strcmp (long_options[option_index].name, "num-threads") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->num_threads_arg), 
                 &(args_info->num_threads_orig), &(args_info->num_threads_given),
                &(local_args_info.num_threads_given), optarg, 0, "-1", ARG_INT,
                check_ambiguity, override, 0, 0,
                "num-threads", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of iota values to use in alignment grid.  */
          else if (strcmp (long_options[option_index].name, "niota") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->niota_arg), 
                 &(args_info->niota_orig), &(args_info->niota_given),
                &(local_args_info.niota_given), optarg, 0, "3", ARG_INT,
                check_ambiguity, override, 0, 0,
                "niota", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of psi values to use in alignment grid.  */
          else if (strcmp (long_options[option_index].name, "npsi") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->npsi_arg), 
                 &(args_info->npsi_orig), &(args_info->npsi_given),
                &(local_args_info.npsi_given), optarg, 0, "6", ARG_INT,
                check_ambiguity, override, 0, 0,
                "npsi", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of sub-bin frequency shifts to sample.  */
          else if (strcmp (long_options[option_index].name, "nfshift") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->nfshift_arg), 
                 &(args_info->nfshift_orig), &(args_info->nfshift_given),
                &(local_args_info.nfshift_given), optarg, 0, "2", ARG_INT,
                check_ambiguity, override, 0, 0,
                "nfshift", '-',
                additional_error))
              goto failure;
          
          }
          /* Partition the timebase into this many chunks for sub period analysis.  */
          else if (strcmp (long_options[option_index].name, "nchunks") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->nchunks_arg), 
                 &(args_info->nchunks_orig), &(args_info->nchunks_given),
                &(local_args_info.nchunks_given), optarg, 0, "5", ARG_INT,
                check_ambiguity, override, 0, 0,
                "nchunks", '-',
                additional_error))
              goto failure;
          
          }
          /* Discard sfts with small weights that contribute this fraction of total weight.  */
          else if (strcmp (long_options[option_index].name, "weight-cutoff-fraction") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->weight_cutoff_fraction_arg), 
                 &(args_info->weight_cutoff_fraction_orig), &(args_info->weight_cutoff_fraction_given),
                &(local_args_info.weight_cutoff_fraction_given), optarg, 0, "0.04", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "weight-cutoff-fraction", '-',
                additional_error))
              goto failure;
          
          }
          /* Discard sfts with small weights that contribute this fraction of total weight in each dataset.  */
          else if (strcmp (long_options[option_index].name, "per-dataset-weight-cutoff-fraction") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->per_dataset_weight_cutoff_fraction_arg), 
                 &(args_info->per_dataset_weight_cutoff_fraction_orig), &(args_info->per_dataset_weight_cutoff_fraction_given),
                &(local_args_info.per_dataset_weight_cutoff_fraction_given), optarg, 0, "0.04", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "per-dataset-weight-cutoff-fraction", '-',
                additional_error))
              goto failure;
          
          }
          /* This determines scaling factor between median and maximum of exponentially distributed variable. Used for computing power sum weights.  */
          else if (strcmp (long_options[option_index].name, "power-max-median-factor") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->power_max_median_factor_arg), 
                 &(args_info->power_max_median_factor_orig), &(args_info->power_max_median_factor_given),
                &(local_args_info.power_max_median_factor_given), optarg, 0, "0.1", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "power-max-median-factor", '-',
                additional_error))
              goto failure;
          
          }
          /* Use TMedians to estimate noise level (as opposed to in-place standard deviation).  */
          else if (strcmp (long_options[option_index].name, "tmedian-noise-level") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->tmedian_noise_level_arg), 
                 &(args_info->tmedian_noise_level_orig), &(args_info->tmedian_noise_level_given),
                &(local_args_info.tmedian_noise_level_given), optarg, 0, "1", ARG_INT,
                check_ambiguity, override, 0, 0,
                "tmedian-noise-level", '-',
                additional_error))
              goto failure;
          
          }
          /* integration step size, in seconds.  */
          else if (strcmp (long_options[option_index].name, "summing-step") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->summing_step_arg), 
                 &(args_info->summing_step_orig), &(args_info->summing_step_given),
                &(local_args_info.summing_step_given), optarg, 0, "864000", ARG_INT,
                check_ambiguity, override, 0, 0,
                "summing-step", '-',
                additional_error))
              goto failure;
          
          }
          /* larger values accomodate bigger spindown ranges but require more bins to be computed in uncached function.  */
          else if (strcmp (long_options[option_index].name, "max-first-shift") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->max_first_shift_arg), 
                 &(args_info->max_first_shift_orig), &(args_info->max_first_shift_given),
                &(local_args_info.max_first_shift_given), optarg, 0, "10", ARG_INT,
                check_ambiguity, override, 0, 0,
                "max-first-shift", '-',
                additional_error))
              goto failure;
          
          }
          /* specify statistics postprocessing to apply. Possible values: linear, sorted.  */
          else if (strcmp (long_options[option_index].name, "statistics-function") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->statistics_function_arg), 
                 &(args_info->statistics_function_orig), &(args_info->statistics_function_given),
                &(local_args_info.statistics_function_given), optarg, 0, "linear", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "statistics-function", '-',
                additional_error))
              goto failure;
          
          }
          /* Write out all power sum data into data.log file. It is recommend to restrict the sky to very few pixels.  */
          else if (strcmp (long_options[option_index].name, "dump-power-sums") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->dump_power_sums_arg), 
                 &(args_info->dump_power_sums_orig), &(args_info->dump_power_sums_given),
                &(local_args_info.dump_power_sums_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "dump-power-sums", '-',
                additional_error))
              goto failure;
          
          }
          /* allocate memory and compute skymaps with final results.  */
          else if (strcmp (long_options[option_index].name, "compute-skymaps") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->compute_skymaps_arg), 
                 &(args_info->compute_skymaps_orig), &(args_info->compute_skymaps_given),
                &(local_args_info.compute_skymaps_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "compute-skymaps", '-',
                additional_error))
              goto failure;
          
          }
          /* use sky marks from the fine grid, this uses constant spindown.  */
          else if (strcmp (long_options[option_index].name, "fine-grid-skymarks") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fine_grid_skymarks_arg), 
                 &(args_info->fine_grid_skymarks_orig), &(args_info->fine_grid_skymarks_given),
                &(local_args_info.fine_grid_skymarks_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "fine-grid-skymarks", '-',
                additional_error))
              goto failure;
          
          }
          /* number of bins to exclude to the left and to the right of highest point when computing linear statistics.  */
          else if (strcmp (long_options[option_index].name, "half-window") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->half_window_arg), 
                 &(args_info->half_window_orig), &(args_info->half_window_given),
                &(local_args_info.half_window_given), optarg, 0, "20", ARG_INT,
                check_ambiguity, override, 0, 0,
                "half-window", '-',
                additional_error))
              goto failure;
          
          }
          /* do not report outlier if its frequency is within that many bins from the tail - happens with steep spectrum.  */
          else if (strcmp (long_options[option_index].name, "tail-veto") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->tail_veto_arg), 
                 &(args_info->tail_veto_orig), &(args_info->tail_veto_given),
                &(local_args_info.tail_veto_given), optarg, 0, "10", ARG_INT,
                check_ambiguity, override, 0, 0,
                "tail-veto", '-',
                additional_error))
              goto failure;
          
          }
          /* granularity of power cache frequency shift resolution, in fractions of a frequency bin.  */
          else if (strcmp (long_options[option_index].name, "cache-granularity") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->cache_granularity_arg), 
                 &(args_info->cache_granularity_orig), &(args_info->cache_granularity_given),
                &(local_args_info.cache_granularity_given), optarg, 0, "-1", ARG_INT,
                check_ambiguity, override, 0, 0,
                "cache-granularity", '-',
                additional_error))
              goto failure;
          
          }
          
          break;
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */

  if (args_info->injection_group_counter > 1)
    {
      fprintf (stderr, "%s: %d options of group injection were given. At most one is required.%s\n", argv[0], args_info->injection_group_counter, (additional_error ? additional_error : ""));
      error = 1;
    }
  

  update_multiple_arg((void *)&(args_info->config_arg),
    &(args_info->config_orig), args_info->config_given,
    local_args_info.config_given, 0 , 
    ARG_STRING, config_list);

  args_info->config_given += local_args_info.config_given;
  local_args_info.config_given = 0;
  
  if (check_required)
    {
      error += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_release (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  free_list (config_list, 1 );
  
  cmdline_parser_release (&local_args_info);
  return (EXIT_FAILURE);
}

#ifndef CONFIG_FILE_LINE_SIZE
#define CONFIG_FILE_LINE_SIZE 2048
#endif
#define ADDITIONAL_ERROR " in configuration file "

#define CONFIG_FILE_LINE_BUFFER_SIZE (CONFIG_FILE_LINE_SIZE+3)
/* 3 is for "--" and "=" */

static int
_cmdline_parser_configfile (char * const filename, int *my_argc)
{
  FILE* file;
  char my_argv[CONFIG_FILE_LINE_BUFFER_SIZE+1];
  char linebuf[CONFIG_FILE_LINE_SIZE];
  int line_num = 0;
  int result = 0, equal;
  char *fopt, *farg;
  char *str_index;
  size_t len, next_token;
  char delimiter;

  if ((file = fopen(filename, "r")) == NULL)
    {
      fprintf (stderr, "%s: Error opening configuration file '%s'\n",
               CMDLINE_PARSER_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  while ((fgets(linebuf, CONFIG_FILE_LINE_SIZE, file)) != NULL)
    {
      ++line_num;
      my_argv[0] = '\0';
      len = strlen(linebuf);
      if (len > (CONFIG_FILE_LINE_BUFFER_SIZE-1))
        {
          fprintf (stderr, "%s:%s:%d: Line too long in configuration file\n",
                   CMDLINE_PARSER_PACKAGE, filename, line_num);
          result = EXIT_FAILURE;
          break;
        }

      /* find first non-whitespace character in the line */
      next_token = strspn (linebuf, " \t\r\n");
      str_index  = linebuf + next_token;

      if ( str_index[0] == '\0' || str_index[0] == '#')
        continue; /* empty line or comment line is skipped */

      fopt = str_index;

      /* truncate fopt at the end of the first non-valid character */
      next_token = strcspn (fopt, " \t\r\n=");

      if (fopt[next_token] == '\0') /* the line is over */
        {
          farg  = NULL;
          equal = 0;
          goto noarg;
        }

      /* remember if equal sign is present */
      equal = (fopt[next_token] == '=');
      fopt[next_token++] = '\0';

      /* advance pointers to the next token after the end of fopt */
      next_token += strspn (fopt + next_token, " \t\r\n");

      /* check for the presence of equal sign, and if so, skip it */
      if ( !equal )
        if ((equal = (fopt[next_token] == '=')))
          {
            next_token++;
            next_token += strspn (fopt + next_token, " \t\r\n");
          }
      str_index  += next_token;

      /* find argument */
      farg = str_index;
      if ( farg[0] == '\"' || farg[0] == '\'' )
        { /* quoted argument */
          str_index = strchr (++farg, str_index[0] ); /* skip opening quote */
          if (! str_index)
            {
              fprintf
                (stderr,
                 "%s:%s:%d: unterminated string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }
      else
        { /* read up the remaining part up to a delimiter */
          next_token = strcspn (farg, " \t\r\n#\'\"");
          str_index += next_token;
        }

      /* truncate farg at the delimiter and store it for further check */
      delimiter = *str_index, *str_index++ = '\0';

      /* everything but comment is illegal at the end of line */
      if (delimiter != '\0' && delimiter != '#')
        {
          str_index += strspn(str_index, " \t\r\n");
          if (*str_index != '\0' && *str_index != '#')
            {
              fprintf
                (stderr,
                 "%s:%s:%d: malformed string in configuration file\n",
                 CMDLINE_PARSER_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }

    noarg:
      if (!strcmp(fopt,"include")) {
        if (farg && *farg) {
          result = _cmdline_parser_configfile(farg, my_argc);
        } else {
          fprintf(stderr, "%s:%s:%d: include requires a filename argument.\n",
                  CMDLINE_PARSER_PACKAGE, filename, line_num);
        }
        continue;
      }
      len = strlen(fopt);
      strcat (my_argv, len > 1 ? "--" : "-");
      strcat (my_argv, fopt);
      if (len > 1 && ((farg && *farg) || equal))
        strcat (my_argv, "=");
      if (farg && *farg)
        strcat (my_argv, farg);
      ++(*my_argc);

      cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
      cmd_line_list_tmp->next = cmd_line_list;
      cmd_line_list = cmd_line_list_tmp;
      cmd_line_list->string_arg = gengetopt_strdup(my_argv);
    } /* while */

  if (file)
    fclose(file);
  return result;
}

int
cmdline_parser_configfile (char * const filename,
                           struct gengetopt_args_info *args_info,
                           int override, int initialize, int check_required)
{
  struct cmdline_parser_params params;

  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;
  
  return cmdline_parser_config_file (filename, args_info, &params);
}

int
cmdline_parser_config_file (char * const filename,
                           struct gengetopt_args_info *args_info,
                           struct cmdline_parser_params *params)
{
  int i, result;
  int my_argc = 1;
  char **my_argv_arg;
  char *additional_error;

  /* store the program name */
  cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
  cmd_line_list_tmp->next = cmd_line_list;
  cmd_line_list = cmd_line_list_tmp;
  cmd_line_list->string_arg = gengetopt_strdup (CMDLINE_PARSER_PACKAGE);

  result = _cmdline_parser_configfile(filename, &my_argc);

  if (result != EXIT_FAILURE) {
    my_argv_arg = (char **) malloc((my_argc+1) * sizeof(char *));
    cmd_line_list_tmp = cmd_line_list;

    for (i = my_argc - 1; i >= 0; --i) {
      my_argv_arg[i] = cmd_line_list_tmp->string_arg;
      cmd_line_list_tmp = cmd_line_list_tmp->next;
    }

    my_argv_arg[my_argc] = 0;

    additional_error = (char *)malloc(strlen(filename) + strlen(ADDITIONAL_ERROR) + 1);
    strcpy (additional_error, ADDITIONAL_ERROR);
    strcat (additional_error, filename);
    result =
      cmdline_parser_internal (my_argc, my_argv_arg, args_info,
                              params,
                              additional_error);

    free (additional_error);
    free (my_argv_arg);
  }

  free_cmd_list();
  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

static unsigned int
cmdline_parser_create_argv(const char *cmdline_, char ***argv_ptr, const char *prog_name)
{
  char *cmdline, *p;
  size_t n = 0, j;
  int i;

  if (prog_name) {
    cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
    cmd_line_list_tmp->next = cmd_line_list;
    cmd_line_list = cmd_line_list_tmp;
    cmd_line_list->string_arg = gengetopt_strdup (prog_name);

    ++n;
  }

  cmdline = gengetopt_strdup(cmdline_);
  p = cmdline;

  while (p && strlen(p))
    {
      j = strcspn(p, " \t");
      ++n;
      if (j && j < strlen(p))
        {
          p[j] = '\0';

          cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
          cmd_line_list_tmp->next = cmd_line_list;
          cmd_line_list = cmd_line_list_tmp;
          cmd_line_list->string_arg = gengetopt_strdup (p);

          p += (j+1);
          p += strspn(p, " \t");
        }
      else
        {
          cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
          cmd_line_list_tmp->next = cmd_line_list;
          cmd_line_list = cmd_line_list_tmp;
          cmd_line_list->string_arg = gengetopt_strdup (p);

          break;
        }
    }

  *argv_ptr = (char **) malloc((n + 1) * sizeof(char *));
  cmd_line_list_tmp = cmd_line_list;
  for (i = (n-1); i >= 0; --i)
    {
      (*argv_ptr)[i] = cmd_line_list_tmp->string_arg;
      cmd_line_list_tmp = cmd_line_list_tmp->next;
    }

  (*argv_ptr)[n] = NULL;

  free(cmdline);
  return n;
}

int
cmdline_parser_string(const char *cmdline, struct gengetopt_args_info *args_info, const char *prog_name)
{
  return cmdline_parser_string2(cmdline, args_info, prog_name, 0, 1, 1);
}

int
cmdline_parser_string2(const char *cmdline, struct gengetopt_args_info *args_info, const char *prog_name,
    int override, int initialize, int check_required)
{
  struct cmdline_parser_params params;

  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;

  return cmdline_parser_string_ext(cmdline, args_info, prog_name, &params);
}

int
cmdline_parser_string_ext(const char *cmdline, struct gengetopt_args_info *args_info, const char *prog_name,
    struct cmdline_parser_params *params)
{
  char **argv_ptr = 0;
  int result;
  unsigned int argc;
  
  argc = cmdline_parser_create_argv(cmdline, &argv_ptr, prog_name);
  
  result =
    cmdline_parser_internal (argc, argv_ptr, args_info, params, 0);
  
  if (argv_ptr)
    {
      free (argv_ptr);
    }

  free_cmd_list();
  
  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

